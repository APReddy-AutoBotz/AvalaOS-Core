# One Module at a Time

- Build only the requested module.
- Do not pull future modules into the current implementation.
- If a future dependency is needed, create a stub/interface only.
- Mark out-of-scope work clearly instead of implementing it.