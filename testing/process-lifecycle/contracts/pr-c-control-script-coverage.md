# PR C controlled-human control-script coverage contract

This feature-owned gate measures exactly seventeen governed production JavaScript control sources with Node 22's built-in test coverage. It is additive to the existing controlled-human source command: the original Node test segment, including the optional local PostgreSQL integration test, remains intact. The measured child executes exactly nineteen test files: the non-optional helper suites, the standalone scoring-law drift guard, direct-import suites for both newly governed controls, the runner self-test under its import guard, and genuine existing consumer scenarios. The real credential-preflight entrypoint fixture remains mandatory.

## Acceptance boundary

- The inventory contains exactly the seventeen source paths declared by `scripts/runPrCControlledHumanScriptCoverage.mjs`, each bound to its current SHA-256 digest. A hardcoded self-test independently pins the exact source, test, producer/file, and scenario-name inventories so removing an entry from the runner's own constants cannot self-validate.
- Command 80 is the zero-argument standalone `node scripts/checkPrCScoringLawDrift.mjs` source guard. It pins the accepted base, exact ordered three-file scope, baseline raw hashes, TypeScript 5.8.3 import structure and fifteen named type symbols, and full canonical scorer bytes after the sole allowed first-import value-to-type and `../types`-to-`../types.ts` transform. Test and regression content remain canonically byte-equivalent across uniform LF or CRLF. BOM, invalid UTF-8, NUL, bare CR, mixed line endings, import substitution, body/formula/weight/threshold/hard-stop/recommendation/version drift, symlink or repository escape, replacement-ref/base/ancestry failure, race, CLI argument, loader, and Git-environment substitution reject with fixed diagnostics. The compiler may resolve through the already-authorized candidate dependency link, but its real path must equal the exact candidate `node_modules/typescript` entry and its package version remains pinned. Its Git children receive only bounded infrastructure variables and fixed noninteractive/config authority; unrelated caller variables and credentials are not relayed. This gate emits no assertion marker, so a green suite or fake artifact cannot synthesize its result.
- Node's built-in LCOV reporter must emit authentic non-empty coverage. Every inventory path remains visible; an unloaded historical helper is disclosed truthfully as `loaded: false` with zero measurement and is never silently excluded or force-imported. Because this correction deliberately executes direct imports of `acceptanceExecutionProfile.mjs` and `exhaustiveAcceptanceEvidence.mjs` plus the runner self-test, those three paths must each be loaded with a positive non-empty line measurement; silent omission of any of those intended measured paths fails closed. This scoped rule does not globally reject a truthful historical `loaded: false` row.
- The test process must pass with zero failed, cancelled, skipped, or todo tests. A green process without TAP, LCOV, or all eight exact scenario reports fails closed. A failing measured child emits only a closed `controlled-human-source-test-failure` projection: bounded numeric TAP totals, fixed `test`, `hook`, `file-wrapper`, or `unclassified` classification, a fixed failure-code enum, and bounded coordinates whose file is an exact member of the nineteen-file test inventory. The credential-preflight entry fixture creates its campaign seed in a native `before` hook; that hook alone replaces a caught seed-construction error with one owned startup tag containing either an exact allowlisted fixture code, an exact allowlisted Git operation/reason tuple, the failure-only `PR_C_PREFLIGHT_FIXTURE_FSCK_STATUS:<1..255>:<stderr-category>:<stdout-family>` token, the failure-only `PR_C_PREFLIGHT_FIXTURE_SHALLOW_STATE_REJECTED:<TRUE|FALSE|INVALID>:<ABSENT|EMPTY_REGULAR|NONEMPTY_REGULAR|INVALID_PATH>` tuple, or `UNKNOWN`, without retaining the original error or cause. `FALSE:ABSENT` is the only accepted shallow state and therefore is not an admissible failure token. The reusable read-only sampler requires exact UTF-8 Git results for `--git-common-dir`, `--git-path shallow`, and `--is-shallow-repository`; the shallow path must equal the resolved common Git directory's `shallow` entry, and lstat-before/after must prove a stable absent or single-link regular-file shape. Controls, replacement characters, aliases, symlinks, directories, hard links, escapes, races, native failures, or malformed results project only closed `INVALID`/`INVALID_PATH` states. The canonical parent samples only into private memory at START, after commands 1 through 10, immediately before command 11, and after command 11. It emits that closed 13-row sequence only when command 11 exits nonzero and its sole validated failure is the entry file's owned shallow startup tuple; success, unrelated/duplicated/malformed failure projections, child-supplied sequences, or reordered checkpoints emit nothing. The diagnostic is not an assertion marker, command record, acceptance result, or causal claim and retains no paths, file contents, object IDs, or Git streams. The fsck token is available only for exact `git fsck --full --strict`, default allowed status zero, and a native integer status from 1 through 255 after output-limit, timeout, execution, and signal precedence. The stderr category is `EMPTY`, one structurally positioned `MSG_<id>` from Git v2.55.0's complete `FOREACH_FSCK_MSG_ID`, `MULTIPLE_MSG_IDS`, `FATAL`, `ERROR_WITHOUT_MSG_ID`, or `UNKNOWN`. The independent stdout family is the closed `EMPTY`, `MISSING`, `BROKEN_LINK`, `UNREACHABLE`, `DANGLING`, `ROOT`, `TAGGED`, `BROKEN_LINK_AND_MISSING`, `MISSING_WITH_INFO`, `BROKEN_LINK_WITH_INFO`, `BROKEN_LINK_AND_MISSING_WITH_INFO`, `MULTIPLE_INFORMATIONAL`, or `UNKNOWN` enum. Object positions accept only bare lowercase 40-hex tokens, and each nonempty stdout stream must end in LF. Broken links require an exact adjacent Git `%7s` `from`/`to` pair; orphan rows, wrong spacing/types, descriptions, partial final rows, controls, blank rows, oversized input, or any unknown row poison the entire stdout family to `UNKNOWN`. Missing and broken-link families are observations rather than causal claims; root/tagged/unreachable/dangling families are informational. Neither field decodes exit bits or retains streams, hashes, object IDs, paths, or details. The grammar is pinned to Git v2.55.0 [`fsck.h`](https://github.com/git/git/blob/v2.55.0/fsck.h), [`fsck.c`](https://github.com/git/git/blob/v2.55.0/fsck.c), [`builtin/fsck.c`](https://github.com/git/git/blob/v2.55.0/builtin/fsck.c), and the three closed packed-ref report paths in [`refs/packed-backend.c`](https://github.com/git/git/blob/v2.55.0/refs/packed-backend.c). Hook classification and startup or ordinary fixture-code authority come only from matching-quote `failureType`, `error`, or `code` fields directly inside the failure row's single diagnostic `---`/`...` block at exactly two additional spaces beyond its enclosing `not ok` indentation. Stack text, titles, foreign indentation, additional root blocks, and malformed or duplicate fields cannot supply diagnostic authority. Only an exact `hookFailed` error field at a coordinate owned by that entry test can project the allowlisted startup token; unknown startup remains the fixed `hook-failure` code. Node may repeat that same hook failure for distinct registered-test ordinals: identical code/coordinate repetitions coalesce to one sanitized cause while numeric TAP failure totals remain unchanged; a replayed ordinal or conflicting code/coordinate rejects. The after hook skips cleanup only when the seed was never created, while successful setup retains the established 9 full-integrity and 95 object-inventory totals. An exact test-file path title is conservatively a `file-wrapper` failure, including `subtestsFailed` and load-shaped wrappers; it never claims that a module evaluation caused the failure without an independent discriminator. Nested assertion and before/after-hook rows retain their separate fixed classifications. Test titles, messages, stacks, values, environment, and absolute paths are never retained. Unknown fixture tokens, foreign-root or non-inventory files, malformed or duplicate rows, conflicting title/stack tokens, obsolete `module` labels, oversized TAP, and a failure projection paired with a successful child reject before any passed report can be written. The source-unchanged local full-fetch seed result remains non-acceptance evidence. Hosted run `34834915313/1` reported `FSCK_STATUS:2:EMPTY:BROKEN_LINK_AND_MISSING`; current `043f...` CI instead stopped in the startup hook with `SHALLOW_REJECTED`. Two anonymous private GitHub clone probes were nonshallow and complete, so the creator and transition remain unknown. This telemetry correction does not identify the CI root cause or turn either observation into acceptance or a causal attribution.
- Later exact-CI parent telemetry confirmed the shallow transition: metadata remained `FALSE:ABSENT` through command 4 and first became `TRUE:NONEMPTY_REGULAR` after command 5, the Delivery/Monitor browser command. The installed Playwright 1.61.1 runner enables Git capture by default under CI, and its `gitDiff` path fetches the pull-request base with depth 1. The repository therefore pins all 21 root Playwright configs to the literal unconditional `captureGitInfo: { commit: false, diff: false }`; fifteen configs required correction while six already complied. The CI checker owns an independent exact file inventory and TypeScript-AST validation of the direct import, sole literal `defineConfig` export, direct property, and two false literals. This is the confirmed source defect and correction boundary; it does not convert the failed hosted run into a PASS or attribute earlier fsck observations to a different cause.
- Scenario evidence is written by the exact owning test file only after its named assertions pass. Exact producer/file/name inventories reject missing, duplicate, skipped, substituted, or stale scenario output; suite exit and the runner's expected scenario array never synthesize a passed scenario.
- Existing consumers exercise the profile through the installed-Playwright temporary synthetic metadata configuration and the exported local browser runner with synthetic transports. Existing `runExhaustiveAcceptanceReport.test.mjs` cases spawn the real `runExhaustiveAcceptanceReport.mjs` wrapper for malformed declaration, planned-scope, local/hosted-substitution, and hostile-provenance paths. These descriptions do not claim that every repository Playwright config or a real browser batch executed.
- The installed-Playwright metadata consumer binds `PWTEST_CACHE_DIR` explicitly beneath its own uniquely owned temporary fixture. Five separate fresh two-commit repositories exercise CI default, `commit:false` alone, `diff:false` alone, the complete fixed setting, and non-CI default through the real installed CLI without a browser. CI default and `commit:false` alone must create the expected nonempty shallow boundary; `diff:false` alone remains nonshallow but still captures `gitCommit`; only the complete fixed setting preserves the custom sanitized CI/source/workflow metadata with neither Git field. Non-CI default remains nonshallow. Every repository uses only an owned file remote, empty config/templates/hooks, disabled credentials/prompts/replacements/lazy fetch/optional locks, an allowlisted environment, and stable common/shallow paths, HEAD/tree/ref targets, index bytes, and tracked bytes. Inherited cache locations, arbitrary Git variables, `NODE_OPTIONS`, resolved-path escapes, and symlink substitutions cannot supply authority. Teardown owns all synthetic Git/config/report data, and a green CLI exit without exactly one passed toy spec is insufficient.
- The actual production credential-preflight entrypoint runs in a clean isolated Git checkout with the accepted base as ancestor, exact source-before/source-after identity, Node 22, the fixed command, and one exclusive output file. External test-only modules replace only `pg.Client`, Supabase client construction, and `fetch`; no production injection surface is added and no network or real database is reachable. The seed regression separately exercises an owned, self-contained clean committed checkout and an owned committed checkout with a one-file dirty governed overlay. Each actual seed binds its source head, source and candidate trees, the independently derived current governed-path union plus the exact five intentionally absent workflows, independently calculated inventory digest, changed/deletion metadata, private clone verification, and seed/clone cleanup. The repository working tree is never edited to create either case.
- One fresh per-test-file candidate seed performs the exact governed overlay, hash verification, staging, and synthetic commit once. The initial seed and each committed-source fixture now initialize an empty private Git database and read the captured raw `HEAD` closure through a temporary read-only source-object binding. Shallow, graft, replacement, promisor/partial-clone, alternates, and HTTP-alternates metadata reject before export. `rev-list --objects --no-object-names --missing=error` must yield a bounded, unique, lowercase SHA-1 OID set; that set is supplied only on native stdin to `pack-objects --no-reuse-object --window=0`, with reverse indexes disabled and an exact owned `.pack`/`.idx` pair required. The source-object binding is removed before a detached checkout or any target verification, and the target's independently enumerated raw closure must equal the in-memory source set. Neither the OID list nor a list/digest of source objects is retained. The 32 production-entry fixture constructions remain separate private detached `git clone --local --no-hardlinks` copies of the already self-contained frozen seed. Each exact resolved local source is checked for source-root/object-directory escape and object alternates; source head, tree, status, and prohibited metadata are compared or rechecked across construction. Every clone has a private control directory and empty Git configuration, no remote, alternates, shared or hard-linked object files, `node_modules`, extra files, or mutable cross-fixture worktree. Its actual source head, candidate head/tree, governed set/digest, change/deletion metadata, and ancestry are independently verified. Construction Git authority and child-entry Git authority are distinct: the child trusts only its own clone, disables system Git configuration, hooks, credential helpers, replacement/lazy-fetch authority, commit graphs/fsmonitor, automatic maintenance, and non-local protocols, and inherits no source/seed path or arbitrary environment.
- Fixture construction removes every inherited `PATH`/`Path` entry that is equal to or contained by the live source or candidate seed, including a legitimate child whose name begins with `..` and an outside symlink that resolves into either excluded root. Only the exact parent traversal `..` or `..` followed by the platform separator is outside. Windows drive-letter case and separators and POSIX case-sensitive paths are compared as paths rather than serialized JSON; a similarly prefixed sibling remains available, as do unrelated absolute system Git/Node search locations. The composed child environment is checked again after the exact synthetic-negative override allowlist, and immediately before entry execution; that final check resolves the retained frozen path again so a subsequently retargeted symlink cannot acquire source or seed authority. `PATH`, `Path`, Git object/config/credential/hook authority, Node loader authority, unsupported override keys, or any encoded source/seed reference reject before the child or provider fixture runs. This is fixture containment only and does not exempt a source-bearing npm prefix or alter the production credential-preflight environment contract.
- The seed overlay contains only the governed candidate inventory plus the five exact removed `pr264-controlled-human-{checkpoint,edge-deploy,prepare,quiesce,verify}.yml` workflows, and verifies byte-or-absence equivalence before its synthetic commit. `.agent/`, `output/`, `tools/`, `docs/marketing/`, `.env*`, and `scripts/testPilotOperationsRecoveryPostgres.mjs` remain expressly outside the overlay. A final original-source identity comparison rejects drift; it never silently reseeds.
- Mandatory negatives cover wrong output path, dirty source, nonancestor source, source change during execution, output collision, signing-key whitespace and bounds, password type and maximum length, and the 16 KiB artifact limit. Seven separate actual-entrypoint input scenarios cover required fields, forbidden credentials, signing authority, target tuple, password JSON, exact persona set and password values. Each requires exit 1, empty stdout, one exact fixed stage line on stderr, no canary disclosure, no artifact, and a trace containing only fixture installation with no client/connect/fetch event. The exact immutable 27-token phase inventory rejects arbitrary, prototype-derived and non-string diagnostic values.
- The independently pinned inventory requires exactly 46 owned scenarios across eight reports, including 32 production-entrypoint scenarios. Replacing the prior coarse input scenario with seven and adding the current-empty recovery cases does not allow a declaration, suite exit or expected-name array to substitute for their actual assertions. Parent LCOV and mandatory subprocess scenario execution remain separate measurements.
- Fixture adversaries are separate from those 32 assertion-owned scenarios and from the committed-source fixtures. Pure table-driven mutations cover candidate head/tree, ancestry, governed digest/content, change/deletion and extra-file claims plus bounded Git argv, failure, timeout, signal and output handling. A bounded number of real private clones exercise seed/worktree contamination, missing objects, remote, alternate, hard-link, environment escape, cleanup authority, active-clone, stale-handle and cleanup-failure rejection without rebuilding the candidate overlay. The seed-head negative writes the distinct existing accepted-base commit, proves the candidate head is different before mutation, requires verification to reject, restores the original bytes, and then requires verification to pass; a clean committed candidate can therefore never turn the negative into a no-op. Actual overlay, campaign-clone, committed-source, adversarial-clone and entry-run counters are asserted; actual seed/clone/entry elapsed values are emitted only as a diagnostic projection, with no numeric threshold or performance pass.
- Full `git fsck --full --strict` runs around initial and final acceptance of the campaign seed, around initial and final acceptance of each clean/dirty seed regression, once for each of the two committed-source fixtures, and once for the clone-local nonancestor commit. The initial verified seed object inventory is an immutable module-private authority bound to a private owner. Ordinary clones replace only their redundant full `fsck` with an independent exact object-file inventory: every normalized relative object path, byte length, and SHA-256 digest must equal the owned seed manifest, while regular-file, symlink, hard-link, remote, alternate, `node_modules`, clean-tree, head/tree, ancestry, change/deletion, governed inventory and source-drift checks remain mandatory. Timestamps are not evidence and are not trusted. The full-check and successful byte-inventory verification counters are incremented by the real operations and asserted as eight/93 before campaign-seed cleanup and nine/95 after final cleanup; the corresponding actual overlay, campaign clone, committed source, adversarial clone, and entry totals are 3, 32, 2, 4, and 32. Suite declarations cannot synthesize them.
- Object adversaries additionally preserve a pack file's size and restore its prior modification time while changing a byte, inject an extra object path, omit an expected pack, mutate pure manifest paths/digests/sizes, and present forged or stale authority handles. Each rejects fail-closed. A full check that observes different pre/post byte inventories rejects instead of issuing an owned manifest. Nonancestor and committed-source fixtures never adopt the seed manifest: each receives a new private manifest only after its own full integrity check.
- The report binds the accepted base, current HEAD, governed working-tree digest, exact source hashes, exact test inventory, exact scenario-report hashes, built-in line/branch/function measurements, and uncovered line/branch/function paths.

No new numeric percentage threshold or performance budget is defined. The gate requires authentic execution and complete measurement; percentages and uncovered paths are disclosed for review.

The whole nineteen-file measured child has one independently pinned 900,000 ms harness-containment timeout. A timeout, spawn error, signal, nonzero exit, unexpected process output, or partial TAP summary fails before a passed report can be written. This allowance is only for completion of the deterministic local coverage harness on slower Windows workers; it does not alter a product, provider, database, browser, or deployment performance threshold. Each Git command remains bounded to 30,000 ms and 4 MiB per captured stream with a bounded argv; errors use fixed sanitized classifications. Entry children keep their existing 30,000 ms bound and are never retried.

The measured child schedules those exact nineteen files serially with one independently validated `--test-concurrency=1` argument. This contains contention between heavyweight temporary-Git and Playwright fixtures on Windows without removing tests, allowing skips, retrying a failure, increasing the whole-child timeout, or changing any existing 30,000 ms Playwright subprocess bound. Missing, duplicated, or substituted concurrency arguments fail closed. Product, browser, provider, database, and deployment performance criteria are unchanged; serialization is not evidence that a prior combined-run failure was transient.

The built-in LCOV measurement covers execution in the measured Node test workers. Subprocesses spawned by genuine report-wrapper scenarios and the isolated production-entry child are separately mandatory scenario contracts and are not folded into the parent LCOV counts. Their candidate source hashes or result identities remain independently checked by their existing production contracts; the report must not imply that child-only branches contributed to the percentages. The outer canonical invocation of `runPrCControlledHumanScriptCoverage.mjs` is the actual entrypoint and is likewise separate from child numeric coverage; only the self-test import contributes that runner's child LCOV measurement.

The Edge wrapper's import-side-effect assertion uses a fresh bounded Node child and the canonical module URL, without a query or fragment. Its only working-directory fixture is the byte-identical canonical migration needed by the eager server-action catalog reader. The child receives a closed OS-only environment and no inherited coverage settings; it traps process launches, filesystem mutations, and network calls, verifies the traps before import, and requires the fixture tree and bytes to remain unchanged. This assertion is mandatory, but its subprocess execution does not add a second parent LCOV source identity. Duplicate LCOV source records still fail closed; records are never silently merged, discarded, or promoted into coverage.

## Evidence and exclusions

The canonical command creates one fresh exclusive attempt beneath the resolved configured `output/pr-c-controlled-human-script-coverage/` boundary, writes only its sanitized JSON summary there, and emits the complete sanitized measurement plus report digest to the canonical command log. It preserves prior attempts. Raw TAP, raw LCOV, temporary scenario reports, temporary Git checkouts, transport traces, database values, URLs, credentials, and V8 dumps are deleted or remain outside retained evidence. The fixture environment is allowlisted and does not inherit hosted, provider, GitHub, Netlify, Supabase, controlled-human secret, `NODE_OPTIONS`, `NODE_V8_COVERAGE`, or arbitrary Git configuration. Its empty owned Git config disables system configuration and binds `safe.directory` only to the resolved governed root; wildcard trust, hooks, credential helpers, and extra inherited Git config are excluded.

This is local synthetic/source evidence. PostgreSQL, hosted credential delivery, deployment, service-credential authentication, temporary-token authentication, human testing, real providers, pilot, production, readiness, and certification remain separate `not_run`, planned, or prohibited boundaries.

The retained committed-head diagnostic that motivated the earlier correction is local diagnostic evidence only, not CI-cause or human-readiness proof. It showed a healthy clean seed lifecycle with identical source/candidate head, unchanged tree, no changed/deleted paths, 239 governed seed entries, two full integrity checks and four object-inventory verifications. The unmodified Entry file then failed at the seed-head adversary because writing the source head was a no-op, followed by the scenario ownership assertion and final hook counter assertion. A later source-unchanged current-head diagnostic also completed seed create, verify, and removal successfully with private Git 2.55.0, while the current hosted CI failure remains unexplained. The source correction must still pass the canonical measured command and exact-head CI; neither diagnostic can substitute for either.

## Rollback

If this corrective gate must be rolled back, hold the browser, acceptance, and merge gates and forward-fix the affected config or checker. Do not re-enable Playwright's CI Git-capture defaults or remove only the checker while leaving an unpinned config. Retain every prior measurement and failed hosted attempt, and forward-fix the seventeen-source contract or its 900,000 ms whole-child containment. Removing the parent-only shallow sampler must remove its diagnostic line without changing any command record, marker, exit result, shallow rejection, or acceptance behavior; never restore the obsolete bare shallow token as an admitted cause. Do not restore HEAD-bundle construction merely because its advertised header names one head: that header is not raw-object-closure proof. Disable seed reuse while preserving the slower self-contained fixture path until a reviewed closure-preserving replacement is available. Removing the fsck classifier restores generic fixed `fsck:STATUS` observability only; it must not alter the fsck command, accepted status, containment precedence, seed acceptance, or cleanup behavior. Such repairs must also preserve the clean/dirty seed regressions, distinct accepted-base tamper, closed nested-failure projection, frozen-seed isolation control, byte-owned integrity manifest, source/seed-free child search path, and standalone scoring-law source guard. The prior no-op source-head tamper, raw test-title disclosure, raw `git diff` scoring gate, per-case overlay construction, repeated clone `fsck` campaign, shared Git object authority, timestamp-only cache, unsanitized npm-inherited path, path-string exemption, incomplete inventory, and 180,000 ms whole-child timeout are known insufficient and must not be restored or represented as canonical acceptance. Disable the optimized campaign rather than sharing mutable worktrees, alternates, hard links, remotes, Git configuration, cleanup authority, accepting an object-path/byte mismatch, or allowing source/seed execution authority through an override. Never delete retained measurements, add production injection parameters, weaken TLS or source identity, allow skips, exclude a governed source, relax Environment approval, change scoring law without a versioned scoring change, or restore secret relay.

Cleanup is fail-closed, not a proof of universal residue removal. Normal completion removes every exact branded clone before the seed and then rechecks the original source identity. A forged, stale, active-clone, missing-root, resolved-path escape, contamination, or removal failure is reported as failure. If an outer harness timeout kills the test worker, owned temporary residue may remain for later bounded operator cleanup; no complete-cleanup or performance pass may be inferred from that interrupted run.
