model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Read the minimum authoritative sequence in AGENTS.md. Inspect only the active task scope. Return source-referenced findings to the controller; do not edit files, create PRs, or inventory the full historical evidence corpus. Never spawn subagents, create descendant agents, or call delegation/spawn tools. Return results only to the root controller."
