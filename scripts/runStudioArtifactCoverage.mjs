import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';import ts from 'typescript';
const roots=['services/studioArtifacts/contracts.ts','supabase/functions/_shared/studioArtifactCommand.ts','supabase/functions/_shared/studioArtifactHandler.ts','supabase/functions/_shared/studioArtifactCommand.test.ts'];
const out=path.resolve('.agent/studio-artifacts-coverage');fs.rmSync(out,{recursive:true,force:true});fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'package.json'),JSON.stringify({type:'commonjs'}));
const program=ts.createProgram({rootNames:roots,options:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.CommonJS,moduleResolution:ts.ModuleResolutionKind.Node10,skipLibCheck:true,outDir:out,noEmit:false,esModuleInterop:true,rewriteRelativeImportExtensions:true,lib:['lib.es2022.d.ts','lib.dom.d.ts']}});
const diagnostics=ts.getPreEmitDiagnostics(program);if(diagnostics.length){console.error(ts.formatDiagnosticsWithColorAndContext(diagnostics,{getCanonicalFileName:x=>x,getCurrentDirectory:()=>process.cwd(),getNewLine:()=>"\n"}));process.exit(1)}program.emit();
const slash=v=>path.join(out,v).replace(/\\/g,'/');const result=spawnSync(process.execPath,['--experimental-test-coverage','--test-coverage-lines=95','--test-coverage-functions=95','--test-coverage-branches=85',`--test-coverage-include=${slash('supabase/functions/_shared/studioArtifactCommand.js')}`,`--test-coverage-include=${slash('supabase/functions/_shared/studioArtifactHandler.js')}`,'--test',slash('supabase/functions/_shared/studioArtifactCommand.test.js')],{stdio:'inherit'});process.exit(result.status??1);
