import { test, expect } from '@playwright/test';
const hub=`<main style="max-width:1100px;margin:auto;padding:24px;font:16px system-ui"><h1>Trust and Assurance Evidence Hub</h1><p role="note">CI and authored metadata do not prove hosted or production behavior.</p><nav aria-label="Trust Assurance views"><button>Assurance overview</button><button>Claims</button><button>Evidence</button><button>Review queue</button><button>Snapshot builder</button><button>Buyer-safe preview</button><button>Publication history</button></nav><section><h2>No published snapshot</h2><p>Draft content is not buyer-visible.</p><p role="status">Review required: creator, reviewer and publisher must be distinct.</p></section></main>`;
for(const viewport of [{name:'desktop',width:1440,height:900},{name:'Pixel 7',width:412,height:915}])test(`${viewport.name} governed empty/review state`,async({page})=>{await page.setViewportSize(viewport);await page.setContent(hub);await expect(page.getByRole('heading',{name:'Trust and Assurance Evidence Hub'})).toBeVisible();await expect(page.getByText('Draft content is not buyer-visible.')).toBeVisible();const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false);expect(await page.locator('button').count()).toBe(7);});
