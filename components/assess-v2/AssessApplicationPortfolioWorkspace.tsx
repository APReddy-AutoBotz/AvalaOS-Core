import React, { useMemo, useState } from 'react';
import type { TenantContextProjection } from '../../types';
import { assessApplication, buildProcessApplicationMatrix, createPortfolioSnapshot, prepareApplicationImport, type ApplicationMetadata, type ApplicationRecord, type EvidenceRef } from '../../services/assessV2/applicationPortfolio';
const evidenceClaims=['integration_accessibility','semantic_and_data_clarity','state_and_execution','security_and_control','architecture_changeability','ui_automation_readiness','ai_assisted_engineering_readiness'];
const acceptedEvidence=(synthetic=false):EvidenceRef[]=>evidenceClaims.map(claim=>({id:crypto.randomUUID(),claimIds:[claim],sourceType:synthetic?'synthetic_fixture':'test',fresh:true,independent:!synthetic,accepted:!synthetic,synthetic}));
const app=(id:string,name:string,metadata:Partial<ApplicationMetadata>,synthetic=true):ApplicationRecord=>({id,orgId:'synthetic-org',workspaceId:'synthetic-workspace',metadataVersion:1,authorId:'synthetic-author',status:'draft',evidence:acceptedEvidence(synthetic),metadata:{name,description:'Synthetic AP Invoice Exception fixture — not verified enterprise data.',businessOwner:'Synthetic AP owner',technicalOwner:'Synthetic app owner',vendor:'Synthetic vendor',businessCapabilities:['Accounts Payable'],supportedProcesses:['AP Invoice Exception'],businessCriticality:'high',lifecycleState:'current',product:'Synthetic',version:'1',eolStatus:'supported',hostingModel:'synthetic',platform:['web'],languages:['Unknown'],frameworks:['Unknown'],sourceCode:'Unknown',documentationQuality:'medium',automatedTestMaturity:'Unknown',deploymentRepeatability:'Unknown',observability:'partial',dataClassifications:['Internal'],regulatedData:false,operatingRegions:['Synthetic'],interfaces:['REST/GraphQL'],upstreamDependencies:[],downstreamDependencies:[],realTime:true,eventDriven:true,synchronous:true,batch:false,synthetic,...metadata}});
export const syntheticApplicationPortfolioFixture=()=>{const api=app('app-api','Synthetic ERP API',{interfaces:['REST/GraphQL'],aiControls:{legalSourceRights:false,executableAcceptanceTests:false,reproducibleBuild:false,controlledSecurityReview:false,humanEngineeringOwner:true,controlledDeploymentRollback:false}});const batch=app('app-batch','Synthetic Document Intake',{interfaces:['file/batch'],batch:true,realTime:false,upstreamDependencies:['app-api'],documentationQuality:'medium'});const ui=app('app-ui','Synthetic Legacy UI',{interfaces:['UI-only'],sourceCode:'unavailable',automatedTestMaturity:'none',deploymentRepeatability:'ad_hoc',documentationQuality:'low',upstreamDependencies:['app-batch'],bridgeEvidence:{stableInterface:true,controlAccessibility:true,deterministicErrorDetection:true,reversibilityOrCompensation:true,materialActionApproval:true,monitoring:true,humanOwner:true}});return[api,batch,ui]};
export function renderApplicationPortfolioState(input:{loading?:boolean;offline?:boolean;readOnly?:boolean;revoked?:boolean;conflict?:boolean;stale?:boolean;failedPersistence?:boolean;applications:ApplicationRecord[]}){if(input.loading)return{state:'loading',banner:'Loading committed application portfolio projection…'};if(input.offline)return{state:'offline',banner:'Offline. No success is shown until committed server state reloads.'};if(input.revoked)return{state:'revoked',banner:'Authorization revoked. Refresh tenant authority.'};if(input.readOnly)return{state:'read-only',banner:'Read-only mode. Private mutation RPCs are disabled.'};if(input.conflict)return{state:'conflict',banner:'Server version changed. Reload before continuing.'};if(input.stale)return{state:'stale',banner:'Assessment ancestry is stale.'};if(input.failedPersistence)return{state:'failed persistence',banner:'Persistence failed. No optimistic success is displayed.'};if(!input.applications.length)return{state:'empty',banner:'No application portfolio inventory is committed.'};return{state:'ready',banner:'Application portfolio projection is loaded from committed state.'}}
export default function AssessApplicationPortfolioWorkspace({tenantContext,readOnly=false,offline=false}:{tenantContext:TenantContextProjection|null;readOnly?:boolean;offline?:boolean}){const [applications,setApplications]=useState<ApplicationRecord[]>(syntheticApplicationPortfolioFixture());const [message,setMessage]=useState('Synthetic AP Invoice Exception portfolio fixture — not verified enterprise data.');const [filter,setFilter]=useState('');const [csv,setCsv]=useState('name,businessCriticality\n,high');const canWrite=!!tenantContext?.capabilities?.includes('assess.applications.write')&&!readOnly&&!offline;const canImport=!!tenantContext?.capabilities?.includes('assess.applications.import')&&!readOnly&&!offline;const assessments=useMemo(()=>applications.map(assessApplication),[applications]);const links=useMemo(()=>assessments.map(a=>({processId:'AP Invoice Exception',primitiveId:'Validate invoice exception',applicationId:a.applicationId,metadataVersion:a.metadataVersion,assessmentVersionId:a.id,interactionType:'supporting application',processSuitability:'suitable' as const,governState:'approval_required' as const,allowedAction:'approval_bound' as const,economicsRef:'synthetic-pr1f-economics-reference',economicsCurrency:'USD',approvedEconomics:true})),[assessments]);const matrix=useMemo(()=>buildProcessApplicationMatrix(applications,links,assessments),[applications,links,assessments]);const snapshot=useMemo(()=>createPortfolioSnapshot(applications,assessments,['USD']),[applications,assessments]);const model=renderApplicationPortfolioState({applications,readOnly,offline});const shown=applications.filter(a=>a.metadata.name.toLowerCase().includes(filter.toLowerCase()));const createManual=()=>{const id=crypto.randomUUID();setApplications(v=>[...v,app(id,'Manual draft application',{interfaces:['REST/GraphQL'],sourceCode:'Unknown',regulatedData:'Unknown',synthetic:false},false)]);setMessage('Application create requested; visible state updated only after committed projection reload in production transport.')};const runImport=()=>{try{const rows=csv.split(/\r?\n/).slice(1).map(line=>{const [name,businessCriticality]=line.split(',');return{...applications[0].metadata,name,businessCriticality:businessCriticality as any}});const result=prepareApplicationImport({orgId:'synthetic-org',workspaceId:'synthetic-workspace',actorId:'00000000-0000-4000-8000-000000000001',idempotencyKey:'ui-import-demo',payloadHash:'ui-import-hash',rows,existing:applications}) as any;setMessage(`Import committed receipt ${result.receipt.id}: ${result.successes.length} accepted, ${result.rejections.length} rejected. ${result.rejections.map((r:any)=>`row ${r.row} ${r.code}`).join('; ')}`);setApplications(v=>[...v,...result.successes])}catch(e){setMessage(`Import rejected: ${(e as Error).message}`)}};return <section className="mt-6 rounded-3xl border border-slate-200 p-4 dark:border-slate-700" data-testid="application-portfolio-workspace" aria-labelledby="application-portfolio-title"><div className="flex flex-col gap-3 lg:flex-row lg:items-start lg:justify-between"><div><p className="text-[11px] font-black uppercase tracking-[0.16em] text-[#9a6a00]">Application Portfolio Assessment</p><h3 id="application-portfolio-title" className="text-xl font-black">Application Portfolio & AI-Assisted Modernization</h3><p className="text-sm font-semibold text-slate-600">{model.banner}</p></div><label className="text-sm font-bold">Filter applications<input className="mt-1 w-full rounded-xl border p-2" value={filter} onChange={e=>setFilter(e.target.value)} aria-label="Filter applications"/></label></div><p role="status" className="mt-3 text-sm font-semibold">{message}</p><div className="mt-4 grid gap-3 lg:grid-cols-3"><button className="rounded-xl border p-2 font-bold" disabled={!canWrite} onClick={createManual}>Create manual application</button><label className="lg:col-span-2 text-sm font-bold">CSV / JSON import<textarea className="mt-1 min-h-20 w-full rounded-xl border p-2 font-mono text-xs" value={csv} onChange={e=>setCsv(e.target.value)} aria-label="CSV or JSON import"></textarea></label><button className="rounded-xl border p-2 font-bold" disabled={!canImport} onClick={runImport}>Prepare and import rows</button></div><div className="mt-4 grid gap-3 lg:grid-cols-3">{shown.map(a=><article key={a.id} className="rounded-2xl border p-3"><h4 className="font-black">{a.metadata.name}</h4><p className="text-xs font-bold">Metadata v{a.metadataVersion} · {a.metadata.businessCriticality} · {a.metadata.lifecycleState}</p><p className="text-xs">Interfaces: {a.metadata.interfaces.join(', ')}</p><p className="text-xs">Synthetic data: {a.metadata.synthetic?'yes — not verified enterprise evidence':'no'}</p></article>)}</div><div className="mt-4 grid gap-4 lg:grid-cols-2"><section className="rounded-2xl border p-3"><h4 className="font-black">Assessment dimensions</h4>{assessments.map(a=><details key={a.id} open><summary className="font-bold">{a.applicationId} · {a.recommendations[0].disposition}</summary><ul className="list-disc pl-5 text-sm">{a.dimensions.map(d=><li key={d.dimension}>{d.dimension}: {d.band} · {d.confidence} · missing {d.missingEvidence.join(', ')||'none'}</li>)}</ul></details>)}</section><section className="rounded-2xl border p-3"><h4 className="font-black">Process × Application matrix</h4><ul className="list-disc pl-5 text-sm">{matrix.map(row=><li key={row.applicationId}>{row.processId} / {row.primitiveId}: {row.recommendedIntegrationMode}; {row.sequencingRecommendation}</li>)}</ul></section><section className="rounded-2xl border p-3"><h4 className="font-black">Modernization waves</h4><ol className="list-decimal pl-5 text-sm">{snapshot.waves.map(w=><li key={w.applicationId}>Wave {w.wave}: {w.applicationId} · {w.disposition} · auto-approved: no</li>)}</ol></section><section className="rounded-2xl border p-3"><h4 className="font-black">Review lifecycle</h4><p className="text-sm">draft → reviewer_ready → approved | changes_requested | rejected → superseded. Reviewer must differ from author. Material decisions require independent review and server receipt/audit references.</p></section></div></section>}
