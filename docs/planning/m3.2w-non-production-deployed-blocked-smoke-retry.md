# M3.2w Non-Production Deployed Blocked-Smoke Retry

## Milestone Purpose

M3.2w retries the non-production deployed blocked-smoke path after the prior readiness milestones restored Supabase CLI availability, CLI authentication readiness, and database connectivity evidence.

The milestone is intentionally narrow: prove deployed fail-closed behavior only when the approved non-production target, pilot runtime mode, database readiness, deployed function readiness, and safe invocation prerequisites are all confirmed without leaking secrets.

## Scope

In scope:

- Confirm repository and baseline safety.
- Confirm required environment visibility using booleans only.
- Confirm `AVALA_AI_RUNTIME_MODE` is `pilot`.
- Confirm the approved non-production Supabase target without printing project ref, URL, host, token, key, auth header, database URL, username, password, or IP address.
- Run the existing M3.2p mocked DB smoke before deployed retry.
- List and deploy only the existing required Edge Functions if all preflight gates pass.
- Invoke only blocked deployed scenarios, plus safe mocked behavior only if it cannot call a live provider.
- Record sanitized evidence only.

Out of scope:

- Live Gemini or Groq calls.
- Real provider key use.
- Production, shared, customer, or unclear targets.
- Schema, RLS, migration, Admin UI, product UI, Health, scoring, browser fallback, runtime resolver, provider integration, Edge Function entrypoint, `_shared/ai.ts`, dependency, usage, or cost behavior changes.
- Unsupported compliance claims.

## Required Target Functions

M3.2w carries forward the M3.2t deployed blocked-smoke target functions:

- `ai-generate-document`
- `ai-refine-section`
- `ai-provider-test-connection`

These functions call provider execution only after resolver, audit, and secret gates allow the request. M3.2w may therefore exercise blocked paths. It must not run an allowed deployed scenario unless the existing deployed code provides a mock-only route that cannot call a live provider.

## Readiness Sequence

1. Read `docs/00_SOURCE_OF_TRUTH.md`.
2. Confirm repository remote is `APReddy-AutoBotz/AvalaOS-Core`.
3. Confirm workspace is not the historical prototype repository and not Health.
4. Check out `main`, pull `origin main`, and confirm the required M3.2v-dbfix tag is included.
5. Create `milestone/m3.2w-non-production-deployed-blocked-smoke-retry`.
6. Confirm process-visible env booleans for the required Supabase and database variables.
7. Stop if `AVALA_AI_RUNTIME_MODE` is not `pilot`.
8. Stop if the approved non-production target cannot be confirmed without leaking values.
9. Confirm Supabase CLI version and sanitized function listing only if target prerequisites pass.
10. Run `npm run test:m3.2p-smoke` before deployed retry.
11. Stop if DB readiness regresses.
12. Confirm deployed function readiness and deploy only the existing target functions if safe and necessary.
13. Invoke blocked deployed scenarios only after all previous gates pass.
14. Verify audit sanitization, cleanup, retained smoke rows, and no live provider call.

## Stop Result For This Attempt

M3.2w stopped at readiness.

Sanitized readiness results:

- Local `.env` values were loaded into the PowerShell process without value output.
- `DATABASE_URL`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, and `AVALA_AI_RUNTIME_MODE` were all process-visible.
- Runtime mode was confirmed as `pilot`.
- Supabase and database target safety checks passed without printing target values.
- `npm run test:m3.2p-smoke` passed with transaction rollback, 9 sanitized smoke audit events, no retained smoke rows, and no live provider call.
- Sanitized Supabase functions listing passed.
- The three required deployed functions were absent.
- Deploy was attempted only for the three existing required Edge Function directories.
- Deploy failed for all three functions with exit code 1.

Sanitized deploy diagnostic:

- The CLI emitted a Docker-not-running warning.
- The deploy proceeded to upload function assets.
- The remote bundler returned `unexpected deploy status 400`.
- The sanitized bundler reason was module resolution failure for `_shared/providerResolver` referenced from `_shared/providerResolverIntegration.ts`.

Because deployment failed, M3.2w stopped before seed, smoke auth token creation, deployed invocation, deployed audit verification, or deployed cleanup deletes.

## Acceptance Criteria

Outcome A would require:

- Approved non-production target confirmed without value leakage.
- Runtime mode confirmed as `pilot`.
- DB readiness confirmed by `npm run test:m3.2p-smoke`.
- Required deployed functions present or safely deployed.
- Blocked deployed scenarios pass with sanitized responses.
- Audit sanitization passes.
- Rollback or cleanup completes.
- No smoke rows are retained.
- No live provider call occurs.
- No real provider key is used.
- No secrets are printed or stored.

This attempt did not meet Outcome A. It records Outcome B - readiness blocked because required deployed functions are missing and deploy is unavailable without a separately approved implementation fix.

## Verification Plan

The evidence doc records:

- Repository and baseline checks.
- Env visibility booleans only.
- Runtime mode classification.
- Supabase CLI version.
- DB smoke result.
- Deployed-smoke stop point.
- Local regression, build, audit, static scan, wording scan, and diff checks.
- Confirmation that only the two milestone docs changed.

## M3.2x Recommendation

Run an AP-approved Edge deployment bundle readiness milestone before retrying deployed smoke:

- Repair Supabase Edge deploy bundling/module resolution for the existing resolver-integrated functions.
- Keep any runtime, entrypoint, `_shared/ai.ts`, resolver, provider integration, schema, RLS, dependency, or behavior changes out of M3.2w unless AP approves a separate implementation milestone.
- Reconfirm the approved non-production target without printing values.
- Re-run M3.2w from preflight after the deploy bundle blocker is fixed.
