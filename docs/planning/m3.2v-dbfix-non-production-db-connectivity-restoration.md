# M3.2v-dbfix Non-Production DB Connectivity Restoration Plan

## Purpose

Validate that the approved non-production `DATABASE_URL` connectivity blocker from M3.2v-auth has been restored and that the existing M3.2p mocked resolver smoke can pass again.

This milestone is readiness-only. It records evidence for AP/ChatGPT review before any M3.2w deployed blocked-smoke retry.

## Baseline

- Previous milestone: M3.2v-auth Supabase CLI Auth And DB Connectivity Readiness.
- Baseline tag: `avalaos-core-m3.2v-auth-cli-auth-db-connectivity-readiness`.
- Previous outcome: Outcome B - blocked.
- Previous blocker: DB DNS/connectivity failed with sanitized `getaddrinfo ENOENT <db-host-redacted>`.
- Previous M3.2p smoke result: blocked at database connect.
- M3.2w status entering this milestone: no-go.

## Branch

Milestone branch:

- `milestone/m3.2v-dbfix-non-production-db-connectivity-restoration`

## Strict Scope

Allowed:

- Readiness evidence docs.
- Sanitized process-env visibility checks.
- Sanitized `DATABASE_URL` parse checks.
- Sanitized DNS, TCP, and DB connectivity probes.
- Existing M3.2p mocked smoke regression.
- Existing local verification commands.

Not allowed:

- Deploy Edge Functions.
- Invoke deployed Edge Functions.
- Run deployed smoke.
- Run a standalone database seed.
- Retain smoke rows.
- Call Gemini or Groq APIs.
- Use real provider keys.
- Print or store Supabase access token, Supabase URLs, Supabase keys, database URLs, provider keys, auth headers, project refs, hosts, usernames, passwords, IP addresses, or secrets.
- Store secrets in files, docs, logs, git, evidence, or output.
- Use production, shared, customer, live, demo-customer, or unclear databases.
- Edit schema SQL, RLS, or migrations.
- Edit Admin UI or product UI.
- Touch Health.
- Change scoring formulas, weights, thresholds, hard stops, or recommendation logic.
- Change prompt versions.
- Change browser fallback behavior.
- Change runtime resolver or provider integration code.
- Edit Edge Function entrypoints.
- Edit `_shared/ai.ts`.
- Add package dependencies.
- Change usage or cost behavior.
- Make unsupported compliance claims.

## Safety Checks

Repository safety:

- Confirm `origin` fetch and push point to `https://github.com/APReddy-AutoBotz/AvalaOS-Core.git`.
- Confirm workspace is not Klarity-PM.
- Confirm workspace is not KlarityFlow-Health.
- Confirm main is up to date.
- Confirm the M3.2v-auth tag is present and included in current `main`.

Environment visibility, without values:

- `DATABASE_URL`.
- `SUPABASE_ACCESS_TOKEN`.
- `SUPABASE_URL`.
- `SUPABASE_ANON_KEY`.
- `SUPABASE_SERVICE_ROLE_KEY`.
- `AVALA_AI_RUNTIME_MODE`.

Runtime mode:

- `AVALA_AI_RUNTIME_MODE` must be `pilot`.

Target safety:

- Supabase target must parse as the approved non-production Supabase host class without printing host, URL, or project ref.
- Database target must parse as a Postgres URL and avoid production/shared/customer/live markers without printing URL, host, username, password, IP address, or database name.

## Database URL Parse Evidence

Record booleans only:

- `protocol_ok`.
- `username_present`.
- `password_present`.
- `host_present`.
- `port_present`.
- `database_name_present`.
- `ssl_or_sslmode_present_if_required`.

Do not record the URL, host, username, password, port value, database name, project ref, or IP address.

## Connectivity Probe

Run a sanitized probe that internally uses `DATABASE_URL` and reports only:

- `dns_lookup=passed/blocked`.
- `tcp_connect=passed/blocked`.
- `db_connect=passed/blocked`.
- `sanitized_error_code=<code only>`.

Do not print hostname, URL, IP address, username, password, project ref, or connection string.

## Existing M3.2p Mocked Smoke

Run:

```powershell
npm run test:m3.2p-smoke
```

Expected safe behavior:

- The smoke uses the existing mocked resolver path.
- The smoke does not deploy.
- The smoke does not invoke deployed Edge Functions.
- The smoke does not call Gemini or Groq.
- The smoke uses a fake provider value for mocked handoff only.
- Smoke rows are available only inside a transaction.
- The transaction is rolled back.
- No smoke rows are retained.

## Outcome Decision

Outcome A - DB restored:

- `DATABASE_URL` is process-visible and parseable.
- Runtime mode is `pilot`.
- Target safety passes.
- DNS lookup passes.
- TCP connect passes.
- DB connect passes.
- `npm run test:m3.2p-smoke` passes.
- Smoke rows are not retained after rollback.
- M3.2w deployed blocked-smoke retry may proceed only after AP review.

Outcome B - still blocked:

- `DATABASE_URL` is missing or unparseable.
- Runtime mode is not `pilot`.
- Target safety is unsafe or unclear.
- DNS lookup fails.
- TCP connect fails.
- DB connect fails.
- `npm run test:m3.2p-smoke` fails or remains blocked.
- M3.2w remains no-go.

## Verification Commands

Run and record:

- `npm run typecheck`.
- `npm run test`.
- `npm run test:provider-resolver`.
- `npm run test:provider-resolver-integration`.
- `npm run test:m3.2p-smoke`.
- `npm run test:ai-boundary`.
- `npm run test:ai-boundary-static`.
- `npm run build`.
- `npm audit --audit-level=moderate`.
- `git diff --check`.
- Wording scan:

```powershell
rg -n "KlarityPM|Klarity PM|Klarity|AutoBotz|AI Delivery Brain|HIPAA compliant|SOC 2 compliant|GDPR compliant|GxP compliant|autonomous decision|agent decides|AI decides score"
```

## AP Review Gate

If Outcome A is reached, M3.2w is still not self-authorized. AP must review this restoration evidence before deployed blocked-smoke retry proceeds.

If Outcome B remains, M3.2w stays no-go and the sanitized blocker must be carried forward.
