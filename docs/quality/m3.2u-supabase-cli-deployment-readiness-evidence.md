# M3.2u Supabase CLI Deployment Readiness Evidence

## Repo And Remote Safety Confirmation

- Repo: `APReddy-AutoBotz/AvalaOS-Core`.
- Branch: `milestone/m3.2u-supabase-cli-deployment-readiness`.
- `origin` fetch URL: `https://github.com/APReddy-AutoBotz/AvalaOS-Core.git`.
- `origin` push URL: `https://github.com/APReddy-AutoBotz/AvalaOS-Core.git`.
- Historical prototype repository is not used.
- `.env` is not tracked by git.
- Health was untouched.

## Files Changed

Planned and actual files changed:

- `docs/planning/m3.2u-supabase-cli-deployment-readiness.md`.
- `docs/quality/m3.2u-supabase-cli-deployment-readiness-evidence.md`.

No runtime, schema, RLS, migration, Edge Function entrypoint, `_shared/ai.ts`, provider resolver/integration, Admin UI, product UI, Health, scoring, browser fallback, prompt-version, package dependency, usage, or cost file was changed.

## Tooling/Readiness-Only Confirmation

M3.2u did not:

- Deploy Edge Functions.
- Invoke deployed Edge Functions.
- Seed the database.
- Call Gemini or Groq APIs.
- Use real provider keys.
- Print or store Supabase URLs, Supabase keys, database URLs, provider keys, auth headers, tokens, or secrets.
- Store secrets in files, docs, logs, git, evidence, or output.
- Use production, shared, customer, demo-customer, or unclear databases.
- Edit Edge Function entrypoints.
- Edit `_shared/ai.ts`.
- Edit runtime resolver or provider integration files.
- Change schema SQL, RLS, or migrations.
- Change Admin UI, product UI, or Health.
- Change scoring formulas, score weights, thresholds, hard stops, or recommendation logic.
- Change browser fallback behavior.
- Change prompt versions.
- Expand usage or cost behavior.
- Add package dependencies.
- Make unsupported compliance claims.

## Env Visibility Summary

No secret values, raw URLs, or project ref values were printed.

| Item | Result |
| --- | --- |
| `SUPABASE_URL` | Process-visible without printing value. |
| `SUPABASE_ANON_KEY` | Process-visible without printing value. |
| `SUPABASE_SERVICE_ROLE_KEY` | Process-visible without printing value. |
| `AVALA_AI_RUNTIME_MODE` | Process-visible without printing value. |
| Runtime mode | `pilot`. |
| Supabase target parse | Recognized without printing value. |

## Supabase CLI Diagnosis Summary

Initial diagnosis:

- `where.exe supabase` resolved `C:\Users\mailt\.local\bin\supabase.exe` first, followed by the WindowsApps alias.
- `Get-Command supabase` resolved `C:\Users\mailt\.local\bin\supabase.exe`.
- `supabase --version` returned `2.101.0`.
- The user-level CLI directory contained `supabase.exe` but did not contain `supabase-go.exe`.
- `scoop` was not available.
- `choco` was not available.
- Global npm did not contain a `supabase` package.
- Sanitized `supabase functions list --project-ref <project-ref-redacted>` reproduced the M3.2t blocker: `Could not find the supabase-go binary`.

## Repair Result

Local-only repair was attempted and completed.

Repair actions:

- Queried official Supabase CLI GitHub release metadata for `v2.101.0`.
- Downloaded `supabase_2.101.0_windows_amd64.zip` and `checksums.txt` to `C:\tmp\supabase-cli-v2.101.0-repair`.
- Verified the ZIP SHA256 against the release checksum.
- Expanded the verified ZIP in `C:\tmp`.
- Confirmed the release contained:
  - `supabase.exe`.
  - `supabase-go.exe`.
- Confirmed installed `supabase.exe` hash matched the verified release `supabase.exe` hash.
- Copied only the missing verified `supabase-go.exe` companion binary to `C:\Users\mailt\.local\bin\supabase-go.exe`.

Repair boundaries:

- No repo file was modified by the repair.
- No package manifest was modified.
- No dependency was added.
- No install artifact was committed.
- No secret was printed or stored.

## Supabase Version Result

After repair:

- `supabase --version` returned `2.101.0`.
- The CLI no longer stopped at version resolution.

## Non-Production Target Readiness Result

After repair, sanitized `supabase functions list --project-ref <project-ref-redacted>` no longer failed because `supabase-go` was missing.

It stopped at CLI auth:

- Result: blocked.
- Blocker: Supabase access token was not provided to the CLI.
- No access token value was printed, stored, or committed.

Outcome impact:

- The local missing-`supabase-go` blocker is repaired.
- Full non-production target readiness is still blocked because CLI auth is unavailable.
- M3.2v remains no-go until AP/ops provides CLI auth safely without exposing secrets.

## Verification Results

| Command | Result | Notes |
| --- | --- | --- |
| Sanitized env visibility check | Passed | Required variables were process-visible without values; runtime mode was `pilot`; target parsed without value output. |
| `where.exe supabase` | Passed | Local user-level binary resolved before WindowsApps alias. |
| `Get-Command supabase` | Passed | Resolved to user-level `supabase.exe`. |
| `supabase --version` | Passed | Returned `2.101.0` before and after repair. |
| Pre-repair sanitized `supabase functions list` | Blocked | Reproduced `Could not find the supabase-go binary`. |
| Repair checksum verification | Passed | Official v2.101.0 Windows ZIP matched release checksum. |
| Post-repair sanitized `supabase functions list` | Blocked | Missing-`supabase-go` error resolved; command now stops because CLI access token is unavailable. |
| `npm run typecheck` | Passed | Executed as `npm.cmd run typecheck`; `tsc --noEmit` completed successfully. |
| `npm run test` | Passed after sandbox rerun | Aggregate suite passed after rerun outside the Windows sandbox ACL failure. |
| `npm run test:provider-resolver` | Passed after sandbox rerun | M3.2l provider resolver decision regression suite passed. |
| `npm run test:provider-resolver-integration` | Passed after sandbox rerun | M3.2n resolver Edge Function integration regression suite passed. |
| `npm run test:m3.2p-smoke` | Blocked | Existing mocked regression stopped at `connect`; sanitized DB probe classified the blocker as `getaddrinfo ENOENT <db-host-redacted>`. No DB rows were seeded and no live provider call occurred. |
| `npm run test:ai-boundary` | Passed after sandbox rerun | AI mode boundary regression suite passed. |
| `npm run test:ai-boundary-static` | Passed after cleanup | Final scan passed with 15 patterns checked, 675 allowed hits, 0 forbidden hits, and 0 stale allowlist entries. |
| `npm run build` | Passed | Vite production build completed successfully with 178 modules transformed. |
| `npm audit --audit-level=moderate` | Passed | Reported `found 0 vulnerabilities`. |
| Generated `.agent` cleanup | Completed | Test-output folders were removed before the final static scan. |
| `git diff --check` | Passed | No whitespace errors or conflict markers. |
| Wording scan | Passed after review | Hits remain limited to historical migration references, active repository identifiers, Health separation notes, no-go/control examples, prior planning/evidence command references, and this milestone evidence file's repo identifier plus recorded scan command. |

## Static Scan Summary

Final `npm run test:ai-boundary-static` result after generated `.agent` cleanup:

- Patterns checked: 15.
- Allowed hits: 675.
- Forbidden hits: 0.
- Stale allowlist entries: 0.
- Allowed hits remain classified as control documentation, server-side Edge/Supabase environment references, local-demo/internal-dev fallback references, deprecated cleanup-only references, non-secret app storage state, or scan pattern declarations.
- M3.2u introduced no forbidden browser-side AI, provider-key, or runtime-provider boundary hit.

## Wording Scan Review

Command:

```powershell
rg -n "KlarityPM|Klarity PM|Klarity|AutoBotz|AI Delivery Brain|HIPAA compliant|SOC 2 compliant|GDPR compliant|GxP compliant|autonomous decision|agent decides|AI decides score"
```

Expected review categories:

- Historical migration references.
- Active repository identifier references.
- Health separation notes.
- No-go/control examples.
- Prior planning/evidence command references.
- This milestone's repo identifier and recorded scan command.

Review result:

- Passed after review.
- No unsupported compliance claim was introduced.
- No product copy was introduced implying AI decides scores, approvals, risk gates, recommendations, compliance status, or regulated decisions.

## Outcome Recommendation

Outcome B - blocked.

Reason:

- `supabase --version` works.
- The missing `supabase-go` blocker was repaired with a local-only verified release companion binary.
- The approved non-production target could be parsed without printing values.
- Sanitized `functions list` no longer fails on missing `supabase-go`.
- Full target readiness remains blocked because CLI auth credentials are unavailable.
- No deploy, invoke, seed, provider call, or runtime behavior change occurred.

## M3.2v Go/No-Go Recommendation

M3.2v is no-go until AP/ops provides safe CLI auth for the approved non-production target without printing, storing, or committing token values.

After CLI auth is available, M3.2v may reattempt deployed blocked-smoke execution from sanitized preflight and AP review.

## Risks Carried Forward

- CLI auth remains unavailable.
- Target function listing is not confirmed because the CLI stopped at auth.
- The three target Edge Functions remain not proven deployed/reachable.
- Deployed blocked scenarios remain unexecuted.
- Deployed runtime-mode failure behavior remains unproven.
- Safe deployed mock behavior remains unconfirmed.
- Live provider network calls and real keys remain blocked unless AP explicitly approves them.
