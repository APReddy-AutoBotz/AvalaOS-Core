# M5.2d-a Studio Docs Ownership Migration Post-Merge Verification

Milestone: M5.2d-a Studio Docs Ownership Migration Implementation

PR: #83

Merge commit SHA: `32f1d80cefd1f6b538e74222c33bed95175eb45b`

Accepted hardening commit SHA: `141e6701d39aaf6594409da9b86715810c1c7a26`

Post-merge verification commit SHA: this verification commit; exact immutable SHA is the tag target recorded after commit creation.

Current main HEAD SHA before this verification record: `32f1d80cefd1f6b538e74222c33bed95175eb45b`

Tag name: `avalaos-core-m5.2d-a-studio-docs-ownership-migration`

Tag target SHA: this post-merge verification commit; exact immutable SHA is recorded after tag creation.

Recommended next milestone: M5.2g Delivery Work Item Ownership Migration Plan

## Files Verified

- `supabase/migrations/20260607152000_m5_2d_a_studio_docs_document_generations_authority.sql`
- `docs/quality/m5.2d-a-studio-docs-ownership-migration-evidence.md`

## Migration Summary

PR #83 is present on latest `main`. M5.2d-a creates or promotes only `document_generations` as the first applied Studio/Docs authority table.

The migration preserves the current adapter contract:

- `id`
- `org_id`
- `project_id`
- `template_id`
- `generated_at`
- `artifacts`
- `created_at`

The migration adds nullable-first ownership and readiness fields for future workspace-aware RLS and audit workflows without changing runtime behavior.

## Table Created Or Promoted

- `document_generations`

No other table is created or promoted by M5.2d-a.

## Adapter Compatibility Confirmation

`services/adapters/docsAdapter.ts` remains compatible because the migration preserves the existing `document_generations` fields used by the adapter. No runtime adapter changes were introduced.

No `app_id`, `project_app_id`, or document app identifier is added. The existing adapter path may continue to resolve project UUIDs through `projects.app_id`, but `document_generations` stores `project_id` as the applied project authority reference.

## Ownership Columns Confirmation

The migration adds these nullable-first readiness fields:

- `workspace_id`
- `created_by`
- `updated_by`
- `source_process_id`
- `source_assessment_id`
- `status`
- `archived_at`
- `deleted_at`
- `audit_correlation_id`
- `updated_at`

No reviewer or approver fields are added in this slice.

## Status Model Confirmation

The status set remains limited to:

- `generated`
- `draft`
- `archived`
- `failed`

No durable review statuses are introduced. Review-event authority remains deferred.

## Artifacts Payload Confirmation

`artifacts` remains JSONB payload compatibility only. It is not standalone durable evidence, approval, export, storage, review-event, or lineage authority.

`artifacts` must not store secrets, provider keys, service-role values, raw private tokens, personal hosted-project values, or unsupported compliance claims.

## Legacy Draft Schema Hardening Confirmation

DROP POLICY is limited to these known legacy draft Docs policies:

- `Users can see org docs`
- `Users can modify org docs`

No RLS policies are created.

Legacy FK behavior is hardened as follows:

- Legacy single-column `document_generations_project_id_fkey` is dropped if present.
- Cascade variants of named tenant/source/project FKs are dropped only if cascade behavior is detected.
- The hardened FK set covers `document_generations_org_id_fkey`, `document_generations_workspace_org_fkey`, `document_generations_project_org_fkey`, `document_generations_source_process_org_fkey`, and `document_generations_source_assessment_org_fkey`.
- Final tenant/source/project FKs use default/non-cascade delete behavior.
- Actor FKs use `ON DELETE SET NULL`.

No data is dropped. No columns are dropped. No backfill is performed. No constraints are validated.

This is fail-closed readiness hardening, not RLS implementation.

## FK And Delete Behavior Confirmation

- `org_id -> organizations(id)`: default/no-cascade delete behavior.
- `(workspace_id, org_id) -> workspaces(id, org_id)`: default/no-cascade delete behavior.
- `(project_id, org_id) -> projects(id, org_id)`: default/no-cascade delete behavior.
- `(source_process_id, org_id) -> assess_processes(id, org_id)`: default/no-cascade delete behavior.
- `(source_assessment_id, org_id) -> assessments(id, org_id)`: default/no-cascade delete behavior.
- `created_by -> profiles(id)`: `ON DELETE SET NULL`.
- `updated_by -> profiles(id)`: `ON DELETE SET NULL`.

## RLS Enabled With No Policies Confirmation

RLS is enabled on `document_generations`.

No RLS policies are created.

Tenant isolation is not implemented or proven by M5.2d-a. Future RLS policy implementation and isolation tests remain deferred until AP-approved ownership boundaries are sufficient.

## Non-Scope Confirmation

M5.2d-a introduced none of the following:

- Document Vault tables
- document version tables
- document review event tables
- document template authority
- export records
- storage object reference tables
- lineage/handoff tables
- Delivery Pack tables
- Monitor tables
- work-item tables
- provider tables
- RLS policies
- runtime adapter changes
- UI changes
- routing or view-key changes
- demo data changes
- scoring changes
- provider execution changes
- export behavior changes
- deployment changes
- package or dependency changes
- CI changes
- Health repository changes

No `.env` values, Supabase tokens, provider keys, service-role values, personal Supabase project URLs or IDs, organization IDs, bucket names, or secrets were read, printed, summarized, copied, exposed, or committed.

## Static SQL Review Result

Static SQL review passed on `supabase/migrations/20260607152000_m5_2d_a_studio_docs_document_generations_authority.sql`.

- `CREATE TABLE IF NOT EXISTS`: 1
- `ENABLE ROW LEVEL SECURITY`: 1
- `CREATE POLICY`: 0
- `DROP POLICY`: 2, limited to known legacy Docs policies
- `VALIDATE CONSTRAINT`: 0
- seed/backfill statements: 0
- `ON DELETE CASCADE`: 0
- `ON DELETE SET NULL`: 2, actor FKs only
- `app_id` / `project_app_id`: 0
- broader Document Vault/export/lineage/handoff/work-item table creation: 0
- secret-looking literals or personal hosted identifiers: none identified by static review and secret hygiene scan

## Validation Results

| Command | Result |
| --- | --- |
| `npm run typecheck` | Passed |
| `npm run test` | Passed; transient `.agent/provider-resolver-*` folders were removed before final scans |
| `npm run test:view-access-guard` | Passed |
| `npm run test:view-state-persistence` | Passed |
| `npm run test:module-journey-lifecycle` | Passed |
| `npm run test:canonical-demo-seed` | Passed |
| `npm run test:decision-pack-rendering` | Passed |
| `npm run test:assess-to-studio-handoff` | Passed |
| `npm run test:docs-to-delivery-lineage` | Passed |
| `npm run test:buyer-demo-copy` | Passed |
| `npm run test:monitor-burndown-svg` | Passed |
| `npm run test:ai-boundary` | Passed |
| `npm run test:ai-boundary-static` | Passed; 15 patterns checked, 734 allowed hits, 0 forbidden hits, 0 stale allowlist entries |
| `npm run test:secret-hygiene` | Passed; 5 rules checked, 696 allowed classified hits, 0 forbidden hits, 0 tracked `.env*` files |
| `npm run build` | Passed |
| `npm audit --audit-level=moderate` | Passed; 0 vulnerabilities |
| `git diff --check` | Passed before creating this verification doc; final pass recorded after staging |
| `git diff --cached --check` | Passed after staging this verification doc |
| `supabase --version` | Passed; 2.101.0 |

Remote Supabase validation was not run.

`scripts/applySupabaseMigrations.mjs` was not run.

`npm run db:migrate` was not run.

`npm run test:rls:assess` was not run.

Live provider calls were not run.

## Secret Hygiene Result

Secret hygiene static scan passed.

- Rules checked: 5.
- Allowed classified hits: 696.
- Forbidden hits: 0.
- Tracked `.env*` files: 0.

No secrets or `.env` values were exposed.

## Wording Scan Result

Restricted wording scan result after adding this post-merge verification record:

- Repository-wide scan: 551 existing matches.
- Focused scan over this verification document: 0 matches.

The document avoids unsupported compliance claims, autonomous approval claims, AI-owned deterministic decision claims, and old product identity language.

## Clean Git Status Confirmation

Final clean git status is recorded after committing the verification document and creating the tag.

## Risks Carried Forward

- RLS enabled without policies is fail-closed readiness only; tenant isolation is not implemented or proven.
- Configured Supabase Docs paths may fail closed until AP approves policies and runtime behavior.
- Existing/manual non-production `document_generations` rows may require later constraint validation and backfill.
- Workspace, actor, source-process, and source-assessment mapping remains AP-approved future work.
- Approval details embedded in `artifacts` can create false confidence if mistaken for durable review-event authority.
- Document Vault, export/storage, lineage/handoff, Delivery work-item, Delivery Pack, and Monitor persistence remain future milestones.

## Recommended Next Milestone

M5.2g Delivery Work Item Ownership Migration Plan.