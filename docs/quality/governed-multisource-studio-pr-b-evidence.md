# Governed Multi-Source Studio PR B Evidence

Status: Draft PR candidate with final-source focused gates and the canonical local exact-head matrix; GitHub Actions, Netlify preview, repository acceptance, merge, and deployment remain pending

Date: 2026-08-29

Accepted-main baseline: `11e670003a73b0ab5a28650b70afac4b267760f4`

Branch: `controller/governed-multisource-studio-pr-b-20260828`

## Boundary

This evidence covers only governed multi-source Studio PR B: Studio-owned source sets and immutable source packages, direct/Assess-derived/hybrid/manual planning modes, the optional accepted Assess-to-Studio Inbox/Outbox handoff, system and tenant template governance, unified server-side BYOK generation, structured artifact editing with exact provenance and coverage, lifecycle review/approval, responsive UI, rollback controls, and assertion-owned evidence.

PR C Delivery and Monitor execution remains out of scope. PR B adds only a fail-closed Delivery guard for non-`assess_handoff` source packages; it does not create Delivery work items, Monitor runtime state, or downstream execution authority.

No hosted/live infrastructure, real provider, real secret, customer data, deployment, incident, pilot, production, security-certification, or compliance action was used. Provider behavior was mocked, browser checks used synthetic fixtures, and database behavior used one disposable local PostgreSQL 16 container.

## Implemented candidate controls

The controls below describe the PR B source candidate. They are not independent proof that every adverse interleaving or UI recovery journey has executed. The focused result table records only the executable boundary completed against the final source state; anything not represented by an exact assertion remains planned verification or `not_run`.

- Studio now owns independent source-set versions, exact input bundles, and immutable source packages. Assess and Studio can reference the same immutable source version without sharing mutable set membership or authority.
- Source-package modes are an exclusive server-validated union: `assess_handoff`, `assess_plus_transcript_bundle`, `direct_transcript_bundle`, or `manual_brief`. Direct and manual planning never fabricate Assess ancestry; hybrid packages retain distinct Assess and Studio lineage families. Disposable PostgreSQL verifies a valid hybrid package whose Assess and transcript families deliberately share one exact source version: two canonical anchors, one distinct source, successful artifact finalization, and one selected-source projection. The existing direct/manual replay scenario now also proves manual artifact-v2 selected/covered counts of 0/0, workspace selected/coverage counts of 0/0, exact parity, and the same 0/0 truth in the browser harness.
- Manual-brief raw material crosses the browser boundary only in source-package creation, is server-hashed, and is stored in a private forced-RLS relation. An authorized generation command contains no raw brief: the service-only handler retrieves the committed material and sends it through the governed provider as framed untrusted input. Authenticated projections, generation plans/attempts, receipts, and audit metadata do not return raw material; client-supplied hashes and browser provider routes are rejected.
- Existing canonical Studio aggregates are backfilled exactly once as `assess_handoff` packages without rewriting approved versions, artifact/content hashes, review/approval state, private-artifact lineage, or exact Assess ancestry. Legacy `document_generations` stays unverified and non-canonical.
- The optional `assess -> studio` handoff is server-policy derived, version bound, tenant/workspace bound, expiring, independently reviewed and approved, explicitly consumed once, and replay safe. Rejection, changes requested, withdrawal, cancellation, expiry, staleness, and wrong-workspace paths create no target artifact.
- A consumed handoff binds one exact upstream Assess version and hash. Disposable PostgreSQL verifies serialization on the exact Assess case and rechecks currentness under that lock. The competing fixture is a direct synthetic insert under the production case lock, so `genuineAssessTransition` remains explicitly false: this does not prove the full governed Assess transition producer.
- System BRD/FRD/PDD templates and tenant templates share one safe projection while retaining distinct version identity. Tenant draft, review, rejection, approval, deprecation, and replacement preserve immutable history; provider instructions and client action eligibility are not authority.
- Studio generation is routed through the unified server gateway and shared budget ledger. Exact source-package/template/head bindings, provider identity/model/prompt version, usage, response staging, fenced finalization, and replay are validated server side. Disposable PostgreSQL verifies authoritative `reserved`, `uncertain`, `settled`, and `released` reservation states, one explicit released-before-effect fence transfer, wait/no-deadlock ordering, and no second provider-effect owner.
- One server-owned currentness predicate covers Assess-only, Studio-bundle-only, hybrid-both-halves, and manual-brief source packages. Mutation paths serialize on the exact Assess case, Studio bundle aggregate/version, or private manual material in the established source-before-reservation lock order. New stale requests create no generation receipt or attempt; request-to-claim and claim-to-reservation races create no new provider-effect authority, reservation, or effect; and finalization after either hybrid source half advances retains one `stale_completed` version without moving current or approved heads. Artifact and workspace safe projections use the same predicate. These are disposable PostgreSQL 16 synthetic race fixtures and record `genuineAssessTransition: false`; they do not claim execution of the governed Assess transition producer.
- Response-loss and timeout fixtures exercise bounded retry/reconciliation behavior and false-success prevention. An uncertain reservation due at timeout remains nonterminal with zero timeout audits, recoveries, or effects; a second claimant is reconciliation-only with `providerAllowed: false` and receives no second budget authority. Exact no-reservation and released-before-effect timeout compatibility also executes. Finalization uncertainty remains uncertain rather than becoming terminal failure after a provider effect may have occurred.
- Fenced `fail_v2` rejects `reserved`, `uncertain`, `settled`, and `released_reconciled_no_effect` reservations without persistent attempt, audit, or recovery mutation. Terminal failure is allowed only with no reservation or the exact `released` + `before_provider_effect` + `transfer_pending: false` tuple. Exact released-before-effect and no-reservation failure compatibility execute while preserving attempt-to-reservation lock order.
- Strict client decoders reject unknown or private projection fields. Committed command receipts are separated from projection reload outcomes, so a reload failure cannot erase a durable receipt or claim an unverified UI success.
- RPC builders, database decoders, client decoders, and browser fixtures share the exact public source-package, handoff, template, artifact, generation-receipt, coverage, and lifecycle shapes. Private fields, unknown keys, malformed terminal acknowledgements, and selector substitutions fail closed instead of being normalized into a different projection. Focused model/adapter tests and the final browser run exercise bounded selected-source pagination plus summary-driven full-route restoration of a cited human revision.
- Structured sections retain a server-governed section identity and require either an exact safe source anchor or an explicit non-source label such as `human_authored`, `template_required`, or `assumption`. Disposable PostgreSQL validates anchors against the persisted production source-package manifest through the exact SQL validator/finalizer and rejects wrong but well-formed locator, hash, and source-version substitutions.
- Template and artifact selections are invalidated when tenant, workspace, user, or authorization version changes. Async loads bind to the current authority epoch and clear selections that disappear from the refreshed projection. Executable focused and browser tests verify stale-continuation rejection, independent source/template/handoff/artifact read-capability composition, zero unauthorized projection calls, and summary-driven cited-revision reload discovery. These are UI/client authority tests, not database concurrency proof.
- The browser harness uses one worker, one strict owned port, two governed profiles, per-process output isolation, selector-owned SPA readiness, and no trace ZIP dependency. The final source-state run passed all 22 definitions on Desktop Chrome and Pixel 7: 44/44 total.
- Retained Pilot Operations browser verification now uses the same owned-server lifecycle controller on strict port 4427, including exact bind preflight, owned readiness, and bounded teardown. A test-only correction removes a host-load-sensitive cold-start wall-clock oracle and scopes the intended Admin Workbench heading by level; safe projection, command success, stale/revoked/blocked denial, no false success, accessibility, responsive behavior, and the live stop gate remain asserted. The Pilot suite passes 6/6 across Desktop Chrome and Pixel 7 without a lingering Vite descendant.
- The canonical retained full-platform fixture command now uses that lifecycle controller on strict loopback port 4173 instead of assuming an unowned pre-existing server. The controller builds the pilot bundle only with the exact two-part nonproduction sandbox gate (`AVALAOS_HOSTED_NONPRODUCTION_STABLE_TESTING=authorized` and `SITE_NAME=avalaos-pilot`), waits for the owned `/sandbox` route, runs the fixture-mode Playwright configuration, and tears down only its child preview. Direct `VITE_AVALA_HOSTED_SANDBOX_ENABLED` injection remains ineffective because `vite.config.ts` derives that value from the two-part gate. The lifecycle controller regression passes 14/14, and the full-platform browser campaign passes 16/16 across Desktop Chrome and Pixel 7.
- Runtime controls suppress authority-bearing projection actions without hiding safe history: disposable PostgreSQL proves `enabled: false` and `read_only: true` projections remain readable with zero mutation actions, then verifies restoration after the control is re-enabled.
- Delivery rejects direct, manual, and hybrid packages until PR C explicitly defines their downstream authority. No Delivery or Monitor behavior was inferred from Studio UI state.

Existing deterministic scoring formulas, score weights, thresholds, hard stops, recommendation logic, and score version are unchanged.

## Executed focused results

| Gate | Exact local result |
| --- | --- |
| Assertion registry | 61 exact commands; 322 passed assertion markers; 15 explicit `not_run` results; 193 registered synthetic fixtures; 11 exact owners; all 88 canonicalized source files rehashed; and the evidence-contract suite passed 23/23. Remaining registered commands are non-marker compile/build/static/retained gates and cannot synthesize assertion passes. |
| Focused marker ownership | Marker-producing command cardinalities, shown as assertions / unique fixtures: domain 6/4; API 78/75; provider 47/41; client 7/5; PostgreSQL 35/35; browser 118/36; accessibility 20/5; performance 2/1; evidence contract 9/1. Owner assertion counts are domain-workspace-model 12, API-command 39, API-database 34, API-source-authority 5, provider-generation 29, provider-registry 18, client-decoder 1, PostgreSQL 35, browser 140, and migration-static 9. The eleventh owner, boundary-plan, owns the explicit `not_run` boundary material rather than a passed marker. Every assertion resolves to one exact producer path and source hash. |
| Governed Studio PR B coverage gate | 96.82% lines, 89.67% branches, and 95.79% functions across eight named source modules and seven test producers; the unchanged 95% lines / 85% branches / 95% functions thresholds passed. |
| Modified Enterprise integration observation | The separately measured two full legacy Enterprise integration modules reported 42.25% lines, 74.21% branches, and 49.69% functions in aggregate. These are observational whole-module totals, not a governed threshold pass; focused changed-path tests passed. |
| Studio PR B PostgreSQL 16 | Full disposable PostgreSQL 16 harness exited 0. Fresh chain, exact accepted-main upgrade, populated compatibility, dirty-history rejection, RLS/ACL/non-disclosure, default-off/read-only behavior, package/template/handoff lineage, Delivery guard, persisted exact anchor grounding and substitution negatives, manual 0/0 projection parity, overlapping-source hybrid SQL de-duplication/finalization, reservation-state takeover/order, reservation-aware timeout reconciliation, reservation-aware failure finalization, exact-case handoff serialization, runtime-control action suppression/restoration, soft-delete historical projection, provider lifecycle, and cleanup passed. The overlap marker records two canonical anchors with one artifact-v2 selected source, one covered source, one workspace-selected source, and exact workspace parity. `fail_v2` blocks every effect-possible/reconciled reservation state without persistent mutation and permits only no reservation or exact released-before-effect/no-transfer-pending compatibility. The handoff race uses a direct synthetic insert under the production case lock and records `genuineAssessTransition: false`; the governed Assess transition remains unexecuted. No PR C Delivery item or Monitor baseline authority is added. |
| Studio PR B browser | 44/44 passed: 22 definitions on Desktop Chrome and the same 22 on Pixel 7, on strict port 4197. Capability composition, production source controls, direct/manual/hybrid generation, template and handoff lifecycles, summary-driven cited-revision reload, authorization loss, offline/malformed/large-source states, default-off legacy compatibility, and false-success prevention passed. The manual harness asserts the exact user-readable browser/server/provider disclosure, rejects any raw `manualBrief` field in the generation command, and mirrors exact selected/covered counts of 0/0; direct generation records that no manual brief entered either browser command or projection. Earlier failed diagnostic runs are excluded. |
| Accessibility | The dedicated command passed 6/6: three definitions on each governed profile. It verifies structured-field error relationships, keyboard/focus return, zero serious/critical axe findings, and zero Pixel/200%-zoom horizontal overflow. |
| Performance | 2/2 `PERF-001` profile assertions passed only after `[data-testid="studio-artifact-workspace"][data-studio-usable="true"][data-studio-projection-state="artifact-ready"]` was visible. Both per-run medians remained below the unchanged `<2500 ms` budget, with every retained sample bound by the evidence matcher. Exact samples and medians live in the sanitized machine evidence for the applicable run; this active document does not hard-code dynamic timing values. Workspace mount alone was not counted as readiness. |
| Mocked provider campaign | Final-source focused provider/API checks passed for the six registry identities: OpenAI, Azure OpenAI, Anthropic, Gemini, Groq, and OpenAI-compatible. The existing database-adapter material-recovery assertion separately proves service-only private retrieval with exact stored-hash binding. The existing `PROVIDER-008` provider-boundary assertion supplies a synthetic manual brief directly at that boundary and proves it reaches the gateway's `untrustedSource` without browser secret/direct-transport authority and remains length-framed as untrusted data rather than provider instruction; its runtime lineage records `databaseRetrievalExecuted: false`. These are adjacent focused proofs, not one composed retrieval-to-provider integration. Real providers remain `not_run`. |
| Retained full-platform browser | 16/16 passed across Desktop Chrome and Pixel 7 using an owned strict-port pilot preview with the explicit nonproduction sandbox gate. Seven synthetic personas and the sandbox-descendant route boundary exercised authorized surfaces, serious/critical accessibility checks, responsive overflow, storage cleanup, fail-closed egress observation through sign-out/reload/quiescence, and no direct provider traffic. This is deterministic local fixture evidence, not hosted or connected end-to-end proof. |
| Focused type/static gates | Final-source browser and Edge typechecks, build, Studio component/model/adapter tests, PR B migration contract, provider/API tests, scoring-drift guard, Node syntax, and `git diff --check` passed. The migration contract compares adapter named arguments directly with the SQL signatures: stage is exactly `p_attempt_id`, `p_execution_token`, `p_execution_fence`, `p_provider_operation_id`, and `p_response`; finalize is exactly `p_attempt_id`, `p_execution_token`, and `p_execution_fence`. Focused database-adapter tests also assert both `p_execution_fence` bindings. The complete retained matrix is bound to the sanitized machine-evidence manifest; local execution truthfully records `exactHeadCi: not_run`. |

## Machine evidence

`scripts/runTranscriptFlowPrBEvidence.mjs` executes the canonical 61-command registry. A local pre-commit run writes sanitized JSON under `output/process-lifecycle-pr-b/<base-sha>/<working-tree-digest>/<local-run-attempt>/`; its `headGitSha` identifies the checked-out commit while `workingTreeDigest` binds the complete governed changed-file content. Exact-head CI writes under `output/process-lifecycle-pr-b/<base-sha>/<working-tree-digest>/<github-run-attempt>/`, with `PR_B_EXACT_HEAD_SHA` equal to the checked-out pull-request head and workflow `runId` / `runAttempt` bound in the manifest. The path deliberately uses the scoped working-tree digest, not the head SHA, as its second variable segment.

The manifest binds base and head Git identities, working-tree digest, workflow/run identity, command, duration, owner path/hash, source provenance, fixture, runtime persona/capabilities/tenant/workspace, source-package/template/handoff/artifact lineage, marker digest, and command-record digest. A local run records `exactHeadCi: not_run`; only the GitHub workflow may record exact-head CI as executed.

`scripts/verifyTranscriptFlowPrBEvidence.mjs` independently rejects command substitution, missing or duplicate assertions, owner/hash drift, source-provenance drift, runtime-context substitution, fake fixtures, workflow/run/head replay, skipped required assertions, arbitrary output mutation, and unsafe evidence content. A green command exit cannot synthesize an individual Test-ID pass. Generated command logs and manifests are ignored local/CI evidence and are not committed as product source.

The 15 truthful `not_run` Test-ID boundaries are:

- `PATH-003` and `DELIVERY-TR-001` through `DELIVERY-TR-006`: PR C Delivery execution is outside PR B.
- `PATH-004` and `MONITOR-TR-001` through `MONITOR-TR-004`: PR C Monitor execution is outside PR B.
- `PERF-002-B`: the 250 Delivery work-item interaction budget belongs to PR C.
- `PERF-003`: no approved numeric PostgreSQL duration threshold exists; functional PostgreSQL 16 assertions execute separately.
- `PERF-004`: no approved end-to-end memory, provider, or token numeric budget exists; functional caps and one-effect ownership execute separately.

Real providers and provider cost, hosted Supabase/RLS/Storage/Vault/Edge, customer data, controlled-human testing, deployment, pilot, production, exact-head GitHub Actions, Netlify preview, security certification, and compliance certification remain `not_run` until independently executed in their authorized environments. Local mocked, PostgreSQL, and browser evidence does not close those boundaries.

## Rollback

Disable `studio_multisource_enabled`, `studio_tenant_templates_enabled`, `module_handoffs_enabled`, and `direct_studio_planning_enabled` at the server controls. Preserve authorized projections, exact committed replay, existing Assess-derived Studio artifacts, immutable source packages, templates and replacement history, handoff Inbox/Outbox/history, consumed ancestry, generation attempts, receipts, reservations, reviews, approvals, and audit evidence read-only. Provider disablement blocks new generation without corrupting committed drafts or approvals. Correct schema only through additive forward migration; never rewrite history, restore browser AI/provider secrets, or silently infer Assess ancestry or downstream Delivery authority.
