# M5.3a-1 Helper And SELECT Policy Plan Refresh Post-Merge Verification

## Milestone

- Milestone: `M5.3a-1 Helper And SELECT Policy Implementation Plan Refresh`
- PR: `#96`
- Accepted branch: `milestone/m5.3a-1-rls-helper-select-policy-implementation-plan-refresh`
- Accepted commit SHA: `df9b35d19c2df50a69e52cc1145624d11b5675e9`
- Merge commit SHA: `303b86a8a6aadaa985541ee6db4c071a6af38af2`
- Current main HEAD SHA before closure commit: `303b86a8a6aadaa985541ee6db4c071a6af38af2`
- Post-merge verification commit SHA: this closure evidence commit; exact SHA is recorded in the final AP response and tag target after commit creation.
- Tag name: `avalaos-core-m5.3a-1-rls-helper-select-policy-plan-refresh`
- Tag target SHA: this closure evidence commit; exact SHA is recorded in the final AP response after tag creation.
- Recommended next milestone: `M5.3a-1 RLS Helper And SELECT Policy Implementation`, only after AP accepts this refresh closure.

## Files Verified

Merged PR #96 changed exactly four planning/evidence docs:

- `docs/planning/m5.3a-1-rls-helper-select-policy-implementation-plan-refresh.md`
- `docs/security/m5.3a-1-helper-policy-security-refresh.md`
- `docs/architecture/m5.3a-1-harness-integrated-rls-validation-plan.md`
- `docs/quality/m5.3a-1-rls-helper-select-policy-plan-refresh-evidence.md`

Closure evidence added only this file:

- `docs/quality/m5.3a-1-rls-helper-select-policy-plan-refresh-post-merge-verification.md`

## Docs-Only Scope Confirmation

This milestone is docs-only. Exactly four planning/evidence docs were merged by PR #96.

Confirmed non-scope:

- No RLS helpers were implemented.
- No RLS policies were implemented.
- No tenant policy SQL was created.
- No SQL migrations were created.
- No applied schema migrations were altered.
- No `supabase/config.toml` was created or changed.
- No package scripts were changed.
- No package dependencies were changed.
- No harness runner behavior was changed.
- No runtime adapters, UI, routing, view-key, Sidebar, demo data, scoring, provider execution, export behavior, deployment config, CI workflows, Reports/Teams/Admin, or Health repository files were changed.
- No DB connection was made.
- No remote Supabase validation was run.
- `scripts/applySupabaseMigrations.mjs` was not run.
- `npm run db:migrate` was not run.
- `npm run test:rls:assess` was not run.
- No unguarded database-target command was run.
- No provider call was made.
- No `.env` values or secrets were read or exposed.
- Tenant isolation is not implemented or proven.
- Hosted-pilot readiness is not claimed.

## M5.3a-1d Accepted Baseline

The refresh preserves the closed M5.3a-1d baseline:

- PR: `#95`
- Accepted commit SHA: `26753ac964bc2a269db645b7023586f8d0fd2619`
- Merge commit SHA: `e6bb248c41a1200bb21634ae53df4cc4d31bfa26`
- Post-merge verification commit SHA and tag target: `6324f0b8d0448373ae73b7dce25d1e1632c6da4f`
- The M5.3a-1d harness has executable local assertion plumbing, but it still fails closed without explicit local-target proof or a reachable AP-approved local DB.
- No RLS helpers or SELECT policies exist yet.
- No `supabase/config.toml` exists.
- Remote Supabase validation remains prohibited.

## Implementation Scope Confirmation

Future implementation scope is limited to:

- `profiles`
- `organizations`
- `workspaces`
- `roles`
- `organization_members`
- `workspace_memberships`

Deferred surfaces remain out of scope:

- Artifact tables.
- Provider governance tables and policies.
- Legacy draft tables.
- Storage/export.
- Delivery Pack.
- Monitor.
- Lineage/handoff.
- All INSERT, UPDATE, DELETE, soft-delete, review, approval, export, delivery, or mutation policies.

## Helper Function Plan Confirmation

Future helpers are limited to:

- `public.is_active_org_member(p_org_id uuid)`
- `public.is_active_workspace_member(p_workspace_id uuid, p_org_id uuid)`

Helper posture remains:

- `LANGUAGE sql`.
- `STABLE`, not `IMMUTABLE`.
- Boolean-only output.
- Hardened by `search_path`.
- Schema-qualified where possible.
- Granted only to `authenticated` after revoking execute from `PUBLIC` and `anon`.
- `SECURITY DEFINER` remains AP-gated and must be justified only if needed to avoid recursive RLS failure.
- No tenant metadata, role existence, permission existence, or browser service-role exposure.
- No permission helpers are planned in this slice.

## SELECT Policy Plan Confirmation

Future SELECT policy direction remains SELECT-only and deny-by-default:

- `profiles`: own active, non-deleted profile only.
- `organization_members`: own active, non-deleted membership only.
- `workspace_memberships`: own active, non-deleted workspace membership only.
- `organizations`: active organization membership only.
- `workspaces`: active workspace membership only.
- `roles`: organization roles through active org membership and workspace roles through active workspace membership.

No broad `FOR ALL`, INSERT, UPDATE, DELETE, mutation, artifact, provider-governance, or legacy draft table policies are planned in this slice. System role visibility remains AP-deferred unless AP explicitly approves exact semantics.

## Recursion And Security Analysis Confirmation

The merged refresh records recursion risk from helpers querying membership tables while those tables are RLS-protected, membership policies depending on helpers, role policies depending on membership visibility, search-path hijack risk, grant risk, and cross-tenant membership or role existence leakage. It recommends the minimal helper pattern, AP-gated definer behavior only if needed, hardened grants, hardened search path, no permission helpers, no artifact dependency, and executable local assertions before any local tenant-isolation claim.

## Harness Integration Plan Confirmation

The future implementation should use the M5.3a-1d harness without runner behavior changes unless assertion output compatibility requires it. Future assertion SQL should convert the current 16 blocked scenarios into executable pass/fail assertions only after helpers and SELECT policies exist.

Expected counts remain:

- No local target: 16 declared, 0 executed, non-zero, tenant isolation verified: no.
- Rejected target: 16 declared, 0 executed, non-zero, tenant isolation verified: no.
- Local target unavailable: 16 declared, 0 executed, non-zero, tenant isolation verified: no.
- Reachable AP-approved local DB with helpers/policies and passing assertions: 16 declared, 16 executed, zero, tenant isolation verified: yes for scoped tables only.

## Local DB Availability Decision Confirmation

M5.3a-1d did not prove a reachable local DB was available. If no AP-approved reachable local DB exists during future implementation, evidence must say tenant isolation is not verified. Local tenant isolation may be claimed only if helpers/policies exist and executable local DB assertions actually run and pass.

## Auth Simulation Plan Confirmation

Future auth simulation remains generated-local-only:

- Anonymous scenario with no local subject claim.
- Authenticated active member with generated local UUID.
- Disabled organization member with generated local UUID.
- Disabled workspace member with generated local UUID.
- Non-member with generated local UUID.
- Server-only local fixture setup only if AP approves it.

No real JWTs, token printing, service-role key, row dumps, claim-value output, or `.env` values are allowed.

## Fixture Strategy Confirmation

Future fixtures remain transaction-scoped generated local data only:

- Two local organizations.
- Two local workspaces per organization.
- Active member.
- Disabled organization member.
- Disabled workspace member.
- Non-member.
- Organization-scoped roles.
- Workspace-scoped roles.
- Same-org and cross-org rows.
- Same-workspace and cross-workspace rows.
- Deleted-state variants.

No durable production/demo/runtime data is allowed.

## Proof Boundary Confirmation

This closure does not implement or prove tenant isolation. Verified local tenant isolation may only be claimed after AP-approved helpers and SELECT policies exist and executable local DB assertions run and pass for the scoped tables. That proof would still not imply hosted runtime readiness, production readiness, artifact-table coverage, export/storage readiness, provider readiness, or certification.

## Future Implementation File Strategy

Recommended future files remain uncreated in this closure:

- `supabase/migrations/<actual_next_timestamp>_m5_3a_1_rls_helpers_membership_select_policies.sql`
- Updates to `docs/schema/m5.3a_1_rls_membership_isolation_assertions.sql`
- Updates to `scripts/m5.3a-1-rls-isolation-smoke.mjs` only if required by assertion output compatibility
- `docs/quality/m5.3a-1-rls-helper-select-policy-implementation-evidence.md`

The future implementation must use the actual next chronological migration timestamp, not a hardcoded planning timestamp.

## Static Review Gates

Future static review must confirm approved helper names only, `STABLE` not `IMMUTABLE`, AP-approved `SECURITY DEFINER` only if justified, hardened `search_path`, execute revoked from `PUBLIC` and `anon`, execute granted only to `authenticated`, no broad `FOR ALL`, no INSERT/UPDATE/DELETE, no artifact/provider/legacy policies, no service-role/browser exposure, no secret-looking literals, and no personal hosted identifiers.

## Verification Gates

Future implementation should run standard npm/typecheck/build/audit/static scans, `npm run test:rls:m5.3a-1` with explicit local target if an AP-approved local DB exists, fail closed if local DB is unavailable, avoid tenant-isolation claims unless assertions execute and pass, and avoid remote Supabase validation.

## AP Decisions Needed

AP must approve:

- Exact helper SQL.
- `SECURITY DEFINER` use.
- SELECT policy predicates.
- Role visibility rules.
- Membership visibility rules.
- Local DB execution expectations.
- Whether implementation can proceed when no reachable local DB is available.
- How to interpret fail-closed harness results during implementation.
- Whether tenant isolation can be claimed only when local DB assertions pass.

## Risks Carried Forward

- Recursive RLS can block legitimate access.
- Definer helpers can become overbroad if grants or `search_path` are weak.
- Role or membership metadata can leak cross-tenant existence if policies are too broad.
- Static review can create false confidence if executable assertions do not run.
- Runtime may remain fail-closed or incompatible until later approved runtime and artifact-policy work.

## Verification Results

| Command | Result |
| --- | --- |
| `npm run typecheck` | Passed. |
| `npm run test` | Passed. |
| `npm run test:view-access-guard` | Passed. |
| `npm run test:view-state-persistence` | Passed. |
| `npm run test:module-journey-lifecycle` | Passed. |
| `npm run test:canonical-demo-seed` | Passed. |
| `npm run test:decision-pack-rendering` | Passed. |
| `npm run test:assess-to-studio-handoff` | Passed. |
| `npm run test:docs-to-delivery-lineage` | Passed. |
| `npm run test:buyer-demo-copy` | Passed. |
| `npm run test:monitor-burndown-svg` | Passed. |
| `npm run test:ai-boundary` | Passed. |
| `npm run test:ai-boundary-static` | Passed; 15 patterns checked, 734 allowed hits, 0 forbidden hits, 0 stale allowlist entries. |
| `npm run test:secret-hygiene` | Passed; final count recorded below. |
| `npm run build` | Passed. |
| `npm audit --audit-level=moderate` | Passed; 0 vulnerabilities found. |
| `npm run test:rls:m5.3a-1` | Expected fail-closed non-zero result; see harness result. |
| `git diff --check` | Passed before closure doc creation and after final evidence update. |
| `git diff --cached --check` | Passed after staging. |
| `supabase --version` | `2.101.0`. |

The aggregate test generated `.agent/provider-resolver-tests` and `.agent/provider-resolver-integration-tests`; only those generated directories were removed after verifying they were under the workspace `.agent` directory.

## Harness Result

`npm run test:rls:m5.3a-1` returned the expected fail-closed non-zero result without a local DB target:

- Local target gate: blocked.
- Local DB assertion execution: blocked.
- Environment target variable count: 1.
- Scenario contract present: yes.
- Assertion contract present: yes.
- Scenarios declared: 16.
- Scenarios executed: 0.
- Pass count: 0.
- Fail count: 0.
- Blocked count: 16.
- Tenant isolation verified: no.
- Harness status: failed-closed.
- Sanitized classifications: `local-target-proof-missing`, `environment-target-variable-present-not-accepted`.

No target value or secret value was printed.

## Secret Hygiene Result

Secret hygiene static scan passed after adding this closure doc: 5 rules checked, 742 allowed classified hits, 0 forbidden hits, and 0 tracked `.env*` files.

## Wording Scan Result

Repository-wide restricted wording scan returned 574 existing hits from historical migration notes, repository-safety references, Health separation controls, no-go/control documentation, and evidence-command records. Focused restricted wording scan on this new closure evidence file returned 0 hits. No unsupported claim or product copy implying deterministic decisions by AI was introduced.

## No-Secrets Confirmation

No `.env` values or secrets were read, printed, summarized, copied, exposed, or committed. No DB URL, target value, service key, provider key, token, hosted identifier, personal local path, or row payload was printed.

## Clean Git Status Confirmation

Final git status is recorded in the closure response after staging, commit, tag creation, and push.