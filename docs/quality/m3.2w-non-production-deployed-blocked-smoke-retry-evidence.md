# M3.2w Non-Production Deployed Blocked-Smoke Retry Evidence

## Repo And Baseline Safety

- Repo: `APReddy-AutoBotz/AvalaOS-Core`.
- Branch: `milestone/m3.2w-non-production-deployed-blocked-smoke-retry`.
- Commit SHA: pending until commit.
- Workspace: `D:\autobotz-pm`.
- Historical prototype repository is not used.
- Health is not used.
- `docs/00_SOURCE_OF_TRUTH.md` was read before planning and implementation.
- `main` was checked out and pulled from `origin main`; it was already up to date.
- Baseline tag `avalaos-core-m3.2v-dbfix-non-production-db-connectivity-restoration` is present and included in current `HEAD`.
- `.env` is not tracked by git.

## Files Changed

Planned and actual files changed:

- `docs/planning/m3.2w-non-production-deployed-blocked-smoke-retry.md`
- `docs/quality/m3.2w-non-production-deployed-blocked-smoke-retry-evidence.md`

No runtime, schema, RLS, migration, Edge Function entrypoint, `_shared/ai.ts`, provider resolver/integration, Admin UI, product UI, Health, scoring, browser fallback, package dependency, usage, or cost file was changed.

## Readiness-Only And Deployed-Smoke-Only Confirmation

M3.2w was intended as a deployed blocked-smoke retry only. After `.env` was loaded into the PowerShell process without value output, the retry progressed through target safety, DB readiness, sanitized function listing, and required-function deploy attempts. It stopped when deploy failed.

M3.2w did not:

- Invoke deployed Edge Functions.
- Create a deployed smoke auth user or token.
- Seed deployed smoke rows.
- Query deployed smoke audit rows.
- Run cleanup deletes against deployed smoke rows.
- Call Gemini or Groq live APIs.
- Use a real provider key.
- Print or store Supabase access token, Supabase URL, Supabase keys, database URL, provider keys, auth headers, project ref, host, username, password, IP address, token, or secrets.
- Store secrets in files, docs, logs, git, evidence, or output.

M3.2w did:

- Load local `.env` values into the PowerShell process without printing values.
- List deployed functions in sanitized form.
- Attempt deployment only for the three existing required Edge Function directories.

No deploy completed successfully.

## Env Visibility Summary

No secret values, raw URLs, database URLs, hosts, project refs, auth headers, usernames, passwords, IP addresses, or tokens were printed.

| Item | Result |
| --- | --- |
| `SUPABASE_ACCESS_TOKEN` | Process-visible without printing value. |
| `SUPABASE_URL` | Process-visible without printing value. |
| `SUPABASE_ANON_KEY` | Process-visible without printing value. |
| `SUPABASE_SERVICE_ROLE_KEY` | Process-visible without printing value. |
| `DATABASE_URL` | Process-visible without printing value. |
| `AVALA_AI_RUNTIME_MODE` | Process-visible without printing value. |

## Runtime Mode

- Runtime mode result: `pilot`.
- Required value: `pilot`.
- Stop condition: not triggered.

## Target Safety Confirmation

- Supabase target safety: passed without value output.
- Supabase URL parse: passed.
- Supabase host class: `supabase-host`.
- Project-ref shape: standard 20-character lowercase alphanumeric.
- Supabase disallowed target markers: absent.
- Database URL parse: passed without value output.
- Database disallowed target markers: absent.
- Project ref, URL, host, token, keys, auth headers, and secrets: not printed.
- Production/shared/customer target markers: absent in sanitized checks.
- Stop condition: not triggered.

## Supabase CLI Readiness

| Check | Result | Notes |
| --- | --- | --- |
| `supabase --version` | Passed | Reported `2.101.0`. |
| Sanitized CLI auth/listing | Passed | Functions list completed against the approved non-production target without printing token, URL, host, project ref, or keys. |
| Sanitized functions list | Passed | Required function presence was printed only as booleans. |

No access token, project ref, URL, host, key, or auth header was printed.

## DB Readiness

Command:

```powershell
npm run test:m3.2p-smoke
```

Result:

- Passed.
- Output confirmed `DATABASE_URL` visibility without value.
- Output confirmed the non-production guard passed.
- Provider governance tables were present.
- Smoke rows were available only inside the transaction.
- Blocked scenarios passed.
- Allowed mocked scenarios passed.
- Audit sanitization passed for 9 smoke audit events.
- Cleanup confirmed transaction rollback.
- No smoke rows were retained.
- No live provider call occurred; only mocked provider handoff was invoked.

## Deployed Function Readiness

Target functions:

- `ai-generate-document`
- `ai-refine-section`
- `ai-provider-test-connection`

Result:

- Sanitized functions list passed.
- `ai-generate-document`: absent.
- `ai-refine-section`: absent.
- `ai-provider-test-connection`: absent.
- Deploy was required.
- Deploy was attempted only for the three existing required Edge Function directories.
- `ai-generate-document` deploy failed with exit code 1.
- `ai-refine-section` deploy failed with exit code 1.
- `ai-provider-test-connection` deploy failed with exit code 1.
- Post-deploy sanitized functions list passed and all three required functions were still absent.
- No project ref, URL, host, token, key, auth header, or secret was printed.

Sanitized deploy diagnostic:

- Docker-not-running warning was emitted by the CLI.
- The CLI proceeded to upload function assets.
- Remote bundling returned `unexpected deploy status 400`.
- The sanitized bundler reason was module resolution failure for `_shared/providerResolver` referenced from `_shared/providerResolverIntegration.ts`.

Reason for stop:

- Required functions are missing.
- Deploy is unavailable until the Edge Function bundle/module-resolution blocker is repaired in a separately approved implementation milestone.

## Deployed Blocked-Smoke Scenario Results

No deployed blocked-smoke scenarios were executed.

Planned scenarios remain blocked:

| Scenario | Expected result | Actual result |
| --- | --- | --- |
| Missing or blocked provider config | Fail closed before provider call | Not run because required functions were absent and deploy failed. |
| Policy-disabled or unavailable provider | Fail closed before provider call | Not run because required functions were absent and deploy failed. |
| Missing or invalid runtime mode | Fail closed | Not run; deployed invocation was unavailable. |
| Safe mocked allowed scenario | Only if existing deployed code cannot call live providers | Not run; existing deployed entrypoints have no mock-only allowed route and deploy failed. |

## Audit Sanitization

- Deployed audit sanitization: not run because deployed invocation did not start.
- M3.2p mocked smoke audit sanitization: passed for 9 smoke audit events.
- No audit row dumps were produced.
- No raw prompt, completion, provider payload, provider key, auth header, service-role key, database URL, raw secret reference, stack trace, or raw database/PostgREST error was recorded.

## Rollback And Cleanup

- Deployed rollback/cleanup: not required because deployed seed/invoke did not start.
- M3.2p transaction rollback: completed.
- M3.2p smoke rows were not retained.
- Generated local test artifacts under `.agent` were cleaned before the final static scan.
- No unrelated rows were deleted.

## Retained Smoke Rows

- Deployed retained smoke rows: no deployed smoke rows were created by this attempt.
- M3.2p retained smoke row verification: passed through transaction rollback.
- Residual risk: deployed retained-row proof could not be completed because deployed invocation did not start.

## Provider And Secret Safety

Confirmed:

- No live provider call occurred.
- No real provider key was used.
- No provider billing path was exercised.
- No deployed allowed scenario was invoked.
- No Supabase token, Supabase URL, Supabase key, database URL, provider key, auth header, project ref, host, username, password, IP address, or secret was printed or stored.

## Verification Results

| Command | Result | Notes |
| --- | --- | --- |
| `git checkout main` | Passed | Already on `main` before branch creation. |
| `git pull origin main` | Passed | `main` was already up to date. |
| Baseline tag check | Passed | Required M3.2v-dbfix tag is present and included in `HEAD`. |
| Sanitized `.env` process load | Passed | Local `.env` was loaded into the PowerShell process without value output. |
| Sanitized env preflight | Passed | Required Supabase env variables, `DATABASE_URL`, and `AVALA_AI_RUNTIME_MODE` were visible without values. |
| Runtime mode | Passed | `AVALA_AI_RUNTIME_MODE` was `pilot`. |
| Sanitized target safety | Passed | Supabase and database target checks passed without printing target values. |
| `supabase --version` | Passed | Reported `2.101.0`. |
| Sanitized Supabase functions list | Passed | Required functions were listed only as presence booleans; all three were absent. |
| Required function deploy | Failed | All three required function deploys exited 1; sanitized diagnostic shows remote bundle module-resolution failure. |
| Post-deploy sanitized functions list | Passed | All three required functions remained absent. |
| `npm run typecheck` | Passed | `tsc --noEmit` completed. |
| `npm run test` | Passed after sandbox rerun | Aggregate suite passed. |
| `npm run test:provider-resolver` | Passed after sandbox rerun | M3.2l resolver decision regression passed. |
| `npm run test:provider-resolver-integration` | Passed after sandbox rerun | M3.2n resolver integration regression passed. |
| `npm run test:m3.2p-smoke` | Passed | Existing mocked smoke passed with 9 sanitized audit events, rollback, no retained smoke rows, and no live provider call. |
| `npm run test:ai-boundary` | Passed after sandbox rerun | AI mode boundary regression passed. |
| `npm run test:ai-boundary-static` | Passed after generated artifact cleanup | 15 patterns checked, 675 allowed hits, 0 forbidden hits, 0 stale allowlist entries. |
| `npm run build` | Passed | Vite build completed with 178 modules transformed. |
| `npm audit --audit-level=moderate` | Passed | Reported `found 0 vulnerabilities`. |
| `git diff --check` | Passed | No whitespace errors or conflict markers. |
| Wording scan | Passed after review | Hits remain limited to historical migration references, active repository identifiers, Health separation notes, no-go/control examples, prior planning/evidence command references, and this milestone's repo identifier plus recorded scan command. |

## Static Scan Summary

Final static scan result after generated `.agent` cleanup:

- Patterns checked: 15.
- Allowed hits: 675.
- Forbidden hits: 0.
- Stale allowlist entries: 0.

Allowed hits remain classified as control documentation, server-side Edge/Supabase environment references, local-demo/internal-dev fallback references, deprecated cleanup-only references, non-secret app storage state, or scan pattern declarations.

## Wording Scan Review

Command:

```powershell
rg -n "KlarityPM|Klarity PM|Klarity|AutoBotz|AI Delivery Brain|HIPAA compliant|SOC 2 compliant|GDPR compliant|GxP compliant|autonomous decision|agent decides|AI decides score"
```

Expected review categories:

- Historical migration references.
- Active repository identifier references.
- Health separation notes.
- No-go/control examples.
- Prior planning/evidence command references.
- This milestone's repo identifier, readiness evidence, and recorded scan command.

Review result:

- Passed after review.
- No unsupported compliance claim was introduced by the M3.2w docs.
- No product copy was introduced implying AI decides scores, approvals, risk gates, recommendations, compliance status, or regulated decisions.

## Outcome

Outcome B - readiness blocked.

Reasons:

- Required Supabase env variables and `AVALA_AI_RUNTIME_MODE` were process-visible after local `.env` process load.
- Runtime mode was confirmed as `pilot`.
- Approved non-production target safety passed without value output.
- DB readiness passed through `npm run test:m3.2p-smoke`.
- The required deployed functions were absent.
- Deploy was required and attempted only for existing required function directories.
- Deploy failed for all three functions due remote bundle module-resolution failure.
- Deployed invocation remained unavailable.

## M3.2x Recommendation

Recommend M3.2x as an AP-approved Edge deployment bundle readiness milestone:

- Repair Supabase Edge deploy bundling/module resolution for the existing resolver-integrated functions.
- Keep runtime behavior, schema, RLS, Admin UI, Health, browser fallback, scoring, dependencies, provider usage, and provider cost behavior unchanged unless separately approved.
- Reconfirm `.env` process visibility, `pilot` runtime mode, target safety, and M3.2p DB smoke before retry.
- Re-run M3.2w from preflight after AP review.

## Risks Carried Forward

- Deployed blocked scenarios remain unexecuted.
- Deployed runtime fail-closed behavior remains unproven.
- Deployed function readiness is blocked because required functions are absent and deploy failed.
- Safe deployed mocked behavior remains unavailable unless a no-live-call route is explicitly approved and implemented in a separate milestone.
- Supabase CLI reported a Docker-not-running warning during deploy.
- Remote Edge Function bundling failed on shared-module resolution.
