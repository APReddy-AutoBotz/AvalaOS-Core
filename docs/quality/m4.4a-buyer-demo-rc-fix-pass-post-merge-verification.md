# M4.4a Buyer-Demo RC Fix Pass Post-Merge Verification

Milestone: `M4.4a Buyer-Demo RC Fix Pass`

PR: `#68`

PR URL: recorded in the final execution response.

Merge commit SHA: `38daa76fc2cb3666fe9484e89e69429ed4fd920e`

Post-merge verification commit SHA: this document's commit; identical to the milestone tag target and final execution response.

Current main HEAD SHA after pull, before this verification record: `38daa76fc2cb3666fe9484e89e69429ed4fd920e`

Current main HEAD SHA after this verification record: this document's commit; identical to the milestone tag target and final execution response.

## Repository State

- Checked out `main`.
- Pulled latest `origin/main`.
- Confirmed PR `#68` is merged.
- Confirmed PR `#68` merge commit is `38daa76fc2cb3666fe9484e89e69429ed4fd920e`.
- Confirmed `main` fast-forwarded to the PR merge commit before this verification record.
- Confirmed the merge commit parents are `5e381a007306553bee8577160fdd0b37384dcc06` and `fadf2860a9192e81c377ad28b4068adc7fd4d8ec`.

## Files Verified

The PR `#68` diff from `5e381a007306553bee8577160fdd0b37384dcc06` to `38daa76fc2cb3666fe9484e89e69429ed4fd920e` contained only:

- `App.tsx`
- `components/assess/AvalaGovernLiteCardPanel.tsx`
- `components/assess/ProcessDetailStubView.tsx`
- `components/auth/LoginView.tsx`
- `components/auth/OrganizationSetupView.tsx`
- `components/delivery/DeliveryPackView.tsx`
- `components/delivery/WorkspaceView.tsx`
- `components/docs/DocsForgeView.tsx`
- `components/shared/Header.tsx`
- `components/shared/LandingPage.tsx`
- `components/shared/Sidebar.tsx`
- `constants/moduleConfig.ts`
- `docs/quality/m4.4a-buyer-demo-rc-fix-pass-evidence.md`
- `package.json`
- `scripts/checkBuyerDemoCopy.mjs`

Scope confirmation: changes were limited to buyer-facing copy/icon polish, targeted buyer-demo copy checks, package script wiring, and the M4.4a evidence record.

## Fixes Implemented

Confirmed the merged PR addressed the M4.4a must-fix items:

1. Header profile action now says `Sign Out` where it invokes sign-out behavior.
2. Buyer-facing plain `Portfolio` label now says `Avala Portfolio`.
3. Reviewer-like access wording now uses `Review Access`.
4. `Document Vault` uses a distinct sidebar icon from `Avala Studio`.
5. `Avala Monitor` wording now emphasizes readiness, lineage, blockers, and value signals rather than live telemetry.
6. AvalaOS wording clarifies that it governs handoff around execution systems and does not run bots, RPA jobs, agents, or external systems.
7. Avala Studio artifacts are framed as editable human-reviewed drafts or review artifacts requiring human sign-off.
8. Missing source-context fallback copy now directs the user back to Avala Studio or Document Vault and mentions source context.
9. Source context, evidence, deterministic assessment ownership, and human review/sign-off wording were reinforced.

## Findings Intentionally Not Implemented

- M5 enterprise hardening was not implemented because it is outside the buyer-demo RC fix pass.
- AGI/future-roadmap concepts were not implemented or promoted because they are not current buyer-facing capability.
- Reports, Teams, Workspace/Admin behavior, Admin AI Controls, M4.1g export policy, server-side provider hardening, Supabase persistence/RLS/audit hardening, deployment readiness, browser alert replacement, and WIP enforcement were not changed because they are deferred or AP-decisioned outside M4.4a.

## Targeted Buyer-Demo Copy Script

`package.json` includes `npm run test:buyer-demo-copy`, which runs `scripts/checkBuyerDemoCopy.mjs`.

Result: passed.

The script verifies:

- `Sign Out` replaces the profile `Switch` sign-out label.
- `Avala Portfolio` is the buyer-facing portfolio label.
- `Document Vault` and `Avala Studio` use distinct sidebar icons.
- reviewer access copy is review-capable.
- Monitor copy uses readiness/blocker/lineage/value-signal language.
- Studio copy uses editable draft and human sign-off language.
- execution-boundary copy is present.
- missing source-context fallback guidance is present.

## No-Change Confirmations

Confirmed no changes were introduced to:

- route keys or view keys;
- Sidebar behavior beyond label/icon presentation;
- central guard semantics;
- permissions or RBAC behavior;
- scoring formulas, weights, thresholds, hard stops, score version, gates, or recommendation logic;
- mock/demo seed semantics;
- schema, RLS, migrations, or Supabase Edge Functions;
- provider runtime behavior, provider governance behavior, browser-side provider secrets, or runtime mode policy;
- dependencies or deployment configuration;
- Reports, Teams, Workspace/Admin behavior, Admin AI Controls, M4.1g export policy, or Health behavior.

## Verification Results

Final verification status: passed.

| Command | Result | Notes |
| --- | --- | --- |
| `npm run typecheck` | Passed | `tsc --noEmit` completed. |
| `npm run test` | Passed | Full aggregate chain passed, including the targeted buyer-demo copy check. |
| `npm run test:view-access-guard` | Passed | Central view-access guard regression passed. |
| `npm run test:view-state-persistence` | Passed | Persisted view/scope regression passed. |
| `npm run test:module-journey-lifecycle` | Passed | Module journey lifecycle copy regression passed. |
| `npm run test:canonical-demo-seed` | Passed | Canonical demo seed regression passed. |
| `npm run test:decision-pack-rendering` | Passed | Decision Pack render model regression passed. |
| `npm run test:assess-to-studio-handoff` | Passed | Assess-to-Studio handoff regression passed. |
| `npm run test:docs-to-delivery-lineage` | Passed | Docs-to-Delivery lineage regression passed. |
| `npm run test:buyer-demo-copy` | Passed | New targeted buyer-demo copy script passed. |
| `npm run test:ai-boundary` | Passed | AI mode boundary regression passed. |
| `npm run test:ai-boundary-static` | Passed after generated-output cleanup | Checked 15 patterns, 723 allowed hits, 0 forbidden hits, and 0 stale allowlist entries. |
| `npm run build` | Passed | Vite production build completed. |
| `npm audit --audit-level=moderate` | Passed | `found 0 vulnerabilities`. |
| `git diff --check` | Passed | No whitespace errors. |

Note: the aggregate test generated the expected untracked `.agent/provider-resolver-*` output. The resolved paths were verified under the workspace and only those generated directories were removed before the final static scan.

## Static Scan Result

- Patterns checked: 15.
- Allowed hits: 723.
- Forbidden hits: 0.
- Stale allowlist entries: 0.

No static allowlist update was required.

## Wording Scan Result

The required repository wording scan was run. It returned 549 existing repository-wide historical/control hits. Reviewed hit categories were historical migration notes, repository-safety records, Health separation controls, no-go/control documentation, planning/evidence command records, and prior evidence records.

No new stale identity reference, unsupported certification claim, or product copy assigning deterministic scoring, gates, risk, recommendations, or approval ownership to a model was introduced by M4.4a.

## Remaining Talk-Track Items

- Prepared AP demo data remains the expected buyer-demo path.
- Use project/task/readiness signal language for Monitor.
- Provider selector is not part of the standard buyer demo unless approved and verified.
- Reports, Teams, Workspace/Admin, and Admin AI Controls remain outside the core demo path unless AP explicitly chooses to show them.
- Browser persisted state can mask canonical defaults; use clean-browser rehearsal steps.

## Remaining Enterprise-Hardening Items

- Supabase persistence, RLS validation, and audit hardening.
- Server-side provider/BYOK hardening.
- Export policy hardening and M4.1g follow-through.
- Immutable audit coverage and enterprise deployment readiness.
- Role administration, SSO, Workspace/Admin surfaces, Reports, and Teams hardening.
- Production observability and support posture.

## Carried-Forward Risks

- Browser visual rehearsal is still required.
- Browser persisted state can mask canonical defaults.
- Monitor Insights remain partial.
- M4.1g export policy remains deferred.
- Reports, Teams, Workspace/Admin, and Admin AI Controls remain deferred or AP-decisioned.
- M5 enterprise Supabase/RLS/audit/deployment hardening remains pending.
- AGI roadmap concepts must stay out of current buyer-facing claims.

## Recommended Next Milestone

`M4.4b Buyer-Demo Visual Rehearsal And RC Closure Evidence`
