# M3.1a Final Evidence Review

## Metadata

- Milestone: M3.1a Mode Contract + Fail-Closed Orchestrator
- Repository: APReddy-AutoBotz/AvalaOS-Core
- Branch: `milestone/m3.1a-mode-contract-fail-closed-orchestrator`
- Commit message: `M3.1a add AI mode contract and fail-closed orchestrator boundary`
- Date: 2026-06-04
- Status: Ready for AP review

## Files Read

- `AGENTS.md`
- `docs/00_SOURCE_OF_TRUTH.md`
- `docs/planning/m3-slice-roadmap.md`
- `docs/planning/m3.1-browser-ai-boundary-lockdown-implementation-plan.md`
- `docs/quality/m3.1-browser-ai-boundary-lockdown-planning-post-merge-verification.md`
- `docs/quality/m3.1-browser-ai-boundary-lockdown-planning-evidence.md`
- `services/aiOrchestrator.ts`
- `services/aiEdgeClient.ts`
- `services/geminiService.ts`
- `services/geminiProvider.ts`
- `services/groqProvider.ts`
- `services/supabaseClient.ts`
- `App.tsx`
- `components/docs/DocsForgeView.tsx`
- `components/docs/RefineSectionModal.tsx`
- `components/delivery/WorkspaceView.tsx`
- `vite-env.d.ts`
- `package.json`
- `scripts/runTypeScriptTest.mjs`
- existing service regression tests

## Implementation Summary

- Added `services/aiMode.ts` with the approved AI runtime mode contract.
- Added `services/aiMode.test.ts` with focused mode policy regression coverage.
- Updated `services/aiOrchestrator.ts` so generation/refinement resolve mode before choosing Edge or browser fallback.
- Pilot/production now fail closed when Edge AI is unavailable.
- Invalid mode and deployed/non-dev missing mode fail closed.
- Local-demo/internal-dev browser fallback remains bounded as demo/dev behavior.
- Updated `vite-env.d.ts` only for optional `VITE_AVALA_AI_MODE` and `VITE_AI_EDGE_FUNCTIONS_ENABLED`.
- Updated `package.json` scripts only to add `test:ai-boundary` and include it in `npm run test`.
- Added this evidence record and the M3.1a implementation plan.

## Boundary Confirmations

- No scoring behavior changed.
- No Health files changed.
- No schema/RLS changes were added.
- No Supabase Function changes were added.
- No provider config/key-reference resolver was added.
- No browser raw keys are needed for pilot/production.
- No dependencies were added.
- `package-lock.json` was not changed.
- No static scan implementation was added.
- Provider classes were not removed.
- `setup-api-key.js` was not retired.
- No Admin UI was added.
- No runtime agent, MCP/A2A, or external execution behavior was added.
- No compliance claims were introduced.

## Pre-existing Provider Env Risk

- `vite-env.d.ts` still contains `VITE_GEMINI_API_KEY`.
- This is treated as a pre-existing client-env provider key risk.
- M3.1a did not add or normalize provider key typings.
- `VITE_GROQ_API_KEY` was not added to `vite-env.d.ts`.
- Provider env cleanup remains deferred to M3.1b/M3.1c/M3.2.
- Pilot/production browser fallback is now gated by the mode policy, but full provider-env cleanup is not complete until later slices.

## Mode Boundary Limitation

- Client-side mode gating is a UI/orchestrator guard only.
- Server-side Edge enforcement remains mandatory for pilot/production.
- Explicit `local-demo` or `internal-dev` modes are not pilot-safe and must not be used for real customer data.
- M3.1b/M3.1c must continue isolating browser key/provider paths and adding static scan evidence.

## Tests Added

- `services/aiMode.test.ts`

Covered cases:

- valid modes resolve exactly
- invalid mode fails closed with `AI_BOUNDARY_INVALID_MODE`
- dev/no explicit mode resolves `local-demo`
- prod/non-dev/no explicit mode fails closed with `AI_BOUNDARY_MODE_REQUIRED`
- pilot with Edge disabled fails closed with `AI_BOUNDARY_EDGE_REQUIRED`
- production with Edge disabled fails closed with `AI_BOUNDARY_EDGE_REQUIRED`
- pilot with Edge enabled requires Edge path
- production with Edge enabled requires Edge path
- local-demo with Edge disabled allows browser fallback
- internal-dev with Edge disabled allows browser fallback and is marked demo/dev only
- stable error codes are emitted

## Limitations / Deferred Tests

- Focused orchestrator runtime tests are deferred to M3.1c because the current orchestrator/provider modules are coupled to `import.meta.env` and browser provider behavior.
- M3.1a still wires `aiOrchestrator.ts` to the tested `aiMode` helper and validates the integration through typecheck and build.
- Static scan implementation remains deferred to M3.1c.

## Verification Results

| Command | Result | Notes |
| --- | --- | --- |
| `npm run typecheck` | Passed | `tsc --noEmit` completed successfully. |
| `npm run test` | Passed | Typecheck, deterministic Assess scoring regression, Delivery Policy, Govern Lite, Delivery Pack, and AI boundary tests passed. The first sandboxed run hit a Windows sandbox refresh failure before tests started; the command was rerun and passed. |
| `npm run test:ai-boundary` | Passed | AI mode boundary regression suite passed. The script was adjusted to include `types.ts` so the existing test runner emits the expected `services/...` output path. |
| `npm run build` | Passed | Vite production build completed successfully. |
| `npm audit --audit-level=moderate` | Passed | `found 0 vulnerabilities`. |
| `git diff --check` | Passed | No whitespace errors. Git reported LF-to-CRLF working-copy normalization warnings for edited text files on Windows. |
| Wording scan | Passed | Hits reviewed as allowed history, Health separation, active repo identifier, no-go/control docs, existing evidence notes, and current M3/M3.1 planning/evidence control docs. |

## Wording Scan Review

Reviewed before commit. Hits were limited to:

- migration/history notes in `docs/08_MIGRATION_FROM_KLARITYPM.md`
- Health separation notes in `AGENTS.md`, migration docs, and Health planning docs
- active repo identifier references to `APReddy-AutoBotz/AvalaOS-Core`
- no-go/control examples in `docs/quality/no-go-list.md`
- existing M3.0/M3.1 evidence and post-merge evidence notes
- M3/M3.1/M3.1a planning and evidence control docs where the terms appear as prohibited-boundary references or as the required scan command

## Decision

M3.1a is ready for AP review. M3.1b, M3.1c, and later slices require separate approval.
