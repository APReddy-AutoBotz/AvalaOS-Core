# Governed Delivery And Monitor PR C Controlled-Human Walkthrough

Status: `not_run`

The AP authorized preparation and execution of controlled-human testing for PR #264 on 2026-09-04. The authority is narrow: one dedicated synthetic `hosted_nonproduction_pilot` backend and only `https://deploy-preview-264--avalaos-pilot.netlify.app`. Production, stable Netlify production context, AvalaOS.com and every custom domain, customer or external-user data, real provider credentials/calls, live operations, pilot/production promotion, merge, and readiness or certification claims remain prohibited.

Automation prepares and verifies evidence but cannot synthesize `CONTROLLED-HUMAN=passed`. That result exists only after three distinct humans perform all role-owned steps in the enforced active/read-only order, submit three immutable PR comments, the exact bound synthetic exercise is deprovisioned, and the final signed-session verifier passes.

## Preview test profiles and correction plan

The controlled-human build intentionally admits only the canonical preview's `/sign-in` entry after exact server attestation. Its `/sandbox` route and immutable-deploy authentication entry remain blocked. Running ordinary hosted Sandbox persona scenarios against that build is a confirmed test-profile defect, not authority to relax runtime, origin, route, authentication, or provider guards.

The same-PR correction separates three proof boundaries:

| Profile | Required behavior | Evidence boundary |
| --- | --- | --- |
| Ordinary hosted preview | Preserve the existing hosted Sandbox and navigation suites for ordinary preview builds. | Exact hosted head/deployment identity; never substituted by loopback results. |
| Controlled PR #264 preview | Keep alias-to-immutable identity, public shell, accessibility/performance and offline checks; exercise blocked Sandbox and descendant entry, immutable-origin sign-in denial, malformed/wrong-tip server attestation before credential submission, and canonical sign-in CTA routing on Desktop Chrome and Pixel 7. | Hosted negative-boundary assertions only; no provider keys, real credentials, seed, or human completion claims. |
| Same-head local synthetic regression | Build the governed checkout candidate with the existing internal loopback fixture adapter, run the complete Sandbox and navigation suites on both devices, and retain every persona, denied-route assertion and full entry-through-sign-out network observer. | `local_source_fixture` / `exact-head-synthetic-regression`; explicit working-tree source identity and parent-owned invocation ID; artifacts at `output/playwright/pr264-synthetic-regression/<head>/<area>/<invocation-id>/`; exact result inventory, no deploy ID or hosted headers. |

The root controller owns integration, provenance, canonical command registration, complete verification and the one existing PR. Read-only architecture, security and quality reviewers must close before implementation begins. The final review's corrective wave has three non-overlapping tracks: local invocation freshness; hosted report provenance; and controlled browser containment with workflow environment isolation. Tests must reject missing/unknown profiles, foreign origins, substituted heads/configurations/commands/run attempts, fake deploy identity, zero or partial result inventories, skipped executable assertions under green suite exit, and local-to-hosted evidence promotion. Catalog-unbound cases remain explicitly unexecuted rather than being manufactured as PASS. Network observation starts before entry and continues through the complete interaction, quiescence and page teardown; binding requests must not follow redirects outside the observer. Malformed attestation uses fixed dummy `.invalid` input and blocks outbound credential traffic.

The frozen four-blocker remediation matrix requires: clearing inherited hosted identity only in the workflow's local-regression steps while preserving the strict local decoder; validating report-owned hosted metadata against independent execution bindings instead of relabelling it from ambient environment; accounting for API-context binding requests and retaining browser guards through teardown; and parent-owned per-invocation local output identity that rejects a zero-exit child producing no new report. Teardown must also bound and settle asynchronous guarded callbacks before disposing their request context; unresolved or failed callbacks cannot become PASS, and removing guards early or ignoring errors is prohibited. Retained Studio durability regression must await source-package commitment and reload before starting dependent generation; all generation, revision and full-reload assertions remain mandatory, with unchanged timeouts and retries. Every attempt retains its own output. Local `HEAD` is the checkout reference, not a claim that uncommitted contents equal that commit; canonical local-candidate evidence separately binds and rechecks the governed working-tree digest. A successful diagnostic rerun never erases or converts a failed canonical attempt.

The PostgreSQL integration fixture captures every browser-attested/no-binding observation at its actual canonical action boundary using independently read database-clock start and completion endpoints around a scoped read-only state/absence probe. Those absence captures occur outside older write transactions: the fixture prepares the next exact machine step before opening its transaction and splits a test transaction when a canonical unbound step separates actions, preventing a later receipt's transaction-start timestamp from contaminating an earlier quiet observation. The final read-only observation is captured only after quiesce. Machine steps retain their actual anchor/binding endpoints and a separately observed later completion; persisted endpoints must match. The fixture never retrospectively allocates spare milliseconds, subdivides a quiet window, backdates an observation, or fabricates a future completion. It rejects foreign scope, intervening blocked activity, incorrect lifecycle, missing steps, and out-of-order or unobserved timestamps while preserving all 42 authentic machine bindings and the full absence-source inventory. These are integration fixtures, not proof that a browser or human performed those steps. Focused adversarial and repeated real PostgreSQL checks precede the full canonical run; the production observer and migration are unchanged by this fixture correction.

Acceptance requires the focused profile/report/catalog/network-observer/workflow adversarial gates, both local browser suites, the retained default localhost browser and hosted-pilot contracts, and the complete canonical PR C source/retained-platform matrix, followed by fresh final read-only reviews. After the single coherent correction is committed to the existing branch, rerun all exact-head GitHub and Netlify preview checks and independently rederive the two-operator bootstrap bindings before any protected Edge or preparation phase. Prior runs remain historical evidence for their original source; they cannot validate the correction. No exercise has started at this correction boundary, and no database rollback is needed. If verification fails, keep the preview fail closed and do not deploy Edge functions or seed users; repair or revert the coherent correction in the same PR and revalidate. No runtime authorization relaxation or alternate branch/PR is an acceptable workaround.

Actual CI execution must also validate framework-owned metadata and test discovery. All five acceptance-profile configurations explicitly disable automatic Git commit/diff capture and reserve the exact empty `ci` metadata object; the strict validators reject nonempty or unknown enrichment rather than deleting it after execution. Repository/workflow/run-attempt authority remains in the independently validated owned fields, not Playwright's diagnostic `ci` field. A browser-free installed-Playwright smoke uses synthetic GitHub-shaped input to prove the actual reporter output; this is a test fixture, never hosted evidence. The default localhost configuration must exclude the dedicated controlled-preview specification, whose twelve hosted cases remain mandatory in its own configuration. Hosted navigation contracts follow the execution-profile decoder through its exact release/origin/deploy values and retain all six response-identity checks. These requirements close CI-only integration gaps without relaxing test inventory, network observation, retries, numeric budgets, or runtime authorization.

## Preview observer failure correction

Independent read-only architecture, quality and security review confirmed an evidence-harness defect: an allowed request whose fetch or response handling throws can leave Playwright's intercepted route unresolved while its callback rejection is deferred until finalization. Awaiting navigation before finalization can then mask that failure as the unrelated outer navigation timeout. The retained hosted failures establish incomplete preview verification; they do not establish the underlying remote transport cause or an application authorization bypass. A later passing local diagnostic does not replace either failed hosted attempt.

The same-PR correction requires a terminal route action, bounded fallback abort, fixed allowlisted phase/category diagnostics, prompt propagation to the observed navigation or assertion, and guaranteed finalization after operation failure. Guards and callback accounting remain active through page/context closure; late callbacks, failed or timed-out abort/fulfill/close operations and unresolved work fail closed. Preserve exact origin/path/method/final-response/head/deployment/environment checks, a non-redirecting fetch with zero HTTP retries and a 30-second fetch timeout, the 45-second test limit and zero test retries. Do not use continue/fallback, synthetic success responses, raw diagnostic output, detached success, or a longer timeout.

The controller owns command/workflow registration, provenance, documentation and full verification. One implementation worker owns the observer, scenario lifecycle, adversarial unit tests and a separate real-Playwright loopback regression. The pure profile-contract command stays browser-free; the new `npm run test:pr-c-preview-route-termination` runs after Chromium installation in Preview QA and in the canonical PR C matrix. Required focused evidence includes asynchronous fetch rejection/timeout, response-inspection and fulfillment failure, abort failure/hang, late/post-closure callbacks, provider traffic, redirect and identity substitution, and simultaneous operation/observer failure. The real route-handler regression must execute on Windows and Linux, on both device profiles; passing mocked route objects alone is insufficient.

Acceptance then requires the complete current-source canonical matrix, fresh final read-only review, one coherent same-branch commit and all exact-head CI, newly bound preview and independent bootstrap gates. Roll back or repair this correction as one unit with its command, workflow, tests and provenance; keep the hosted boundary blocked until new exact-head verification passes. Do not deploy Edge functions, seed users, request a temporary deployment token or start the human exercise while this gate is failing. No application schema, score, credential or provider change is part of this correction.

## Readable screen-entry motion correction

Exact-head Preview QA exposed a confirmed source accessibility defect in the same-head local Sandbox regression: the keyboard-focused main region inherits the shared screen-entry opacity and blur animation, so otherwise-valid text briefly loses contrast. Independent source-CSS rendering reproduced the failure and also showed that fully invisible content can produce zero Axe violations because zero contrast nodes were inspected. The SANDBOX-009 source ownership additionally omitted the actual App focus target and CSS control. A passing rerun on the unchanged source cannot close these defects; the failed attempt and prior source/evidence bundles remain immutable.

The objective is readable content from the first entry frame, with accurate keyboard/accessibility proof. The same-PR correction makes the shared `kp-screen-in` keyframe transform-only while retaining its duration, easing, translation and reduced-motion rules. It does not change scoring, tokens, routes, authorization, public interfaces, provider behavior, database state, migrations, credentials or hosted configuration. No entry delay, test-only positive animation suppression, relaxed contrast threshold, extra timeout/retry, persona omission or shortened network observer is permitted. An explicitly failing in-memory legacy-keyframe mutation is allowed only as an adversarial oracle check and must be removed before production rendering is reverified.

One implementation worker owns `index.css` and the existing hosted Sandbox specification/contract. The controller owns the catalog, independent source anchors, source-provenance adversaries, active documentation, generated PR C provenance, full verification and the existing PR. The three read-only findings reviewers close before writes; new exact-source reviews follow verification. SANDBOX-009 keeps its existing Test ID, seven personas and both Desktop Chrome/Pixel 7 bindings. Preserve its original entry, skip-link and full-page serious/critical Axe assertions, then reach Home through real navigation for each persona and reactivate the real skip link. Sample the actual main animation under normal and reduced motion at all seven bounded points; main content must remain rendered, focused, fully opaque and unfiltered. Strict contrast proof applies only to the existing `Open work` paragraph in the first opaque Home attention-summary card: its unique DOM ownership, opaque background, subtle foreground and positive inspected node must verify, with no contrast violations or incomplete result at the initial, vulnerable and terminal samples. No synthetic node is inserted. Once per browser project the same paragraph oracle must reject invisible/empty and faded/violating legacy-animation frames; remove the override, restore media/animation state and reverify production rendering.

The first uncommitted focused candidate failed because a new blanket `#app-main` zero-incomplete assertion included existing gradients, translucent surfaces, pseudo-elements and short-text indeterminacy. The failed run remains immutable. Independent quality, architecture and evidence-boundary review found this broader assertion was not the accepted motion or SAFETY-007 contract. Full-page Axe remains a separate, fixture-free serious/critical-violation gate with nonempty actual-product contrast coverage. Retain its color-contrast incompletes separately as `unresolved_manual`; never count those nodes as contrast PASS, silently discard them, or claim complete WCAG/whole-page contrast readiness. SAFETY-007 retains every observed representative persona journey and this full-page meaning. A global visual restyle or a new manual-contrast certification gate is outside this narrow correction. Existing unknown gradient behavior is not transformed into a confirmed defect or a readiness claim by the motion test.

SANDBOX-009 ownership binds route entry, the exact `App.tsx` focus target, complete transform-only keyframe, the existing Home paragraph in `CustomDashboardView.tsx`, its opaque CSS surface and subtle foreground control. Independent contracts and adversaries reject coordinated route-only claims, missing Home/CSS ownership, substituted digests, alternate product targets and name-only anchors. Existing test/project inventories stay unchanged; a new governed digest, exact source snapshot, commit, local invocation, workflow run/attempt and preview evidence are required. Focused hosted contracts, catalog/inventory/provenance adversaries, normal/reduced-motion Desktop/Pixel checks, complete Sandbox/navigation/default browser suites, typechecks, build, static security and the full 80-command canonical matrix precede acceptance. Use the verification-command matrix for exact commands; immutable run-specific records supply actual counts and results rather than mutable self-hashes in this document.

No synthetic exercise has begun at this boundary, so this correction needs no data rollback. If the motion correction cannot be accepted, keep protected deployment/preparation blocked and use a reviewed no-entry-motion fallback with fully readable main content; do not restore transiently invisible text as the accepted fallback. Any correction or fallback receives fresh full current-source and exact-head checks. Initial preview success alone does not authorize rebinding, Edge deployment, seed, a temporary PAT, the human exercise or merge. Those separately gated steps retain the sequence below.

The second uncommitted focused attempt retained Desktop and Pixel SANDBOX-009 timeouts while clicking Home outside its required My Work scope, then the outer runner guard stopped execution before a final browser report. Preserve that failure without synthesizing completed counts. The real UI precondition is to close mobile navigation, select My Work through the workspace switcher, invoke Home, then close navigation before the existing focus and contrast checks. The test contract rejects omission of either precondition; no product access rule, force-click, storage injection, route bypass, retry or timeout change is required.

The third uncommitted focused attempt completed with both keyboard/motion profiles passing, but failed Pixel SAFETY-007 at the Platform Admin boundary because the full-page helper required a resolved positive contrast node before retaining incomplete diagnostics. The failed attempt remains immutable; its missing diagnostic cannot be reconstructed as executed proof. An isolated source-style diagnostic supports a translucent-panel/gradient limitation, not a confirmed application contrast violation. Nonempty full-page inspection means the sum of actual passed, violating and incomplete contrast nodes is positive; a zero-total or inapplicable-only scan still fails. All-incomplete and mixed-incomplete results remain `unresolved_manual`, never resolved contrast or WCAG proof. The strict Home target remains exactly one positive node with no violation or incomplete node.

Each full-page persona/device observation must emit one complete sanitized contrast summary before any terminal assertion, including clean, empty and violating observations. The local and hosted report verifiers require exactly the seven persona summaries for successful SANDBOX-009 and SAFETY-007 attempts, bind their exact execution metadata and observation window, recompute classification/count consistency, and reject missing, duplicate, malformed, stale, late, unretained or substituted summaries. Hosted results explicitly disclose unresolved persona counts. No raw DOM, selector, URL, diagnostic text or infrastructure identifier enters these summaries. A fresh source snapshot, invocation and complete validation remain required after this correction; prior failed runs are not promoted.

## Fail-closed entry gate

Before seed or authentication, `.github/workflows/transcript-flow-pr-c.yml` job `controlled_human_edge` and `.github/workflows/transcript-flow-pr-c.yml` job `controlled_human_prepare` must verify all of the following:

- a trusted `APReddy-AutoBotz` label event on `controller/governed-delivery-monitor-pr-c-20260831` at the exact current PR #264 head;
- terminal-success `.github/workflows/transcript-flow-pr-c.yml` run ID and attempt plus its exact GitHub artifact name and `sha256` digest;
- exact `deploy-preview` context, review `264`, site `avalaos-pilot`, canonical Netlify preview origin, deploy ID, release header, and `hosted_nonproduction_pilot` header;
- dedicated backend target fingerprint, one-use exercise digest, empty-or-exact-replay synthetic state, zero unexpected/provider rows, and production/customer/provider authorization all false;
- exact canonical persona and fixture manifest digests;
- the tracked public Supabase Root 2021 CA at `testing/process-lifecycle/trust/supabase-prod-ca-2021.crt`, with pinned byte and certificate validation by `node scripts/prCControlledHumanPostgresTls.mjs verify-ca` immediately before each exact direct PostgreSQL workflow step. The Node adapters load that validated CA directly and retain `sslmode=verify-full`, hostname verification, and `rejectUnauthorized: true`;
- raw-byte digest of the exact additive `20260904120000` migration plus sanitized preflight/apply/verify phase digests, with only the expected `20260831062024` prior tip or an exact replay accepted;
- exact local source digests plus provider-observed deployment identity, provider bundle/update digests, deployment receipts, and runtime reachability for `tenant-context`, `tenant-session`, `assess-command`, `assess-v2-command`, `studio-artifact-command`, `studio-private-artifact-command`, `enterprise-intelligence-command`, `enterprise-intelligence-query`, and `pr-c-controlled-human-synthetic-generation`; Delivery and Monitor remain served through the two Enterprise functions. Because the provider exposes an opaque bundle digest rather than a canonical local-source digest, the evidence does not claim a fabricated equality. The allowlisted deployment producer is label-gated, protected-environment gated, exact-head/CI/preview/backend attested, suppresses and deletes raw CLI output, signs its manifest, and uploads only that manifest. Preparation verifies the producer run, attempt, artifact digest, HMAC, source set, and provider observations; caller-authored manifest JSON is rejected; and
- repository-owned bounded `preflight`, `plan`, `apply`, and `verify` phases with 12 synthetic Auth personas, eleven exact workspace flags, two Studio artifacts (both eligible for Studio-to-Delivery), two Delivery packages, one approved Monitor baseline, the newly created exercise at concurrency version `1`, zero provider rows, and zero egress.

Any missing or mismatched value stops before seed where possible. A branch alias, latest-preview alias, stable site, custom domain, local/browser fixture, prior run attempt, source-only Edge manifest, or frontend header without backend attestation is not a substitute.

## Audited bootstrap bindings

Bootstrap is performed only from the clean, committed exact PR head after the dedicated synthetic database has been prepared at the exact prior migration tip `20260831062024`. Set the release/review SHA, one-use exercise UUID, exact Supabase project reference and public URL, exact verified-full database URL, environment class, and PR number in the operator process; do not write those raw values to a file. Run the pinned-CA verifier and then the repository-owned derivation command:

```text
node scripts/prCControlledHumanPostgresTls.mjs verify-ca
npm run pr-c-controlled-human:bootstrap-bindings -- --output <private-local-output-path>
```

The derivation command revalidates the tracked CA internally, verifies the public API/project/database tuple, derives the database fingerprint in-process, and rejects a dirty or wrong checkout, a non-empty/provider-bearing target, any target other than the exact prior tip, or stale supplied expectations. Its output contains only the exact head, source/fixture/persona/migration digests, three GitHub environment variables, three Netlify exact-branch variables, and a bootstrap digest. It never emits the database identity, URLs, project reference, exercise UUID, or credentials. A second authorized operator must independently rerun the command from the same exact head and target and compare the complete safe-variable maps plus `bootstrapDigest` before any GitHub or Netlify configuration is changed. A mismatch is blocking.

The hosted-pilot producer entrypoint regression verifies the file actually emitted by the producer into an isolated temporary directory, not a separately fabricated manifest. Its no-argument workflow path remains unchanged. Only that exact generated default manifest path is ignored as untracked output; adjacent unexpected files and tracked changes remain governed by source provenance. The test must preserve any pre-existing checkout artifact byte-for-byte and reject malformed output arguments. This is local test isolation, not hosted evidence.

## Secret and evidence boundary

### Direct protected-job corrective execution plan

Final review of the readable-motion correction found a separate canonical-coverage ownership defect. Its complete committed-head matrix and independent verifier passed, but the measured JavaScript inventory omitted the newly changed execution-profile and hosted-evidence controls. Those results remain valid only for the source and measurements actually executed; a supplemental one-off coverage artifact cannot repair the canonical gate or authorize push, protected preflight, or human testing.

The same-PR correction expands the measured source inventory from ten to thirteen: the existing controlled-human helpers, `acceptanceExecutionProfile.mjs`, `exhaustiveAcceptanceEvidence.mjs`, and the coverage runner itself. Its self-test independently pins the exact expected source, test and scenario inventories. Direct-import assertions must genuinely measure the added controls; actual profile consumers and the real report command supply separately mandatory scenario records, emitted only after their owned assertions pass. Empty or missing measurement, omitted or skipped scenarios, coordinated inventory deletion, substituted source/command, and fabricated output remain failures. The runner's import guard prevents recursive execution when its self-test joins the measured child.

Parent Node LCOV reports only the code actually executed in its measured test workers. Real subprocess entrypoint scenarios and the outer canonical runner invocation remain separately bound and do not inflate those numeric counts. Keep truthful unexecuted rows and actual uncovered branches visible; do not force-import unreachable paths or introduce a JavaScript percentage floor. The correction changes validation ownership, not application behavior, scoring, authorization, browser budgets, credentials or hosted state. Freeze and commit its complete source/provenance/documentation boundary, run the complete fresh exact-head matrix, independently retain and review evidence, and then obtain fresh CI/preview proof. Until those gates pass, the safe fallback is to hold acceptance and protected phases, not to remove the measurement requirement or reuse the earlier successful run.

Fresh source review also requires a separate, measured coverage record for the changed critical JavaScript control scripts. The existing TypeScript Delivery/Monitor gate remains 95% lines, 95% functions, and 85% branches; those thresholds are not silently assigned to the control scripts. Their gate must enumerate and hash the exact production source set, report actual coverage including unexecuted files and uncovered branches, and prove execution of the mandatory production-entrypoint and adversarial scenarios. No new numeric coverage or performance budget is authorized by this correction.

Preflight entrypoint tests use isolated Git/filesystem authority and synthetic external transports while retaining the actual production command, accepted-base ancestry, bootstrap, adapter inspection, before/after source checks, and exclusive output. They must reject dirty or non-ancestor source, mid-execution source substitution, wrong output paths and collisions, invalid signing-key bounds or whitespace, non-string or oversized passwords, oversized artifacts, and skipped checks under a green exit. Production accepts no test adapter, source, inventory, proof, or filename override. The canonical source command owns these tests and the measured report; a suite exit alone cannot supply a missing scenario or source measurement.

The private full-run harness must distinguish only source-owned generated test outputs from source files, hash every original source before and after execution, and reject unrecognized additions, missing source, links, and substitutions. Source, database-residual, worker, and listener postchecks are independently attempted and retained; any failure keeps the whole run non-clean. A failed attempt remains immutable. Corrections require a new source/run identity, the complete matrix, independent evidence verification, and fresh final read-only reviews.

The later protected attempt also failed before its first backend operation. The masked runner fields cannot distinguish an empty value, an inert sentinel, or a real credential; a green local contract cannot establish GitHub secret resolution. Preserve both failed attempts under their original source identities. The controller's fresh read-only reconciliation confirmed the unchanged prior migration tip and no partial controlled migration or deployment. The unused temporary GitHub token secret was removed; that is not proof of Supabase token revocation.

The architecture, security, and quality findings-only wave is closed. The controller authorizes the write phase within the existing PR: replace all eight reusable phase calls with direct protected jobs in the primary workflow, preserving its single authority discovery, exact predecessor run namespace, and requester → approver → reviewer ordering. Remove the five obsolete reusable entrypoints; retain the dedicated recovery workflow only for its separately protected direct dispatch. This avoids changing predecessor discovery to six independently triggered workflow namespaces. One implementation worker owns workflow and existing evidence-identity integration; the controller owns the read-only credential preflight, provenance, active documentation, and complete verification. Neither may delegate recursively.

Before requesting a replacement PAT, a separately AP-approved direct credential-preflight job must prove delivery of the seven existing non-PAT secrets, validate the exact source/preview/exercise/target binding, and inspect the synthetic database in an explicitly read-only transaction. It emits only a fixed, domain-separated HMAC-signed public envelope. Independent verification must bind the exact workflow, job, run, attempt, source, and artifact; missing output under a green suite or job cannot pass. No provider/Admin call, migration, seed, function deployment, credential mutation, or human-test claim is allowed in this preflight. Each later phase retains its own exact-name presence guard and first-use semantic checks.

Acceptance requires adversarial direct-job secret/evidence/recovery/preflight tests, the complete governed and retained-platform matrix, fresh independent final reviews, one same-branch commit, exact-head CI and preview verification, and independently rederived bootstrap bindings. Hosted credential delivery remains unproven until the new protected preflight passes. Only then may a new narrowly authorized token and Edge run be considered. The human gate, final deprovision evidence, and fresh AP merge confirmation remain mandatory. Roll back or repair the workflow, contracts, preflight, provenance, and documentation as one unit; withhold phase labels and retain failures rather than weakening approval, TLS, secret scoping, or evidence rules.

Protected run `34134571033`, attempt `1`, stopped at the first Supabase target-binding step because all three required called-workflow secret fields resolved empty. Authority, exact-head CI, artifact, and preview checks had passed, but provider baseline, migration, Edge deployment, preparation, seed, and human exercise steps did not run. Preserve that failed run as immutable evidence of the original delivery defect; it cannot validate this correction and must not be rewritten or represented by a later mutable artifact hash.

Each protected phase now runs as a direct job in `.github/workflows/transcript-flow-pr-c.yml`; the five reusable-only files and every caller sentinel map are removed. The primary authority still proves the trusted event, current PR head, preview, protected target bindings, and exact predecessor run/attempt/artifacts before phase execution. Each direct job retains `hosted-nonproduction-pilot`, exact least-secret step mappings, and an unconditional first-use presence guard after trusted checkout and dependency installation. The guard rejects absent, blank, sentinel, and wrong-type values with fixed name/category diagnostics only. There is no inheritance, caller secret expression, secret copy, input alias, or fallback. Presence does not replace target, TLS, exercise, provider, or credential semantics.

Local workflow, parser, mutation, CLI, and evidence tests prove source-owned direct-job structure, exact phase names, guard ordering, safe diagnostics, current workflow/job/role identity, and rejection of invalid reports. They do not prove hosted Environment resolution. Before another temporary PAT is requested, apply only `pr264-controlled-human-credentials-preflight` after fresh exact-head CI, preview, and independent bootstrap checks. The direct protected preflight requires actual AP approval and performs only bounded in-memory validation, exact public preview observation, and a verified read-only transaction against the empty prior-tip synthetic database. Its single `credential-preflight.json` is domain-separated HMAC-signed and independently verified against exact run/attempt/artifact/source/command/job and bootstrap authority. A missing or duplicate artifact, stale attempt, absent assertion, wrong signature or binding fails closed. This proves transport only; service-role API authentication, PAT validity, deployment, and human evidence remain separate gates. Dedicated direct-dispatch recovery remains independently protected and cannot substitute for this preflight.

Bootstrap diagnostics are also sanitized: invalid arguments emit only the fixed usage string, and all Git, filesystem, DNS, TLS, and PostgreSQL failures emit only `PR_C_CONTROLLED_HUMAN_BOOTSTRAP_FAILED`. Git child stderr is captured before the CLI boundary. Do not replace this behavior with raw messages, stack traces, connection details, or partial redaction while diagnosing a failed bootstrap.

The protected `hosted-nonproduction-pilot` GitHub environment supplies `PR_C_CONTROLLED_HUMAN_DATABASE_URL`, `PR_C_CONTROLLED_HUMAN_EXERCISE_ID`, `PR_C_CONTROLLED_HUMAN_SUPABASE_URL`, `PR_C_CONTROLLED_HUMAN_SUPABASE_SERVICE_ROLE_KEY`, `PR_C_CONTROLLED_HUMAN_PASSWORD_BUNDLE_JSON`, `PR_C_CONTROLLED_HUMAN_EVIDENCE_HMAC_KEY`, `PR_C_CONTROLLED_HUMAN_SUPABASE_ACCESS_TOKEN`, and `PR_C_CONTROLLED_HUMAN_SUPABASE_PROJECT_REF` directly to the protected direct jobs that need them. Its protected variables hold only `PR_C_CONTROLLED_HUMAN_EXERCISE_DIGEST`, `PR_C_CONTROLLED_HUMAN_TARGET_FINGERPRINT`, `PR_C_CONTROLLED_HUMAN_PUBLIC_TARGET_DIGEST`, and optional sanitized `PR_C_CONTROLLED_HUMAN_DEFECT_HISTORY_JSON`. The public-target digest is computed as SHA-256 over the versioned label plus the canonical public Supabase origin, then independently compared against the protected project reference/API/database tuple during server preparation and the exact browser runtime before sign-in; database URLs and privileged credentials are never exposed. The one-use exercise digest is computed before preview creation from the exact release/review SHA, raw exercise UUID, backend fingerprint, public-target digest, fixture/persona digests, migration tip, and controller authority; it intentionally excludes Netlify's not-yet-issued deploy ID. The deploy ID and canonical origin remain mandatory independent bindings in the preview headers, backend exercise row and attestation, every phase authority, and every retained evidence record. Only the access token is exclusive to the Edge deployment job. The project reference also supports exact tuple checks in credential preflight, preparation, quiesce, final verification, and both recovery entrypoints; neither is emitted; service-role/password secrets are not referenced by that job. The browser receives only its public Supabase URL and safe public credential plus the safe exercise, backend, and public-target digests. No real provider credential may be present.

The CA is public tracked trust material, not an environment secret. Do not add CA PEM bytes or a CA path to the database URL, GitHub secrets, job-global environment, browser variables, or uploaded evidence. The exact database URL remains independently validated and must specify only `sslmode=verify-full` for TLS behavior. If CA validation fails, that database step and the phase stop; do not retry with `require`, disable hostname validation, or set `rejectUnauthorized` false. Abort/expiry cleanup may proceed only after its own immediately adjacent CA verifier succeeds.

Tracked source and uploaded evidence must never contain raw organization/workspace/user/exercise UUIDs, email addresses, passwords, API/service keys, database URLs, Supabase project references, custom/stable URLs, signed URLs, provider payloads, raw logs, transcript/source/customer content, or storage/object identities. The sole permitted URL in sanitized evidence is the canonical PR #264 Deploy Preview. Human identity is stored only as an HMAC pseudonym; the workflow checks that the three GitHub actors are distinct without writing their names.

The preparation workflow generates complete six-field role envelopes directly from its validated `preparation.json`, deriving the preparation digest in code and independently matching the expected release head and exercise digest. Humans must not construct or transcribe these bindings. Each human edits only the `observations` in their downloaded role-owned envelope; the generated kind, role, head, preparation digest, and exercise digest stay unchanged. Incomplete templates remain `not_run` and cannot pass compaction. From the exact reviewed checkout, they run:

```text
npm run pr-c-controlled-human:compact-comment -- --input <completed-role-input.json> --output <new-private-compact-output.json>
```

The command validates the exact six-field comment envelope, complete role/checkpoint/step ownership, every per-step PASS and timestamp, safe bounded browser evidence, exact server anchor/binding shapes, uniqueness, and the evidence-leak rules. It writes canonical minified JSON only when the result is at most 60,000 UTF-8 bytes, below the GitHub transport ceiling, and reports only the role, byte count, and digest. The human posts the exact compact file as one new PR #264 comment from their own GitHub `User` account. Never split, truncate, reformat, wrap in Markdown, or later edit the comment. If compaction rejects or exceeds the bound, stop the exercise for a reviewed transport redesign.

`CH-10` still exercises the canonical direct-transcript source-package authority, whose immutable source-coverage contract requires accepted extraction lineage. The synthetic controller supplies that prerequisite only as disabled, keyless, offline provenance tied to the exact exercise: the route is disabled, token/latency fields are absent, no provider secret or key reference exists, and no provider attempt or egress occurs. Inventory and observer safety exempt only that complete server-verifiable shape; an active route, key reference, unbound configuration/job, token or latency claim, provider audit, generation attempt, simulation, or any other provider row remains a blocking failure. These offline lineage rows are not real-provider evidence and cannot support a provider-readiness claim.

## Eight canonical journeys and fourteen checkpoints

Every listed step has its own `passed`, `failed`, or `blocked` observation, timestamps, exact persona key, evidence digest, zero provider/customer/external-user counts, and zero unexpected receipt/audit/target/item-version/approval/baseline side-effect counts. Aggregate workflow success cannot replace a step result. Before each server-observable action, the assigned human selects and arms exactly one step in the non-production banner. A server-owned catalog defines all 42 controlled actions: 34 positive events and eight denied attempts; evidence validation parses that SQL catalog rather than maintaining a second action map. The server issues an immutable pre-action anchor for the checkpoint, step, persona, canonical action, independently selected target family and ID, actual pre-action version, request identity, authorization version, and an exact action-specific scalar selector descriptor. Unknown, missing, extra, object, or array selector values fail closed. Authored text, rationale, conditions, manual briefs, complete item sets, revision subsets, and accepted sets are canonically hashed before anchoring; raw content is never retained. The normal application command or projection client then executes the real production path with that same server-issued request identity; only its exact receipt, audit, effect, and immutable domain outcome may complete the anchor. Creates bind the exact new target and parent/input selectors derived from that request, while updates validate the declared pre-to-post version transition. Replay steps reuse the original business idempotency identity and are valid only when the receipt/effect has exactly two total distinct attempts—the original and the current anchored retry—and exactly one effect; a third historical attempt fails closed. Response-loss recovery separately reuses one request body and proves two attempts but exactly one effect. The observer uses the same compact lexicographic JSON digest as the server, resolves the catalog-specific receipt/audit/effect causality, treats the Assess-conflict null audit as the exact server sentinel, and uses the current replay attempt while retaining original receipt/effect lineage. The browser receives only sanitized digest/token envelopes for `browserArtifact.serverAnchor` and `browserArtifact.serverBinding`; raw target, request, receipt, audit, selector, and denial-code values remain transient or server-private. Seeded/pre-existing state, an unrelated or aggregate action, a different valid owned resource, the same UUID under another family, a wrong valid version, an opposite outcome, an event outside the step window, or a reused anchor/event/binding cannot satisfy the contract. For denied requests, any effect linked through the exact source-qualified Delivery receipt or exact audit is counted even when its denormalized metadata is malformed, and actor/action mismatch is itself a rejection. Browser-only steps remain explicit signed human attestations with both fields `null`. For a negative step, arming still precedes the normal application attempt. The server wrapper invokes the canonical production projection or mutation authority with the anchored target and request, records proof only after its exact non-disclosing rejection, and rejects unexpected success without creating evidence. The stale-authorization exercise starts with an active authorized Delivery author, anchors the current authorization version, advances that version server-side, and accepts only the exact stale-authorization denial class; a permission, not-found, unavailable, or wrong-target denial cannot substitute. Only a denial-code digest reaches retained evidence. Static authority state may corroborate a denial but can never replace an attempted request.

| Checkpoint | Journey | Required human observation |
| --- | --- | --- |
| `CH-01` | Assess only | Select two Assess transcripts, manually complete remaining fields, resolve conflict, approve, decline Studio handoff, and prove no Studio resource. |
| `CH-02` | Studio only | Select two different Studio transcripts and custom template, edit, independently review/approve, stop, and prove no Delivery resource. |
| `CH-03` | Assess plus different Studio sources | Verify the separately seeded, fully governed Assess handoff is ready; select and lock the disjoint Studio supplement sources before the immutable module-handoff request; an independent Studio reviewer and approver decide that exact request; requester consumes it exactly once, verifies the locked supplements were consumed, then generates and approves the exact-source output. |
| `CH-04` | Full governed | Preview/request the exact Studio handoff; exercise changes-requested and rejection paths; prove neither creates a Delivery package/draft. |
| `CH-05` | Full governed | Fresh request, independent review/approval, one consumption, exact replay, and proof of no second package. |
| `CH-06` | Full governed | Inspect deterministic citations, edit with rationale, compare immutable history, decide every current proposal, and verify the complete bounded set. |
| `CH-07` | Full governed | Request package changes, prove Monitor unchanged, revise only selected descendants, then perform fresh complete review and approval. |
| `CH-08` | Full governed | Create the exact server-derived Monitor baseline, replay it, and prove the same baseline with no duplicate. |
| `CH-09` | Full governed | Compare Enterprise and primary Monitor parity; prove minimized read-only projection, no hashes/approval identities, no mutation controls, and legacy metrics non-authoritative. |
| `CH-10` | Direct planning | Direct Studio through Delivery/Monitor remains visibly `Not assessed · Planning only`. |
| `CH-11` | Direct Delivery | Manual package through independent review/approval and read-only Monitor remains visibly `Not assessed · Planning only`. |
| `CH-12` | Negative | Revoked, same-organization other-workspace, and cross-organization projection/mutation attempts are non-disclosing with zero side effects. |
| `CH-13` | Recovery | Response loss/reload converges on one effect; stale authorization/source changes reject; read-only mode preserves history and removes actions. |
| `CH-14` | Recovery/accessibility | Desktop Chrome, Pixel 7, 200% zoom, keyboard-only handoff/edit, error-summary focus, preserved input, logical focus return, non-color cues, and no horizontal overflow. |

The executable catalog and exact persona/Test-ID/step ownership live in `scripts/prCControlledHumanEvidenceContract.mjs`. Observation templates are generated from that catalog; hand-edited checkpoint structure is not authoritative.

## Three-human execution sequence

1. After exact-head CI, the exact PR preview, and independent bootstrap verification pass, the controller first applies `pr264-controlled-human-credentials-preflight` and waits for AP's actual Environment approval. Independently verify the resulting exact-run/attempt signed credential-transport artifact before requesting a new temporary scoped deployment PAT; confirm the previous token's actual Supabase revocation separately. Only then apply `pr264-controlled-human-edge` to PR #264 and wait for the run to finish before applying another phase label. The primary PR C workflow discovers a prior terminal successful exact-head CI artifact and executes `.github/workflows/transcript-flow-pr-c.yml` job `controlled_human_edge`. It derives the migration digest from the exact checkout, preflights/applies/verifies the additive controlled-human migration, preflights the same dedicated backend, deploys only the nine allowlisted functions, and produces the signed migration-and-provider-attestation artifact. A partial/failed run emits no trusted manifest and creates no active exercise or enabled feature flag. Agents must never submit or bypass an Environment approval.
2. After the Edge run succeeds, the controller applies `pr264-controlled-human-prepare`. The primary workflow discovers the prior CI and Edge artifacts and executes `.github/workflows/transcript-flow-pr-c.yml` job `controlled_human_prepare` with their exact run/attempt/digest bindings. Before Auth-user creation it verifies the protected service-role credential against the exact bound Supabase Admin API without reading, retaining, or printing the response body. Before the first Admin API mutation, the backend persists exact release/deploy/target recovery authority. Each created synthetic Auth identity is discoverable from exact synthetic metadata and is appended to that bounded authority. Apply can therefore resume idempotently after lost output, upload failure, cancellation, or runner loss without creating duplicate users. The resulting preparation artifact and three generated observation templates still record `controlledHumanDisposition: not_run`.
3. Three distinct humans use isolated browser contexts and the exact synthetic accounts. Each edits only the downloaded template assigned to one evidence role. For every `server_event` or `negative_attempt` step, start the step timer, expand **Two-phase exact action evidence** in the non-production banner, refresh the server step list, select exactly that step, and click **Arm before action** before performing any application action. The banner must show the sanitized pre-action anchor. Perform exactly the listed action through the normal application control; the central command/projection client reuses the anchor request identity and completes it only from the matching server result. Refresh the banner after the action: it selects the just-completed proof pair, and the separate completed-step selector lets the human deliberately return to any other completed proof. Copy that selected sanitized pair into `browserArtifact.serverAnchor` and `browserArtifact.serverBinding`. Never copy a raw resource ID, receipt, audit, request, console output, or network log. Leave both fields exactly `null` for browser attestations and no-effect checks. Missing pre-arming, a missing completion, ambiguous/reused evidence, stale or wrong persona/target/version, unexpected negative success, or an action outside the step window is a blocking failure; do not select another event as a substitute. Before quiesce the three humans complete every role-owned active step and retain the template locally; they do not yet post the PR comments. The generated templates are authoritative because checkpoints intentionally contain cross-role duties:
   - requester fragments occur in `CH-01` through `CH-07`, `CH-10`, `CH-11`, `CH-13`, and `CH-14`;
   - reviewer fragments occur in `CH-01` through `CH-07`, `CH-09` through `CH-14`; and
   - approver fragments occur in `CH-02`, `CH-03`, `CH-05`, `CH-07`, `CH-08`, `CH-10`, and `CH-11`.
4. Once all active steps are complete, the controller applies `pr264-controlled-human-quiesce`. The primary workflow discovers the exact preparation artifact, reverifies the exact preview and active backend, and executes `.github/workflows/transcript-flow-pr-c.yml` job `controlled_human_quiesce`. That protected job prepares exact server-held recovery authority, performs the idempotent server-authoritative transition to lifecycle `read_only`, disables mutation flags, freezes the relevant immutable-history digest at concurrency version `2`, and emits a signed transition record with its exact timestamp. A human comment or browser state cannot perform or substitute this transition.
5. Only after the quiesce run succeeds, the reviewer signs in through a fresh isolated browser context and completes `verify-history-readable-and-actions-absent`, the final `CH-13` read-only observation. The evidence contract rejects every active-step completion at or after the server transition and rejects the read-only observation if it began before that transition. Each human then posts their completed canonical JSON as one new, unedited PR #264 comment.
6. After all three comments exist, the controller applies `pr264-controlled-human-checkpoints` once. The authority job requires exactly one matching unedited comment per role and three distinct GitHub users, binds the exact preparation and quiesce run/attempt/artifacts, and captures requester first, approver second, and reviewer last. Every backend observer executes against the same read-only scope and must exactly reproduce the quiesce record's lifecycle, concurrency version, terminal operation-event sequence, operation-event digest, and immutable-history digest. Gapped event history fails closed. The observer reconstructs exact source-qualified request causality for server/denied steps and derives a scoped all-source absence witness for each browser-attestation or no-effect step. It rejects causal or absence-witness reuse within a role and across the complete session; capture cannot mutate lifecycle state.
7. After that run succeeds, the controller applies `pr264-controlled-human-final`. The primary workflow discovers the exact preparation, quiesce, and three checkpoint artifacts and executes `.github/workflows/transcript-flow-pr-c.yml` job `controlled_human_final`. It re-reads all three comments, rejects later edits or signer/content substitution, validates every signed record before lifecycle mutation, and deprovisions directly from the frozen read-only state—there is no resume-to-active transition. The SQL finalizer returns the exact terminal operation-event sequence and the controller cross-checks it against the ordered immutable ledger. A separate post-deprovision database query must reproduce the same stable-state, event, and history digests while producing a distinct server-generated inspection-attempt digest and non-regressing server observation time; that record is authority-bound to the first deprovision result. It also rechecks final provider traffic and the preview before uploading the complete sanitized final session.
8. Only a valid final session may produce `CONTROLLED-HUMAN=passed`. Failed, blocked, missing, duplicate-person, stale-attempt, mismatched-binding, unsafe-content, provider-egress, side-effect, Edge-source, ordering, or reset evidence fails closed. Phase labels must be applied one at a time only after the prior run has completed; to repeat a failed phase, remove its label before deliberately reapplying it.

## Defects, fixes, and retest invalidation

A material defect invalidates the entire human session and its head. Fix it in source, obtain new exact-head CI and Deploy Preview bindings, create a new exercise, and repeat all `CH-01` through `CH-14`. A later passing session may reference only sanitized defect and invalidated-session digests; it must bind the new fix head and prove full retest. Open defects or partial retests cannot pass.

## Reset, rollback, and exit disposition

Final verification deprovisions the exact exercise: all eleven controlled workspace flags are disabled, both runtime controls remain read-only with provider execution disabled, active memberships and sessions are removed, synthetic credentials are disabled, and immutable domain/audit history is retained. Domain rows are not deleted or rewritten. Apply, quiesce, and deprovision are replay-safe at every post-mutation/pre-artifact boundary. Preparation failure uses exact abort recovery. For hard cancellation, lost output, expired authority, or a later PR head, the controller records the prior exact head, deploy ID, exercise digest, backend target fingerprint, and public-target digest in five protected recovery variables, then applies `pr264-controlled-human-abort` or `pr264-controlled-human-expiry`. The protected workflow admits only the trusted owner actor under the `hosted-nonproduction-pilot` environment gate, proves the prior head belongs to PR #264, checks out the current reviewed PR head as executable recovery code, and supplies the prior head only as server-authority data. `expiry` additionally requires the server-held apply authority to be expired. The workflow deletes only discoverable exact synthetic Auth users when no exercise exists, otherwise deprovisions that one exercise. A failed bounded cleanup is blocking; never use broad project/database reset.

Secret-delivery rollback is also fail closed. Retain the failed run, remove or withhold the phase label, and block before protected use. Repair or revert the direct workflows, exact secret guards, versioned evidence identities, read-only preflight, tests, provenance, and documentation as one reviewed unit. Never restore sentinel relay as a working path, pass repository/organization secrets, use `inherit`, remove the fixed Environment, or bypass approval or first-use validation. Remove an unused GitHub token secret and revoke the actual Supabase token separately. A corrected source requires fresh exact-head CI/preview/bootstrap and a new AP-approved no-PAT preflight before another deployment token or protected operation.

TLS rollback is fail closed: retain the current exercise and recovery authority, block further database access, and update the tracked public CA only through a source-reviewed pin change with the complete TLS/workflow adversarial suite and a fresh exact-head run. Never restore access by weakening `verify-full` or by introducing an untracked or secret trust anchor.

The additive migration changes the default timestamp for new privileged-audit and Delivery command-attempt rows from transaction-start time to wall-clock time. A real action executed after a server-issued anchor therefore cannot appear before that anchor merely because prerequisite work shared its transaction. Existing immutable rows are not rewritten. Operational rollback remains the read-only/quiesce/deprovision path above; do not perform a destructive down migration or restore the causally ambiguous timestamp default.

An executed pass establishes only controlled, synthetic, PR-preview usability evidence for one exact PR #264 head. It does not establish production, AvalaOS.com/custom-domain, customer, real-provider, pilot/production, security certification, compliance certification, execution, completion, due-date, or live-telemetry readiness. PR #264 remains Draft until this gate and all exact-head automated gates pass; merge still requires separate final AP confirmation.
