# Assess document-mapping projection correction

Current corrective record within PR #264; no merge, production, real-provider or
whole-platform acceptance claim. Earlier local/release reports remain immutable.

## Executed exact-head CI and exploratory attempt

Head `a0f914ff2f3a35359806b2a088df8b9701e9a03f` completed 18 applicable workflows
successfully; the nineteenth, separately gated hosted-live workflow, was skipped.
Native mapping run
[35119038412](https://github.com/APReddy-AutoBotz/AvalaOS-Core/actions/runs/35119038412)
passed all five groups / 22 commands and 32 Desktop/Pixel mocked-browser scenarios.
The downloaded artifact was independently checksum/manifest verified, including
eight negative evidence mutations. Retained PR C run
[35119038592](https://github.com/APReddy-AutoBotz/AvalaOS-Core/actions/runs/35119038592)
was independently artifact-verified: 80 exact commands, 218 passed assertions and
eight explicit `not_run` boundaries. Neither artifact proves this later correction.

Only the separately approved exploratory synthetic target was activated. Its
full ledger contains 77 migrations with identity tip `20260916151050`; installed
runtime Edge source was compared against committed source. Six existing accounts
were preserved, provider configurations/usage stayed zero, and the original
controlled-human backend was untouched. The Netlify draft was immutable and
manually uploaded; all 56 asset digests matched. The published deployment was not
changed. Infrastructure/object identifiers, secrets and raw responses are omitted.

The actual Author browser attempt completed process creation, V2 case creation
and one manual draft save: three real committed commands. Projection HTTP 503
then blocked upload, before source creation or any provider effect. This attempt
remains **failed**, not a partial feature PASS. Independent read-only checks
confirmed zero source rows, Enterprise command receipts, mapping runs or provider
usage. The saved synthetic case is retained for a resumed retry; no duplicate
assessment or account reset is required.

## Confirmed source defect and correction

The production `createEnterpriseIntelligenceQueryDatabase` selected
`created_by` from `enterprise_evidence_candidate_relationship_reviews`.
The canonical migrated table defines `reviewer_id`, not `created_by`. The
failed parallel query rejected the complete projection. All other 50 literal
production selections matched the installed schema.

The existing query mock recorded only the table name and returned empty rows.
The PostgreSQL suites did not compare production selections to the real schema,
and generic row typing could not catch that drift. Both independent read-only
reviewers closed before the write phase; they identified no second mismatch.

The runtime correction changes only that selected column. The query unit checks
the complete exact-scoped selection, returns a canonical reviewer-owned row and
proves the authorized relationship reaches the candidate projection. A new
AST-owned schema contract extracts all 51 actual production load/query sites,
rejects unsupported/dynamic or substituted proof and compares every column to
the migrated PostgreSQL catalog. The retained PostgreSQL harness also restores
the bad owner column adversarially and requires a real missing-column rejection.
No migration, grant, client fallback, scoring or authority expansion is added.

Final architecture review found a gap in the first schema guard: a substituted
table or dropped column could still pass if the remaining columns existed. The
controller added an independent reviewed digest of all 51 ordered
target/call-kind/table/column tuples. The positive PostgreSQL gate checks this
binding before catalog access; its separate real-database negative still proves
the original nonexistent column is rejected by catalog evidence, not merely by
the digest. All 13 focused tests passed, including unchanged-count dropped-column,
valid-schema substitution, renamed-target and reordered-column adversaries.
Earlier source-bound local runs remain intermediate, not acceptance of this
final guard.

## Verification and rollback boundary

All five canonical local groups passed 22/22 commands at unchanged effective
runtime/test/execution source digest
`67aaf71183553f1b3ae224d80135bba22c69c747d5aee35cb4f62743f3cf6ccd`:

- Feature 10/10: `output/assess-import/validation/feature-e1922a89-dcf5-4d6b-bf03-dd98f81945d6/manifest.json`
- PostgreSQL 2/2: `output/assess-import/validation/postgres-440ead1b-e27b-4498-a09f-66c4a69a8b8a/manifest.json`
- Retained regression 1/1: `output/assess-import/validation/regression-3b4aa4c0-9950-4830-a820-3a8cd2d50e63/manifest.json`
- Static/build 8/8: `output/assess-import/validation/static-03414f23-cd24-4ff0-981b-e136d3a15ef8/manifest.json`
- Browser 1/1: `output/assess-import/validation/browser-3d7ac8a4-f3e4-4f66-a867-37780aa7de63/manifest.json`

The final browser report
`output/playwright/assess-import/7051801d4a6e1e9ac5a2bf20/results.json`
contains 32 passed Desktop/Pixel scenarios, zero skips/flaky/unexpected results
and no runner errors. Synthetic transport is disclosed; no real AI was called.
The independent source-contract review closed after reproducing rejection of the
same-count missing-column and valid-schema table substitutions. Its 13/13 focused
checks and the query unit passed. Both final bounded reviewers found no remaining
correction blocker after this closure.

The full ordered 77-migration transcript gate validated all production selections
against PostgreSQL and rejected the original bad column. Six actual recovery and
response-loss checks passed; only the owned memory-backed container was removed.
Result: `output/assess-import/release-local/fb2ddf70-9cc6-4bcf-8899-68f2799cc99a/result.json`.
Typechecks, workflow YAML, AI/static boundary, secret hygiene, scoring and build
passed. Dependency audit reported zero vulnerabilities. Acceptance catalog,
inventory and adversarial provenance checks passed with 108 source-backed
branches and 10 composites (metadata proof, not 108 hosted passes). The selected
PR C evidence-contract/verifier invocation passed 28/28 tests.

One intermediate browser attempt at prior digest `4401b8d8...` remains failed:
25 scenarios passed and seven later mobile entries could not find the initial
catalog. A concurrent static build replaced the shared output during that run
(build 17:34:22–17:34:36 UTC; first failed entry 17:34:34 UTC). The controller
serialized the final browser run after the build; no timeout or assertion was
weakened. The earlier 21 passing commands and failed browser report are retained,
not relabelled as final-source proof.

The new helper tests are part of the existing canonical harness command; no
extra evidence-only PR or separate branch is introduced. Current provenance and
`git diff --check` passed before commit. Documentation/derived-index reconciliation
does not change the effective runtime/test snapshot; exact committed-head CI
remains a separate mandatory gate.

After local checks and commit, the replacement requires fresh exact-head CI,
source-verified exploratory Edge deployment, an immutable bound draft, and actual
upload/set/bundle persistence plus fresh Desktop/Pixel reopen/signout checks.
The original failed three-command attempt must remain visible if its receipts
are used to bind the resumed journey. No skipped assertion becomes PASS.

Rollback disables new native mapping effects and preserves immutable history;
manual Assess remains available where authorized. Do not add a compatibility
column or weaken permissions to hide a projection error. Real-provider mapping,
three-human acceptance, merge, production and certification remain `not_run`
or unproven under the existing approval boundaries.
