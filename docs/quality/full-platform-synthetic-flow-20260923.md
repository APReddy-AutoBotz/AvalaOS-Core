# Synthetic platform-flow continuation — 2026-09-23

Scope: the separately approved `avalaos-ai-synthetic` non-production project and a private local preview. No production, AvalaOS.com, customer data, real human approval, or provider calls were used in this continuation. The OpenAI campaign's six additional effects were already consumed before this run, so no paid retry was attempted.

## Executed business steps

- The synthetic author signed in, created a process, created and saved an Assess V2 case, supplied declared synthetic process/application/interaction facts, and finalized a reviewer-ready Decision Pack. The server retained the immutable decision; sign-out and the restricted network observer passed.
- A separate insufficient-evidence case had 21 material decision claims but submitted evidence for only one. An independent synthetic reviewer assignment and `changes_requested` resolution committed. Approval was not attempted on that case.
- A uniquely named second scenario finalized with all 21 material claim IDs attached to a synthetic `test` evidence link. Independent reviewer assignment committed. The initial attestation attempt failed closed with HTTP 503 / `COMMAND_UNAVAILABLE`; server readback showed zero attestations and zero review resolutions at that point. After the exact-head correction, the independent reviewer accepted the evidence and approved the decision.
- After the second exact-head correction, a different synthetic approver resolved all 13 Govern controls and created one durable Assess → Studio handoff. Server readback joined one accepted attestation, one approval, one Govern resolution, one handoff, and one Studio source. The browser signed out under a restricted network observer without provider calls.
- The Studio author could see one eligible handoff and the Request handoff button. Initially the request failed closed with HTTP 403 / `PERMISSION_DENIED` before any receipt because fresh Edge authority listed only `studio.artifacts.*`. After the guarded authority correction, a new request committed one handoff and one receipt, but the browser received HTTP 503 / `COMMAND_UNAVAILABLE` with a false `failed_before_commit` outcome. The SQL response omitted the required receipt ID, so Edge normalization failed after commit. Server readback established the requested handoff; no artifact was created.
- The initial author browser harness created three same-name disposable process records while the catalog was still loading. It was corrected to await settled catalog state, and the second scenario uses a unique name. No synthetic records were deleted or represented as business proof.
- Four Assess project-only capabilities were granted with separate explicit user approvals to the existing synthetic roles: author `assess.v2.finalize`; reviewer `assess.v2.approve`; approver `assess.v2.govern.resolve` and `assess.v2.studio.handoff`. The user then approved seven exact Studio handoff grants: author read/request/consume, reviewer read/review, approver read/approve. The guarded synthetic workspace flag `module_handoffs_enabled` was enabled to exercise this approved path. None of these changes affect production or other projects or constitute human acceptance.

## Confirmed defects and bounded correction

1. The process-bound V2 client looked up only `draft` and `reviewer_ready`, making an existing case disappear from Assess after review assignment. It now discovers every non-deleted, non-superseded lifecycle state while preserving tenant/workspace/process predicates and stable latest-case ordering.
2. The case author with `assess.v2.draft.write` could not load the review projection to start a requested revision. The review component now permits that read and enables the revision control only for a draft writer. The server still enforces ownership and every command capability.
3. The private PR 1E attestation function's unparenthesized JSON containment operands parsed as `boolean -> unknown` (rollback-only diagnostic SQLSTATE `42883`). The append-only migration parenthesizes both operands in the exact existing function source, fails on source mismatch, advances the non-production migration identity and bootstrap binding, and is reapply-safe. No review/approval rule or capability changes in this migration.
4. After the synthetic-only attestation correction and exact-head PostgreSQL gates, the separate reviewer accepted the exact 21-claim evidence link and approved the decision; readback found one accepted attestation and one approval. The next approver's Govern command failed closed with HTTP 503 and no resolution row. Sanitized PostgreSQL logs classify three attempts as SQLSTATE `42702`, ambiguous column `control`: the command function has both a PL/pgSQL `control` record and an unqualified aggregate column `control`. A second append-only source-bound correction qualifies the aggregate as `required.control`, preserves the Govern rules, and advances the synthetic migration identity. At that point Govern remained blocked.
5. The Govern correction was verified in exact-head PostgreSQL CI, applied only to the synthetic project, and the browser/server chain then committed Govern and the durable Assess → Studio source. Studio's Edge command preflight revealed a separate artifact-only authority projection; an append-only, source-bound migration extends its fresh capability projection to the handoff and template command families while requiring a matching Studio read capability. It grants no roles and leaves command-specific SQL checks intact. Exact-head PostgreSQL CI passed, and synthetic readback verified the new capability families and marker.
6. The next browser request revealed that the SQL handoff command omitted `receiptId` from committed and replay responses, though the Edge adapter requires it. An append-only, source-bound correction adds `receiptId` to new commits and exact replays, including the previously committed synthetic row, without rewriting stored historical responses or authorizing extra roles. The Edge/client boundary now reports a post-RPC response mismatch or post-effect authority loss as `commit_uncertain`, blocks further mutation, and requires a fresh reload instead of falsely claiming pre-commit failure. Focused PostgreSQL tests assert request, review, approval, consume, and replay receipt IDs. Exact-head CI and synthetic verification of this newest correction remain pending.

The server's stricter meaningful-interaction validator also rejected an incomplete draft while the browser left Finalize enabled and displayed a generic error. Supplying the four missing declared read-interaction facts allowed finalization. That UI validation mismatch remains a separate user-facing issue; server rejection was correct and no partial decision was written.

## Verification and remaining gates

The focused V2 client/review component tests, TypeScript typecheck, and private-preview build passed after the client/UI correction. The first exact-head CI attempt exposed linked migration-tail/identity preflight failures; these were failed attempts, not accepted proof. Subsequent exact-head CI validated the repaired PostgreSQL chain before the two synthetic-only corrections were applied. For the Studio authority migration, local migration-tail adversaries (8/8), source contract, and `git diff --check` passed, then exact-head PostgreSQL CI passed before guarded synthetic application. For the newer receipt correction, the focused Edge command/DB adapter, Studio client/UI, both typechecks, workflow YAML, migration-tail adversaries (8/8), source contract, and `git diff --check` passed locally. Local PostgreSQL execution is `not run` because Docker's Linux engine is unavailable; exact-head PostgreSQL CI and synthetic application remain pending. The private preview is synthetic browser evidence, not exact-head hosted acceptance proof.

The connected happy path is still **not complete**. The retained scenario now has Assess review/approval, Govern resolution, a durable Assess → Studio source, and one requested Studio module handoff, but no approved Studio artifact, Delivery work package, or Monitor baseline. After exact-head CI validates the receipt correction and it is applied only to the approved synthetic target, reconcile the committed request through exact replay, then continue with independent review/approval/consume actions and verify exact server records and denied-role boundaries. Existing AI-generated PDD/BRD/FRD drafts are separate observations, not evidence of this case's downstream handoff. The hosted Supabase migration applications were assigned tool-generated history timestamps different from repository filenames; the SQL markers and function bindings are exact, but this synthetic history difference remains disclosed rather than manually rewritten. Do not mark PR #264 Ready or merge based on synthetic role simulation.

Rollback: leave append-only review and handoff history intact. If a forward correction fails, use the existing Assess V2 read-only/disable fallback and disable synthetic module handoffs, retain receipts and audit, and forward-fix. Do not rewrite prior migrations or approve an unattested decision.
