# M3.2b Provider Config Schema/RLS Proposal Post-Merge Verification

Milestone: M3.2b Schema/RLS Migration Proposal Review for Provider Config and Key References

Status: Post-merge verification only

Date: 2026-06-06

## Merge Record

- Merged PR: `#15 M3.2b propose provider config schema RLS`
- Merge commit SHA: `adfc71a5bd2345deea5ba7c214e2f212ce591059`
- Planning/proposal commit SHA: `f34ec00aa8c559241ec92e53c190baeb28c57c83`
- Current `main` HEAD SHA before this verification evidence commit: `adfc71a5bd2345deea5ba7c214e2f212ce591059`

## Repository And Remote Safety

- Current repository: `APReddy-AutoBotz/AvalaOS-Core`
- Confirmed only expected `origin` remote:
  - `origin` fetch: `https://github.com/APReddy-AutoBotz/AvalaOS-Core.git`
  - `origin` push: `https://github.com/APReddy-AutoBotz/AvalaOS-Core.git`
- No push was made to the historical Klarity repository.
- Health was untouched.

## Files Verified

Merged M3.2b planning/proposal files verified on `main`:

- `docs/planning/m3.2b-provider-config-schema-rls-proposal.md`
- `docs/quality/m3.2b-provider-config-schema-rls-proposal-evidence.md`

This post-merge evidence file is the only file added by this verification step:

- `docs/quality/m3.2b-provider-config-schema-rls-proposal-post-merge-verification.md`

## Proposal Content Confirmation

`docs/planning/m3.2b-provider-config-schema-rls-proposal.md` records:

- current schema inventory from `docs/schema/ai_governance_jobs.sql`
- classification of existing `ai_provider_configs`, `prompt_templates`, `ai_generation_jobs`, and `ai_usage_events`
- candidate schema proposal
- RLS/access model proposal
- secret/key-reference proposal
- AP clarification that raw provider secrets must not be stored even encrypted in ordinary application DB rows unless separately approved
- migration sequencing proposal
- M3.1c scanner interaction
- open questions / AP decision points
- stop conditions
- clear statement that candidate schema discussion is not approval to migrate, alter, depend on, or implement these structures in this milestone

## Planning / Proposal Only Confirmation

This post-merge verification did not perform M3.2b implementation.

Confirmed unchanged by this verification step:

- no schema/RLS migration was created
- no migration file was edited
- no Supabase Function changes
- no Admin UI changes
- no runtime AI behavior changes
- no product code changes
- no tests changed
- no package/package-lock/dependency changes
- no Health changes
- no scoring changes
- no runtime-agent, MCP/A2A, or external execution changes
- no compliance claims

## Verification Command Results

| Command | Result |
| --- | --- |
| `npm run typecheck` | Passed. |
| `npm run test` | Passed after sandbox retry. Deterministic scoring, Delivery Policy, Avala Govern Lite, Delivery Pack, and AI boundary tests passed. |
| `npm run test:ai-boundary` | Passed after sandbox retry. AI mode boundary regression suite passed. |
| `npm run test:ai-boundary-static` | Passed after sandbox retry. Static scan reported 15 patterns checked, 603 allowed hits, 0 forbidden hits, and 0 stale allowlist entries. |
| `npm run build` | Passed. Vite production build completed successfully. |
| `npm audit --audit-level=moderate` | Passed. Reported 0 vulnerabilities. |
| `git diff --check` | Passed. No whitespace errors. |
| Wording scan | Passed after review. Hits were limited to migration/history notes, Health separation notes, active repo identifiers, no-go/control docs, planning/evidence prohibited-boundary references, and required verification command references. |

## Static Scan Summary

`npm run test:ai-boundary-static` passed with:

- 15 patterns checked
- 603 allowed hits
- 0 forbidden hits
- 0 stale allowlist entries

The M3.2b post-merge verification did not change the scanner, allowlist, runtime code, Supabase Functions, schema, migrations, tests, package files, or dependencies.

## Wording Scan Review

The required wording scan was run:

```powershell
rg -n "KlarityPM|Klarity PM|Klarity|AutoBotz|AI Delivery Brain|HIPAA compliant|SOC 2 compliant|GDPR compliant|GxP compliant|autonomous decision|agent decides|AI decides score"
```

Reviewed hits were allowed historical/control references:

- migration/history notes
- Health separation/protection notes
- active repository identifier references
- no-go/control documentation
- planning/evidence prohibited-boundary references
- required verification command references

No wording scan hit introduced product copy claiming that AI decides deterministic scores, approvals, risk gates, recommendations, compliance status, or regulated decisions. No unsupported compliance claim was introduced.

## Guardrail Confirmations

- No M3.2b implementation was performed.
- No schema/RLS migration was created.
- No migration file was edited.
- No Supabase Function changes were made.
- No Admin UI changes were made.
- No runtime AI behavior changes were made.
- Health was untouched.
- Scoring was untouched.
- Product code was untouched.
- Tests were untouched.
- Package files and dependencies were untouched.
- Runtime agents, MCP/A2A, and external execution were untouched.
- No unsupported compliance claims were introduced.
- Future implementation remains AP-gated.

## Risks / Open Questions Carried Forward

- Final schema shape for provider config, key refs, workspace policy, audit events, prompt versions, and usage events remains undecided.
- Whether existing `ai_provider_configs` is sufficient after alteration remains undecided.
- Whether key refs need a separate table remains undecided.
- Whether workspace provider policy needs a separate table remains undecided.
- Whether provider audit events can reuse jobs/usage or require `ai_provider_audit_events` remains undecided.
- Exact admin/control-plane actor model needs AP-approved design.
- Migration rollback strategy and validation evidence need AP-approved design.
- Future secret-manager integration may require package/dependency changes, which are not approved in M3.2b.
