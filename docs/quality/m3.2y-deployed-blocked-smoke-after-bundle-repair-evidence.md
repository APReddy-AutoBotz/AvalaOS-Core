# M3.2y Deployed Blocked-Smoke After Bundle Repair Evidence

## Summary

- Branch: `milestone/m3.2y-deployed-blocked-smoke-after-bundle-repair`
- Commit SHA: pending until this evidence commit is created
- Repository: `APReddy-AutoBotz/AvalaOS-Core`
- Remote safety: confirmed repository is not `Klarity-PM` and not `KlarityFlow-Health`
- Baseline tag: `avalaos-core-m3.2x-edge-deployment-bundle-readiness-repair` confirmed present
- Files changed:
  - `docs/planning/m3.2y-deployed-blocked-smoke-after-bundle-repair.md`
  - `docs/quality/m3.2y-deployed-blocked-smoke-after-bundle-repair-evidence.md`
- Outcome: Outcome B - readiness blocked
- Blocker: deployed Edge runtime returned `mode_not_allowed`, indicating the remote deployed runtime is not exposing the required pilot runtime mode to the function.

## Scope Confirmation

No code, runtime resolver logic, provider decision behavior, Edge Function entrypoint, `_shared/ai.ts`, schema/RLS/migration, Admin UI, product UI, Health, scoring, browser fallback, dependency, usage, or cost behavior was changed.

No deploy or redeploy was performed during M3.2y because the required functions were already present from the M3.2x bundle-readiness validation.

## Env Visibility

Values were loaded from local `.env` into the PowerShell process only for the checks that required them. No values were printed or stored.

Boolean-only visibility summary:

| Name | Visible |
| --- | --- |
| `DATABASE_URL` | true |
| `SUPABASE_ACCESS_TOKEN` | true |
| `SUPABASE_URL` | true |
| `SUPABASE_ANON_KEY` | true |
| `SUPABASE_SERVICE_ROLE_KEY` | true |
| `AVALA_AI_RUNTIME_MODE` | true |

Runtime mode:

- `AVALA_AI_RUNTIME_MODE.is_pilot=true`

## Target Safety

Target details were not printed. Classification-only target checks:

- Supabase URL parse: true
- Supabase host classification: Supabase host
- Supabase project ref shape: true
- Supabase target disallowed markers absent: true
- Database URL parse: true
- Database scheme: PostgreSQL-compatible
- Database host classification: Supabase direct or pooled host
- Database target disallowed markers absent: true
- Runtime mode is pilot locally: true
- Target safety passed: true

## DB Readiness

Command: `npm run test:m3.2p-smoke`

Result: pass.

Sanitized result summary:

- `DATABASE_URL` visibility: visible without value
- Non-production guard: passed
- Provider governance tables: present
- Seed/verify: smoke rows available inside transaction
- Blocked scenarios: passed
- Allowed mocked scenarios: passed
- Audit sanitization: passed for 9 smoke audit events
- Cleanup: transaction rolled back; no smoke rows retained
- No live provider call occurred; only mocked provider handoff was invoked
- Outcome A for the local mocked DB readiness harness

## Supabase CLI And Function Readiness

Command: `supabase --version`

Result: `2.101.0`

Sanitized function listing result:

- `supabase_functions_list_exit_zero=true`
- `function_ai-generate-document_present=true`
- `function_ai-refine-section_present=true`
- `function_ai-provider-test-connection_present=true`
- `required_functions_present=true`

Deploy readiness:

- Required functions were present.
- No redeploy was performed.
- No deployed invocation occurred during the deploy-readiness step.

## Deployed Blocked-Smoke Result

The deployed smoke runner created a temporary auth session and disposable smoke rows, invoked only blocked paths, and used explicit cleanup. It did not print URLs, project refs, DB coordinates, tokens, auth headers, provider keys, or secret values.

Sanitized deployed-smoke transcript:

```text
M3.2y deployed blocked smoke starting.
DATABASE_URL.visible=true
SUPABASE_ACCESS_TOKEN.visible=true
SUPABASE_URL.visible=true
SUPABASE_ANON_KEY.visible=true
SUPABASE_SERVICE_ROLE_KEY.visible=true
AVALA_AI_RUNTIME_MODE.visible=true
AVALA_AI_RUNTIME_MODE.is_pilot=true
target_safety_supabase_host_ok=true
target_safety_database_host_ok=true
target_safety_markers_absent=true
target_safety_project_ref_shape_ok=true
target_safety_passed=true
db_required_tables_present=true
auth_session_ready=true
seed_rows_ready=true
deployed_generate_missing_policy_observed_failure_class=mode_not_allowed
retained_rows_zero_after_failure=true
cleanup_after_failure_attempted=true
Outcome B - blocked at deployed_runtime_mode: remote_runtime_mode_not_pilot
```

Scenario result:

- Attempted first deployed blocked scenario: `ai-generate-document` missing-policy blocked smoke.
- Expected provider-policy block was not reached.
- Observed safe fail-closed class: `mode_not_allowed`.
- Interpretation: deployed invocation is available, but the remote Edge runtime is not exposing the required pilot runtime mode.
- Per milestone stop rules, no further deployed smoke scenarios were run.

## Audit Sanitization

Deployed audit sanitization was not reached because the milestone stopped at the deployed runtime-mode readiness blocker.

The failure occurred before provider-policy, secret lookup, live provider execution, or provider billing paths. Cleanup removed smoke rows after the failed attempt, and retained row verification passed.

## Cleanup And Retention

Cleanup result:

- `cleanup_after_failure_attempted=true`
- `retained_rows_zero_after_failure=true`

Retained smoke rows:

- Zero retained smoke rows confirmed after the failed deployed attempt.

## Provider Safety

- No Gemini or Groq live API call occurred.
- No real provider key was used.
- No provider billing path was exercised.
- No deployed allowed path was attempted.
- No remote runtime, provider, schema, or function code change was made to force progress.

## Verification Results

| Command | Result |
| --- | --- |
| `npm run typecheck` | Pass |
| `npm run test` | Pass |
| `npm run test:provider-resolver` | Pass |
| `npm run test:provider-resolver-integration` | Pass |
| `npm run test:m3.2p-smoke` | Pass |
| `npm run test:ai-boundary` | Pass |
| `npm run test:ai-boundary-static` | Pass after removing generated `.agent` test-output directories |
| `npm run build` | Pass |
| `npm audit --audit-level=moderate` | Pass; found 0 vulnerabilities |
| `git diff --check` | Pass |

## Static Scan Summary

AI boundary static scan:

- Patterns checked: 15
- Allowed hits: 675
- Forbidden hits: 0
- Stale allowlist entries: 0

Generated `.agent` test output from resolver test emit commands initially caused forbidden hits in generated JavaScript. The generated directories were removed and the required static scan passed. No source or committed documentation change was needed for that cleanup.

## Wording Scan

Command:

```text
rg -n "KlarityPM|Klarity PM|Klarity|AutoBotz|AI Delivery Brain|HIPAA compliant|SOC 2 compliant|GDPR compliant|GxP compliant|autonomous decision|agent decides|AI decides score"
```

Result: reviewed.

Summary:

- Hits are historical migration notes, Health separation guardrails, no-go/control examples, prior evidence command references, active repository identifier references, and the two new M3.2y docs' repo-safety/command references.
- New M3.2y hits are limited to repository safety wording and the required scan command.
- No unsupported compliance claim was introduced.
- No product copy was introduced implying AI decides scores, approvals, risk gates, recommendations, compliance status, or regulated decisions.

## M3.2z Recommendation

M3.2z should perform an AP-approved non-production remote runtime configuration readiness repair:

- Verify deployed Edge runtime mode without printing target or secret values.
- Set only the required remote Edge runtime mode to `pilot` if AP approves that action.
- Do not set or use provider keys.
- Do not call live Gemini or Groq APIs.
- Rerun M3.2y blocked-smoke scenarios after remote runtime mode readiness is confirmed.

## Risks Carried Forward

- Remote deployed Edge runtime mode is not currently confirmed as pilot from inside the deployed function.
- Deployed provider-policy, config, key-reference, and audit-sanitization blocked scenarios remain unverified because the milestone stopped at runtime-mode readiness.
- A safe deployed allowed path remains unavailable unless AP approves a mocked deployed path that cannot call live providers.
- Supabase CLI printed an update notice during one listing attempt; final function listing used sanitized in-memory text capture and printed only presence booleans.
