# M4.1d-1 Audit Gate Remediation Evidence

## Summary

- Branch: `milestone/m4.1d-1-audit-gate-remediation`
- Base main commit: `1a61a3b1b2fcecbea5b631f7366e22f98979bb75`
- Scope: npm audit gate remediation only.
- Result: `BLOCKED` for full audit clearance because the remaining npm remediation path requires `npm audit fix --force` and a semver-major Vite upgrade.
- `npm audit fix --force`: avoided.

## Repo Safety

- Remote verified: `origin https://github.com/APReddy-AutoBotz/AvalaOS-Core.git`
- Repository safety: confirmed as `APReddy-AutoBotz/AvalaOS-Core`
- Historical prototype repository: not used.
- KlarityFlow Health repository/files: not used or modified.
- `.env` files: not opened or committed.
- Deployed functions: not invoked.
- Providers: not called.
- Secrets/provider keys: not printed.

## Original Audit Failure Summary

The post-merge M4.1d-1 verification on `main` failed only at:

```text
npm audit --audit-level=moderate
```

Original audit JSON reported 3 high-severity advisories:

| Package | Direct | Vulnerable range | Chain / effect | npm fixAvailable |
| --- | --- | --- | --- | --- |
| `@vitejs/plugin-react` | Yes | `4.0.0-beta.0 - 5.1.4` | via `vite` | `true` |
| `vite` | Yes | `4.2.0-beta.0 - 8.0.3` | via `esbuild` | `vite@8.0.16`, semver major |
| `esbuild` | No | `0.17.0 - 0.28.0` | transitive through `vite` | `vite@8.0.16`, semver major |

The advisory was `GHSA-gv7w-rqvm-qjhr`, titled `esbuild: Missing binary integrity verification in Deno module enables remote code execution via NPM_CONFIG_REGISTRY`.

## Affected Versions

Before remediation, the lockfile contained:

| Package | Version |
| --- | --- |
| `@vitejs/plugin-react` | `5.1.2` |
| `vite` | `6.4.2` |
| `esbuild` | `0.25.12` |

After plain `npm audit fix`, the lockfile contains:

| Package | Version |
| --- | --- |
| `@vitejs/plugin-react` | `5.2.0` |
| `vite` | `6.4.3` |
| `esbuild` | `0.25.12` |

## Remediation Applied

Ran:

```text
npm audit fix
```

This avoided `--force` and changed only `package-lock.json`. It updated the non-forced dependency resolution inside the existing package ranges:

- `@vitejs/plugin-react`: `5.1.2` -> `5.2.0`
- `vite`: `6.4.2` -> `6.4.3`
- related Babel/plugin utility transitive entries in the lockfile

No `package.json` version range changed.

## Remaining Blocker

After the safe remediation, `npm audit --audit-level=moderate` still fails with 2 high-severity advisories:

| Package | Direct | Remaining issue | npm remediation path |
| --- | --- | --- | --- |
| `vite` | Yes | depends on vulnerable `esbuild` range | `npm audit fix --force`, installs `vite@8.0.16` |
| `esbuild` | No | vulnerable transitive package through `vite` | `npm audit fix --force`, installs `vite@8.0.16` |

The required remediation is a semver-major Vite upgrade. AP instructions prohibit `npm audit fix --force`, broad dependency upgrades, and major upgrade application without explicit AP approval. No override was added because forcing `esbuild` outside Vite's declared dependency range would not be a clearly compatible non-breaking remediation.

## Changed Files

This PR changes only:

1. `package-lock.json`
2. `docs/quality/m4.1d-1-audit-gate-remediation-evidence.md`

No product, UI, routing, scoring, provider, schema, Supabase Edge, or Health file was changed.

## Verification Results

Final verification status: `BLOCKED` because npm audit still fails and the only remaining npm-provided fix is a forced semver-major Vite upgrade.

| Command | Result | Notes |
| --- | --- | --- |
| `npm run typecheck` | Passed | TypeScript check completed successfully. |
| `npm run test` | Passed | Full regression chain completed successfully. |
| `npm run test:view-access-guard` | Passed | M4.1d-1 guard regression passed. |
| `npm run test:decision-pack-rendering` | Passed | M4.1a regression passed. |
| `npm run test:assess-to-studio-handoff` | Passed | M4.1b regression passed. |
| `npm run test:docs-to-delivery-lineage` | Passed | M4.1c regression passed. |
| `npm run test:ai-boundary` | Passed | AI-boundary regression passed. |
| `npm run test:ai-boundary-static` | Passed | Final scan passed after removing generated `.agent` provider-resolver test output. |
| `npm run build` | Passed | Vite `6.4.3` production build completed successfully. |
| `npm audit --audit-level=moderate` | Failed | 2 high-severity advisories remain in the Vite/esbuild chain; npm requires `npm audit fix --force` to install `vite@8.0.16`. |
| `git diff --check` | Passed | No whitespace errors. |
| `git diff --cached --check` | Passed | Staged diff check completed before commit. |
| Wording scan | Passed after review | Hits were expected historical migration, repository identity, Health separation, no-go/control, planning, and evidence-command references. No product copy was introduced. |

## Static Scan Summary

Final `npm run test:ai-boundary-static` result:

- Patterns checked: 15
- Allowed hits: 713
- Forbidden hits: 0
- Stale allowlist entries: 0

The full test run generated `.agent/provider-resolver-tests` and `.agent/provider-resolver-integration-tests`. These generated test-output folders were removed before the final static scan. No tracked runtime code was changed.

## Wording Scan Summary

The wording scan returned existing historical migration, repository identity, Health separation, no-go/control, planning, and evidence-command references. The new evidence file adds repo-safety references only. No product copy or unsupported product claim was introduced by this remediation.

## No Product Expansion Confirmation

Confirmed:

- No product code change.
- No `App.tsx` change.
- No View/RBAC guard integration.
- No Sidebar change.
- No ModuleJourney change.
- No UI or routing behavior change.
- No deterministic scoring formula, weight, threshold, hard stop, gate, risk tier, recommendation, or score-version change.
- No schema, RLS, or migration change.
- No Supabase Edge Function change.
- No provider runtime or governance behavior change.
- No Health change.
- No deployment, deployed function invocation, or provider call.

## Recommendation

Do not create the pending `avalaos-core-m4.1d-1-central-view-rbac-guard-helper-tests` tag until a post-merge verification run passes `npm audit --audit-level=moderate`.

AP should choose one of these next actions:

1. Explicitly approve a focused Vite 8 semver-major audit remediation PR with full regression verification.
2. Explicitly approve an alternative package-manager override strategy after accepting the compatibility risk.
3. Defer the tag and carry the audit gate as an unresolved dependency-risk exception.
