# M5.3a-7 RLS and Artifact Evidence Harness Design Plan Post-Merge Verification

## Closure Record

Milestone: M5.3a-7 RLS and Artifact Evidence Harness Design Plan.

PR: `#150`.

Accepted/head commit: `438f793f8905d2216ec61f840ce97f31466df1a5`.

Actual merge commit: `2d01ca4a1f825d0ecd7d22bcfdd25be3c663e391`.

Post-merge verification commit: the commit containing this closure document.

Tag target: the commit containing this closure document.

PR #150 was verified as merged. Current `main` includes the merge commit.

## Files Verified

PR #150 merged exactly:

- `docs/planning/m5.3a-7-rls-and-artifact-evidence-harness-design-plan.md`
- `docs/quality/m5.3a-7-rls-and-artifact-evidence-harness-design-planning-evidence.md`

## File Changed By Closure

- `docs/quality/m5.3a-7-rls-and-artifact-evidence-harness-design-plan-post-merge-verification.md`

No code, tests, package files, CI, Supabase files, SQL, migrations, RLS policies, artifact harnesses, provider files, runtime files, UI files, deployment files, or prior evidence files were changed by this closure.

## Docs-Only Planning Closure Summary

M5.3a-7 closed a docs-only harness design planning milestone after the M5.3a-6 RLS and artifact evidence approval gate.

The plan defines a future approval-gated sanitized evidence harness concept, proposed assertion workflow, design-only assertion set, harness safety controls, sanitized output contract, prohibited outputs, stop and fail-closed conditions, decision paths, recommended path, future Path A candidate milestone, explicit non-goals, and proof boundaries.

This closure does not start M5.3a-8, does not start technical implementation, does not implement a harness, does not add tests, does not execute DB, schema, RLS, or artifact validation, does not reopen local readiness, and does not claim schema, RLS, artifact, tenant, local, hosted, production, or startup readiness.

## Accepted M5.3a-6 Baseline

The accepted M5.3a-6 baseline remains:

- M5.3a-6 closed as a docs-only approval gate;
- no assertion is approved for execution;
- no harness exists or is implemented;
- local-readiness loop remains stopped;
- local DB remains unresolved;
- schema is not proven;
- RLS is not proven;
- RLS helper behavior is not newly validated;
- artifact SELECT isolation is not verified;
- tenant isolation is not newly verified;
- hosted readiness is not proven;
- production readiness is not proven;
- local startup success is not proven;
- no local readiness evidence exists.

M5.3a-7 does not change or prove any baseline item.

## Proposed Harness Design Concept

M5.3a-7 records this design-only future harness concept:

- harness mode: `approval-gated-sanitized-evidence-harness`;
- execution mode: not implemented;
- run mode: not approved;
- DB execution: not approved;
- schema inspection: not approved;
- RLS tests: not approved;
- artifact assertions: not approved;
- output mode: sanitized buckets only;
- raw output mode: prohibited;
- local value capture: prohibited;
- proof-boundary output: required.

No implementation, run, DB execution, schema inspection, RLS test, artifact assertion, hosted validation, or command execution was approved by this concept.

## Proposed Assertion Workflow

M5.3a-7 records this future planning-level workflow:

- AP selects the exact future milestone.
- AP selects exact assertion names.
- AP approves scope and tenant boundary.
- AP approves whether schema availability may be checked.
- AP approves whether RLS helper behavior may be checked.
- AP approves whether artifact SELECT isolation may be checked.
- AP approves same-tenant and negative-path evidence.
- AP approves run count.
- A future harness checks prerequisites.
- A future harness fails closed if prerequisite, scope, tenant boundary, or output safety is ambiguous.
- A future harness records only sanitized buckets.
- A future harness records proof boundaries.
- A future harness stops without retrying into broader scope.

No workflow step is approved for implementation or execution by M5.3a-7.

## Proposed Assertion Set

M5.3a-7 carries forward these assertion names as design-only:

- `schema-availability-precondition`
- `rls-helper-tenant-context-boundary`
- `rls-helper-missing-context-fail-closed`
- `rls-helper-invalid-context-fail-closed`
- `artifact-select-same-tenant-allowed-if-approved`
- `artifact-select-cross-tenant-blocked`
- `artifact-select-missing-context-blocked`
- `artifact-select-invalid-context-blocked`
- `artifact-select-output-sanitization`
- `evidence-harness-fail-closed-safety`

All assertions remain design-only, not implemented, not executed, and require future AP approval before implementation or run.

## Harness Safety Controls

Future harness safety controls must include:

- one-run default unless AP approves otherwise;
- no raw stdout or stderr capture;
- no raw log capture;
- no stack traces;
- no scratch output;
- no local paths;
- no `.env` values;
- no host, port, or IP values;
- no DB URLs;
- no database, user, project, or target values;
- no row payloads;
- no auth headers;
- no claim values;
- no provider keys;
- no service-role or private tokens;
- no hosted identifiers;
- no container or image IDs;
- no machine-specific values;
- no root-cause inference;
- no readiness overclaims;
- no local-readiness reopening;
- fail closed if safe sanitized evidence cannot be produced.

## Sanitized Output Contract

Future harness output may be designed only at field-name level unless AP approves otherwise:

- `milestoneName`
- `assertionName`
- `runCount`
- `schemaAvailabilityBucket`
- `rlsHelperBehaviorBucket`
- `artifactSelectIsolationBucket`
- `tenantBoundaryBucket`
- `outputSafetyResult`
- `cleanupResult`
- `failClosedReason`
- `proofBoundaries`

No real values, row data, identifiers, local paths, DB URLs, tenant IDs, auth claims, project refs, target values, host values, port values, IP values, or command strings are included by M5.3a-7.

## Future Prohibited Outputs

Future harness output must not include:

- raw stdout;
- raw stderr;
- raw logs;
- stack traces;
- scratch output;
- local paths;
- `.env` values;
- host values;
- port values;
- IP values;
- DB URLs;
- database names;
- user names;
- project refs;
- target values;
- row payloads;
- auth headers;
- claim values;
- provider keys;
- service-role values;
- private tokens;
- hosted identifiers;
- container IDs;
- image IDs;
- machine-specific values;
- concrete command strings unless separately approved by AP.

## Future Stop Or Fail-Closed Conditions

Future harness work must stop or fail closed if:

- AP approval is absent or ambiguous;
- assertion scope is unclear;
- tenant boundary scope is unclear;
- prerequisites are missing or ambiguous;
- local-readiness loop would reopen;
- DB execution would occur without future AP approval;
- schema inspection would occur without future AP approval;
- RLS tests would run without future AP approval;
- artifact assertions would run without future AP approval;
- hosted validation would occur without future AP approval;
- raw output inspection is needed;
- local path or environment details are needed;
- DB URL, secret, token, provider, service-role, auth, claim, payload, target, container, image, or machine-specific values could be exposed;
- result would require root-cause inference;
- result would imply full schema readiness;
- result would imply full RLS readiness;
- result would imply broad artifact readiness;
- result would imply hosted readiness;
- result would imply production readiness;
- result would imply local startup success;
- more runs are needed than AP approved.

## Decision Paths

M5.3a-7 records these decision paths:

- Path A: Evidence Harness Implementation Approval Gate.
- Path B: Documentation Tracking Reconciliation.
- Path C: Hosted/Nonlocal Evidence Planning.
- Path D: Pause.

## Recommended Path

Path A is recommended only if AP wants to continue toward harness implementation planning.

Path B is recommended if tracking drift is the priority.

## Future Path A Candidate Milestone

`M5.3a-8 RLS and Artifact Evidence Harness Implementation Approval Gate`

M5.3a-8 must not start without AP approval. This closure does not start M5.3a-8.

## Explicit Non-Goals

M5.3a-7 did not:

- add implementation code;
- add tests;
- add package scripts;
- change CI;
- run DB execution;
- run Supabase stack;
- run Docker;
- inspect schema;
- run RLS tests;
- run artifact assertions;
- run migrations or bootstrap;
- perform hosted validation;
- call providers;
- rerun classifier;
- reopen local readiness;
- document or execute concrete commands;
- claim readiness.

## Proof Boundaries

- No root cause is inferred.
- Local DB remains unresolved.
- Schema is not proven.
- Artifact SELECT isolation is not verified.
- Tenant isolation is not newly verified.
- RLS is not proven.
- Hosted readiness is not proven.
- Production readiness is not proven.
- Local startup success is not proven.

## Verification Results

- `npm.cmd run typecheck`: passed.
- `npm.cmd run test`: passed. The generated `.agent/provider-resolver-tests` and `.agent/provider-resolver-integration-tests` output directories were removed with a workspace path guard before final scans and commit.
- `npm.cmd run test:ai-boundary-static`: passed with `15` patterns checked, `734` allowed hits, `0` forbidden hits, and `0` stale allowlist entries.
- `npm.cmd run test:secret-hygiene`: passed with `5` rules checked, `749` allowed classified hits, `0` forbidden hits, and `0` tracked `.env*` files.
- `npm.cmd run build`: passed with a non-fatal Browserslist age warning.
- `npm.cmd audit --audit-level=moderate`: passed with `0` vulnerabilities.
- `git diff --check`: passed.
- `git diff --cached --check`: passed after staging the closure document.
- `supabase --version`: passed; CLI version `2.101.0`.

## Secret Hygiene

Secret hygiene passed. The static scan reported `5` rules checked, `749` allowed classified hits, `0` forbidden hits, and `0` tracked `.env*` files.

## Wording Scan

Repository-wide wording scan completed with `574` existing hits, all outside the new M5.3a-7 closure doc and the focused M5.3a-7 files.

Focused wording scan on the new closure doc and M5.3a-7 files completed with `0` hits.

## Exposure Confirmation

This closure does not include any concrete command string, executable command string, shell command, local path, host value, port value, IP value, DB URL, secret, target value, row payload, auth header, claim value, provider key, service-role value, private token, hosted identifier, container ID, image ID, raw output, scratch output, local config value, or machine-specific value.

No prohibited commands were run during post-merge verification.

## Git Status

Final git status is clean after staging, commit, tag creation, and push.
