# Assess and Studio domain-budget correction — active evidence

## Boundary

Resumed local verification on 2026-09-22. The approved campaign expired at
`2026-09-19T05:07:16.751Z`; no paid retry, expiry extension, replacement campaign,
reset or hosted mutation is part of this resumed local phase. The original
aggregate charge and failed-attempt history remain unchanged.

Implementation base: `ca6a616a659ea678a3b5d9a556d916d96d1b50f2`, existing
PR #264 branch. This is the active implementation evidence, not a new acceptance
claim or a rewrite of earlier failed/closed evidence. The active AI-output plan
owns scope, approval and rollback.

Executed predecessor evidence: all 18 applicable exact-head workflows passed;
the dedicated AI backend's affected command function and 87 source files across
its function graphs were verified. A subsequent real mapping UI attempt failed
before provider execution. Sanitized database readback found two claimed mapping
runs, no matching legacy AI jobs, no token reservations, no analysis debit and no
proposals. The prior validation debit and carried charge remain conservatively
USD 1.3407792. Runtime is disabled; no automatic paid retry occurred.

## Frozen findings and repair

| Finding | Required correction | Current proof |
| --- | --- | --- |
| Mapping run supplied to legacy job-only token RPCs | Real mapping identity union and four dedicated lifecycle RPCs | Executed local SQL/gateway evidence at the pre-review fingerprint below |
| Mapping takeover ignores budget effect history | Fenced recovery that cannot repeat a possible provider effect | Final actual SQL transfer/uncertainty/claim-response recovery assertions pass |
| Studio receipt rejected by Enterprise-only currency ledger | Explicit real-domain currency identities under one shared cap | Executed local SQL/gateway and concurrent shared-cap evidence |
| Prior real-SQL bridge stops before budgets/provider | Complete production mapping and Studio chains with mocked provider egress | Fourteen explicit final pipeline assertion markers passed, not inferred from suite exit |
| Grounded proposal decoder omits the SQL-required proposal version | Emit the initial immutable proposal version and test the exact staged shape | Corrected; actual SQL stage/commit passed |
| Two new RPC identifiers exceed PostgreSQL's 63-byte identifier limit | Shorten SQL/runtime names together and assert the exact deployed function inventory | Exact installed-name/ACL and static length regressions passed |
| Studio completed replay omits token/fence fields required by its TypeScript claim decoder | Decode the real completed replay contract without inventing ownership or retrying the provider | Actual completed/stale-completed response-loss replay passes, including mutable-material changes |
| Reservation replay accepts changed token limits | Compare immutable input/output limits across mapping, Enterprise and Studio | Otherwise-valid positive replay before/after both mutations passed for all three domains |
| Lost mapping transfer-claim response strands an unconsumed pending grant | Recover exact same/newer-fence pending ownership before atomic one-use budget grant | Corrected; actual SQL marker 013 and final independent reviews pass |
| Terminal Studio replay reloads mutable template/provider material | Recover exact durable finalization without loading generation material or granting provider authority | Corrected; actual SQL marker 014 and final independent reviews pass |
| New adapter/recovery tests are not mandatory measured coverage | Bind both exact measured commands into canonical feature/regression CI | All three source measurements are mandatory; omission/substitution adversaries and final measured gates pass |

Architecture, quality and security read-only reviews closed before implementation.
No externally exploitable authorization bypass was demonstrated. Review conclusions
do not establish deployed correction or general security certification.

## Pre-review frozen execution (2026-09-22)

Source fingerprint `8037b5737d946ba529ddca5aac1eebc6e40274fc1cab12c32ba5d6b6da1fa8b0`
passed all 25 canonical commands: feature 10, PostgreSQL 4, regression 2,
browser 1 and static 8. Respective retained manifests are under
`output/assess-import/validation/`:

- `feature-9b3343f8-1354-4d24-9a8b-b3407e65b084/manifest.json`
- `postgres-1434e151-2371-4af9-8555-da5ecd6e435b/manifest.json`
- `regression-4ed0e50a-9f07-449c-900e-7ebf534099e9/manifest.json`
- `browser-ae3ab54d-a014-4b2f-a3de-028bd417ebee/manifest.json`
- `static-927d1796-08db-4a43-a5e6-662a1f703fff/manifest.json`

The browser report records 32 expected passes, no skipped/unexpected/flaky cases,
on Desktop and Pixel. The pipeline emitted twelve exact positive/adversarial
assertion results. Full fresh/populated PostgreSQL validation passed 16 report
entries at `output/creation-access/postgres-94b2fad6-6d78-4f93-bf05-6c8378c8bd3b/result.json`:
81 migrations, process 54, Admin 109, current/predecessor campaign 159 each,
19 preserved debits, five rejected predecessor mutations, reapply rejection,
exact names/ACL/RLS, retained suites, verified owned-container cleanup.
Native PostgreSQL 16.15 recovery passed six checks at
`output/testing/domain-budget-20260922/pilot-recovery-a307a836-960c-4648-983a-d23166c9c45a/artifacts/pilot-operations/postgres-recovery.json`.
The outer wrapper reported a scalar-string/array cleanup-check error after those
passes; the controller independently checked the exact source-reading junction
target and removed only that junction. Evidence and source remained intact.
Retained PR C evidence contracts passed 64 tests and validated declarations for
81 commands/221 assertion markers; this is not execution of all 81 commands.

Final independent architecture, quality and security reviews nevertheless found
the two recovery defects and mandatory-coverage gap above. These passes do not
prove those omitted scenarios or the subsequently corrected source. All reviewers
closed before renewed implementation. Dedicated workers own mapping SQL, Studio
terminal runtime/tests, and actual SQL pipeline regressions; the controller owns
coverage binding, integration, documentation, reruns and acceptance.

The security-diff skill completed scan
`c6acba27-c6e7-4ff5-85f5-3c9bde234156` on the unchanged pre-review patch. Eighteen
intended changed source files were reviewed; 91 unrelated generated files were
excluded. No technically plausible security candidate was identified. The formal
report conservatively retains partial coverage because the draft publisher kept
nine obsolete pending-file entries after completed review submissions. This
publication limitation is disclosed, not removed from the sealed report. Neither
that report nor its zero findings certify security or the upcoming corrections.

Direct diagnostic coverage passed existing 90/80/85 thresholds for the mapping
budget adapter (100/87.50/100) and Studio database adapter (94.07/89.93/89.83).
Those exact measured commands are now mandatory, with omission/substitution
negatives; the updated contract suite passes 18 tests. Final changed-source
coverage and the full frozen command groups must be rerun after both repairs.

The recovery implementation now also requires measured
`studioArtifactGeneration.ts` coverage. Focused post-fix Studio database coverage
passed at 94.24/90.20/89.83 and generation coverage at 96.74/93.33/92.00.
The final combined command groups remain pending until integration freezes.
The final focused Studio run measured generation at 96.77/93.33/92.00;
application/Edge typechecks and the retained Studio artifacts suite also passed.

The frozen mapping-recovery migration passed the complete creation-access suite
again at `output/creation-access/postgres-84945d64-1060-4d66-b223-a9c26cd03d65/result.json`:
16 passed entries, 81 migrations and verified owned-container cleanup. This
includes fresh/populated/ACL/RLS/currency-preservation and retained checks listed
above, without rewriting the earlier attempts.

Post-repair native PostgreSQL 16.15 recovery passed all six exact assertions with
successful bounded wrapper cleanup at
`output/testing/domain-budget-20260922/pilot-recovery-e7f75218-ea99-4f15-8f2e-949c44055727/artifacts/pilot-operations/postgres-recovery.json`.
The wrapper now normalizes the junction target to an array before validating and
unlinking the exact task-owned source-reading junction; production source and
the unrelated recovery-script line-ending state were not edited.

The first extended pipeline attempt stopped after markers 001–005 because a new
test wrongly expected the claim RPC to reject an altered `p_catalog_hash` hint.
The accepted claim contract derives its canonical hash from the actual targets;
that supplied hint is deliberately nonauthoritative. This is an invalid test
oracle, not a new product defect. Preserve that failed attempt; add a positive
control proving the hint cannot change canonical identity, and mutate actual
catalog/target/source bindings for the negative. No production guard is relaxed.

A subsequent extended run correctly performed the authorized higher-fence rebind
but failed a harness expectation that the entire reservation row remain unchanged.
Only token/fence/updated timestamp may change there; count, pending status,
original transfer timestamp and budget identity must remain exact. The test now
captures the valid rebind before asserting that rejected substitutions cause no
further mutation. A following run passed 001–005, 013, 006 and 007, then correctly
rejected a fixture's direct system-template mutation with `STUDIO_IMMUTABLE`.
The template-replacement scenario must use the existing canonical tenant-template
create/review/approve/replace lifecycle with distinct synthetic participants.
These remain failed test attempts, not product defects or accepted pipeline proof.
Further fixture-only iterations preserved an intentionally uncertain BRD attempt
instead of reusing its one-active-attempt slot, captured the legitimate aggregate
increment made by the generation request before comparing stale finalization,
and paired the downstream FRD system-template ID with `templateKind: system`
instead of inheriting the earlier tenant-template kind. Both new recovery markers
passed before that last downstream selector failure. No failed run is promoted to
a complete pipeline pass; all corresponding disposable databases were cleaned.

## Executed local checks

- `node --test scripts/assessImportValidationContract.test.mjs scripts/prCMigrationTailContract.test.mjs`:
  15 tests passed, zero failures/skips. This proves the canonical mandatory pipeline
  command binding and approved migration-tail checks, not the pipeline itself.
- `node --check scripts/runCreationAccessPostgres.mjs` and
  `node --check scripts/testSyntheticAiCampaignPostgres.mjs`: passed before the
  later upgrade-test additions; final source verification remains required.
- The resumed 2026-09-22 canonical command/migration-tail contract rerun passed
  17/17 tests after adding RPC-name truncation and mandatory Studio recovery
  regressions; local synthetic
  campaign budget/fixture/oracle tests passed 18/18.
  These focused results do not replace the pending frozen-source full gates.
- `node scripts/runCreationAccessPostgres.mjs` passed at
  `output/creation-access/postgres-c65a1e38-646d-4168-9a51-9a82e91a0a0e/result.json`:
  all 81 migrations, 54 process assertions, 109 synthetic Admin assertions,
  current and predecessor campaign suites (159 assertions each), populated
  80-to-81 upgrade preserving all 19 currency debits, token records and campaign
  settings, exact function-name/ACL/forced-RLS checks, five rejected predecessor
  mutations, atomic reapply rejection, retained Pilot Operations and Delivery/
  Monitor PR C suites. All 16 report entries passed; owned-container cleanup was
  verified. This is local synthetic database proof, not hosted/paid acceptance.
  The full suite passed again with the Studio terminal-replay SQL correction at
  `output/creation-access/postgres-e91607a9-e2f2-4b4b-9e02-08486a3a4a7e/result.json`;
  its migration digest matches the current source, including function ACL checks
  for the dynamically forwarded bootstrap and Studio claim functions.
- Retained native PostgreSQL 16.15 backup/restore passed all six assertion results
  using separate disposable databases: clean restore, corrupt/incomplete backup
  rejection, wrong backup schema version, interrupted restore retry and canonical
  response-loss receipt. New evidence is isolated at
  `output/testing/domain-budget-20260922/pilot-recovery-80f7fc73-bf36-4b73-b2c0-8dbd17305344/artifacts/pilot-operations/postgres-recovery.json`;
  previous recovery artifacts were not overwritten. Only task-owned temporary
  databases, dump files and the source-reading junction were removed.
  The same six checks passed again after the Studio SQL correction, isolated at
  `output/testing/domain-budget-20260922/pilot-recovery-cac31585-0553-4855-b47a-2193b955e8b2/artifacts/pilot-operations/postgres-recovery.json`;
  its temporary resources were also cleaned without touching prior evidence.
- `npm audit --audit-level=moderate --json` completed with zero reported
  vulnerabilities across 206 dependencies on 2026-09-22. This is dependency
  advisory coverage, not general application security certification.

## Final frozen-source execution and review (2026-09-22)

Source fingerprint
`4946c79a7d0d779e17d68d617b63fffee07a870602b1c0218d71b2771c1b689f`
passed all 25 canonical commands. The controller independently rehashed all 1,106
bound source files and checked each command against canonical execution source;
all results have exit 0, stable source, and no timeout, truncation or spawn failure.
Final manifests under `output/assess-import/validation/`:

- `feature-8705bf37-26ed-4034-a632-70dc22f8507d/manifest.json`: 10/10.
- `postgres-b2c797c6-77b0-4e7b-9efc-acd0c2c1bf13/manifest.json`: 4/4.
- `regression-88325bf3-a7eb-48ed-8081-d698b2d9e9f8/manifest.json`: 2/2.
- `browser-1428a1e0-df25-40f0-8a3a-d7aa9e503353/manifest.json`: 1/1.
- `static-c2234e47-218d-453f-b176-a067b15d223c/manifest.json`: 8/8.

Actual Playwright results at
`output/playwright/assess-import/e6c1e2361c1c13e2a4e88ecd/results.json`
record 32 expected passes, zero skipped, unexpected or flaky cases. The earlier
final-source attempt `browser-1529a95e-db22-4e61-9f6e-d1b5195a6060` remains failed:
all 32 launches hit sandbox `spawn EPERM` before application assertions. Its
Playwright report `c2b7de31fa9617fcce3cada8` is retained. The identical command
passed with browser-launch permission; no source or assertion was changed.

The final full-chain PostgreSQL manifest and six native recovery assertions are
recorded above. Native recovery has null head/run identifiers and therefore proves
disposable local recovery only, not exact-head CI or hosted execution. Final extra
checks passed: 64 PR C evidence-contract tests, 18 synthetic campaign tests,
10 server campaign checks, Studio template/provider/generation contracts and
zero dependency-audit vulnerabilities. The 81-command/221-marker PR C registry
is validated declaration/provenance, not a claim of full local matrix execution.
All disposable databases were removed. The remaining task-owned PostgreSQL
container was independently checked empty, exact-ID/task-label bound and RAM-only,
then stopped and removed; images, backups and unrelated workloads were preserved.

Independent final architecture, quality and security reviews all closed with no
remaining blocker in this correction. Security-diff scan
`424b195e-3561-4b1c-a391-7418aaf9a0dd` was successfully sealed on snapshot
`e013165b58989bc9504c0a657725cfcc2e2633913eb64fc587ca180b6a0ac88d`:
19 intended source files reviewed, 91 unrelated generated files explicitly
excluded, complete bounded coverage, zero deferred candidates and zero findings.
The earlier partial scan remains immutable. The final scan reviews the frozen
runtime/test source, not the subsequent documentation/provenance reconciliation.
This is neither a full-repository audit nor security certification.

The corrected real-SQL pipeline is now frozen and passes all fourteen explicit
markers, ordered 001–005, 013, 006–010, 014, 011–012. Script SHA-256:
`f0f3ca89c739f33abc559b084d1ea4a4fffdfffcbe8e5dd480d248b875b01ce5`.
Marker 013 covers committed claim-response loss, same/newer-fence recovery,
authoritative binding/currentness/authorization negatives, one concurrent grant,
old/cleared fence denial and unchanged budget identity. Marker 014 covers both
completed and stale-completed finalizer-response loss, canonical tenant-template
replacement, disabled provider/read-only controls and exact durable replay with
no material, budget, stage, failure, secret or provider activity. The whole run
asserted zero default network/environment reads; post-run inspection found zero
owned pipeline databases. Provider secrets and HTTP remain mocked.

All nine failed fixture/oracle iterations remain failures: nonauthoritative hash
hint, two overly narrow expected-error patterns, valid fence-rebind snapshot,
immutable system template, invalid provider status (`inactive` rather than
schema-canonical `disabled`), occupied uncertain-attempt slot, legitimate
request-time aggregate increment, and mixed tenant/system template selector.
Each owned database was cleaned; no runtime guard or campaign charge was reset.

## Remaining gates

New-head CI, including the full retained PR C matrix, follows this locally clean
same-branch commit/push. Hosted retry is not authorized under the expired campaign.
No green suite exit may stand in for an unexecuted assertion. Actual numeric
performance profiling, an additional accessibility audit, complete field-by-field
hosted acceptance, document approval/handoff, controlled-human acceptance, merge
and production are not proven. No new UI is introduced by this correction.

## Retained operational attempts and rollback

Initial non-frozen PostgreSQL run
`output/creation-access/postgres-503ed402-0a7a-4c02-aa5d-bf44b87dfbbe/result.json`
applied all 81 migrations, passed 54 process and 109 synthetic Admin assertions,
then failed campaign bootstrap with `SYNTHETIC_AI_CAMPAIGN_TARGET_NOT_EMPTY`.
The old bootstrap pins the previous migration tip. This is a confirmed forward
integration dependency, not a target that should be reset. The migration must
update that exact guard without relaxing emptiness or campaign immutability.
The failed attempt is retained; its owned RAM-backed container was removed.

The next non-frozen run
`output/creation-access/postgres-2dabe8b7-60b3-483a-ba26-eb9553bdf8aa/result.json`
again applied 81 migrations and passed 54 process and 109 Admin assertions.
The bootstrap fix passed; the retained campaign suite then failed after 21
assertions because its legacy extraction fixture used an effect identity without
a corresponding running job and token reservation. The stricter domain guard
correctly rejected it. Repair the legacy test fixture with real matching legacy
authority, not a fake mapping job or relaxed production guard. Cleanup passed.

Further non-frozen local attempts retain their complete sanitized manifests under
`output/creation-access/`: `postgres-5c5cb97b-a180-454b-bc57-e532c7a35211`
stopped on an old stale-authorization error expectation;
`postgres-a6b4a33e-f9ee-4fbf-8ecf-8f2138f3de0b` and
`postgres-23ee87bd-1c7a-40af-887e-40d6c08af8c1` stopped because negative fixtures
held real token reservations and reached the daily request cap before the intended
currency-cap race. The known no-effect bypass fixture now releases its reservation
through the canonical RPC, without changing any limit or currency debit.
`postgres-9fad9410-20ad-483b-8216-290ae6405db0` passed both current and predecessor
campaign suites (159 assertions each, including the genuine two-client final-slot
race) but stopped in the populated-upgrade assertions. All these owned-container
cleanups passed; the full upgrade remains under investigation, not PASS.

The diagnostic rerun `postgres-35a03cc8-a8c9-48c8-9c22-d22cf9852a48` isolated
that assertion to the exact function inventory: PostgreSQL silently truncated two
new identifiers. SQL and runtime now share shorter names, with a 63-byte regression
guard. The subsequent passing run is recorded above; earlier attempts remain
failed evidence and were not rewritten.

The controller rejected initial adversarial pipeline coverage because mutations
were applied to an already-completed mapping request, which could deny for an
unrelated reason. The strengthened test uses a fresh live claimed run with real
token/currency reservations, checks valid replay before and after mutations, and
checks unchanged provider counters. It found
`ADVERSARIAL_MUTATION_ACCEPTED_maximum-output`: the new token reserve RPC omitted
immutable request-limit comparisons on replay. The runtime fix and full rerun are
required; the earlier green pipeline is not evidence for this boundary. The same
strengthening adds first settlement after authorization revocation and actual
committed-settlement response loss, rather than claiming those from transition
replay alone.

The replay-limit repair also covers the same shared-ledger gap in the accepted
Enterprise and Studio reservation functions via body-only forward changes. Fresh
application attempt `postgres-43717447-44dd-4f4f-a293-80da91f8f60e` rejected a
newline-sensitive predecessor match with `STUDIO_BUDGET_RESERVATION_REPLAY_DRIFT`.
Its owned container was cleaned. The exact occurrence guard now matches semantic
tokens across CRLF/LF; a following integration attempt also caught and corrected
an overstrict postcondition whose old token intentionally remains nested inside
the replacement. Failed attempts remain failures. A later direct-SQL adversary
initially expected an exception even though the canonical function returns a safe
`PROVIDER_ROUTE_STALE` error envelope; that test must assert the exact envelope
and unchanged reservation rather than change the production failure contract.

The first disposable PostgreSQL launch failed before database creation because the
local Docker logging driver rejected compression with one retained log file. Only
that empty, task-labelled container was removed; the replacement uses compression
disabled, a cached pinned PostgreSQL 16 image, loopback networking and RAM-backed
data. Unrelated Docker projects and persistent data were untouched.

Rollback is provider-disabled/read-only retention of receipts, runs, attempts,
budgets, debits and failed evidence. Never drop populated ledgers, reset the cap,
refund ambiguous effects, extend expiry or fabricate a legacy job/receipt.
