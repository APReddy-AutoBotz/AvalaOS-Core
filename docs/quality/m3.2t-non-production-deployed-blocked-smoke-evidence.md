# M3.2t Non-Production Deployed Blocked-Smoke Evidence

## Repo And Remote Safety Confirmation

- Repo: `APReddy-AutoBotz/AvalaOS-Core`
- Branch: `milestone/m3.2t-non-production-deployed-blocked-smoke`
- Historical prototype repository is not used.
- `.env` is not tracked by git.
- No Supabase URL, Supabase key, provider key, database URL, auth token, auth header, or service-role value is printed, stored, committed, or added to evidence.

## Files Changed

Planned and actual files changed:

- `docs/planning/m3.2t-non-production-deployed-blocked-smoke.md`
- `docs/quality/m3.2t-non-production-deployed-blocked-smoke-evidence.md`

Smoke script:

- Not added. M3.2t stopped before seed/auth/invoke because the deployed functions were not reachable and deployment could not safely proceed with the incomplete local Supabase CLI.

No runtime, schema, RLS, migration, Edge Function entrypoint, `_shared/ai.ts`, provider resolver/integration, Admin UI, product UI, Health, scoring, browser fallback, prompt-version, package dependency, usage, or cost file was changed.

## Non-Production Target Confirmation

No secret values, raw URLs, or project ref values were printed.

Sanitized preflight result:

| Item | Result |
| --- | --- |
| `SUPABASE_URL` | Process-visible without printing value. |
| `SUPABASE_ANON_KEY` | Process-visible without printing value. |
| `SUPABASE_SERVICE_ROLE_KEY` | Process-visible without printing value. |
| `AVALA_AI_RUNTIME_MODE` | Process-visible without printing value. |
| Runtime classification | `pilot`. |
| Supabase target host class | `supabase-host`. |
| Project-ref shape | `standard-20-lower-alnum`. |
| Disallowed target markers | Absent. |
| Non-production target confirmation | Passed. |

Stop condition was not target safety. The stop condition was deployed function unavailability plus unsafe deploy tooling.

## Edge Function Availability And Deploy Status

Sanitized `OPTIONS` reachability checks:

| Edge Function | Result |
| --- | --- |
| `ai-generate-document` | `404`; not deployed/reachable. |
| `ai-refine-section` | `404`; not deployed/reachable. |
| `ai-provider-test-connection` | `404`; not deployed/reachable. |

Deploy status:

- Deploy was required because all three target functions returned `404`.
- Deploy was attempted only against the sanitized non-production project ref derived from `SUPABASE_URL` without printing it.
- Deploy did not complete.
- The local Supabase CLI shim could not find the co-located `supabase-go` binary.
- No Edge Function was deployed by M3.2t.

Outcome impact:

- M3.2t stopped before seed, auth user creation, deployed invocation, blocked scenario execution, audit writes, or cleanup deletes.

## Seed Summary

- Seed was not run.
- No M3.2t smoke auth user was created.
- No M3.2t profile, organization, role, membership, key-reference, config, policy, audit, AI job, or usage row was created.
- No customer data was used.
- No real provider key or raw provider payload was inserted.
- No raw prompt or completion body was inserted.

## Auth Token Handling Summary

- No smoke auth user was created.
- No auth token was created.
- No auth token was printed.
- No auth token was stored.
- No auth header was printed, stored, committed, or added to evidence.

## Blocked Scenario Results

Blocked deployed scenarios were not executed because M3.2t stopped at Edge Function availability/deploy safety.

Planned blocked scenarios remain unexecuted:

| Scenario | Planned expected failure | Result |
| --- | --- | --- |
| Missing policy | `provider_policy_missing` | Not run; functions unavailable and deploy blocked. |
| Disabled config | `provider_config_ineligible` | Not run; functions unavailable and deploy blocked. |
| `manual_placeholder` key reference | `key_reference_ineligible` | Not run; functions unavailable and deploy blocked. |
| Wrong provider-prefix smoke reference | `secret_reference_unsafe` | Not run; functions unavailable and deploy blocked. |
| Missing/invalid runtime mode | `mode_not_allowed` | Deferred; no safe reversible deployed runtime toggle was available and functions were not deployed. |

## Deferred Scenario Notes

- Runtime-mode mutation was not attempted.
- Missing/invalid runtime mode remains deferred, not failed.
- All functional blocked scenarios remain unexecuted because M3.2t reached an Outcome C stop condition before seed/invoke.

## Audit Sanitization Summary

- No deployed blocked invocation ran.
- No M3.2t audit rows were created.
- No audit row dumps were produced.
- No raw prompt, completion, provider payload, provider key, auth header, service-role key, `DATABASE_URL`, raw secret reference, stack trace, or raw database/PostgREST error was recorded.

## Cleanup Summary

- Cleanup delete was not required because seed did not run.
- No M3.2t smoke rows were created.
- No unrelated rows were deleted.
- Generated local test-output folders were removed before the final static scan.

## Provider And Secret Safety Confirmation

M3.2t did not:

- Run allowed live provider smoke.
- Call Gemini or Groq APIs.
- Use real provider keys.
- Read provider secret environment variables.
- Print provider keys.
- Store provider keys.
- Print or store Supabase keys.
- Print or store `SUPABASE_URL`.
- Print or store `DATABASE_URL`.
- Store secrets in files, docs, logs, git, evidence, or output.
- Commit `.env`.

## Verification Results

| Command | Result | Notes |
| --- | --- | --- |
| Sanitized env preflight | Passed | Required variables were process-visible without values; runtime classified as `pilot`; non-production target confirmation passed. |
| Edge Function `OPTIONS` reachability | Blocked | All three target functions returned `404`, so deployed functions were not reachable. |
| Edge Function deploy attempt | Blocked before deployment | Supabase CLI shim could not find `supabase-go`; no deploy completed. |
| M3.2t smoke command | Not added / not run | Stopped before script creation because functions were unavailable and deploy tooling was incomplete. |
| `npm run typecheck` | Passed | `tsc --noEmit` completed successfully. |
| `npm run test` | Passed after sandbox rerun | Aggregate suite passed after rerun outside the Windows sandbox ACL failure. |
| `npm run test:provider-resolver` | Passed after sandbox rerun | M3.2l provider resolver decision regression suite passed. |
| `npm run test:provider-resolver-integration` | Passed after sandbox rerun | M3.2n resolver Edge Function integration regression suite passed. |
| `npm run test:m3.2p-smoke` | Passed | Existing mocked smoke passed with non-production guard, blocked and allowed mocked scenarios, 9 sanitized smoke audit events, transaction rollback cleanup, and no live provider call. |
| `npm run test:ai-boundary` | Passed after sandbox rerun | AI mode boundary regression suite passed. |
| `npm run build` | Passed | Vite production build completed successfully with 178 modules transformed. |
| `npm audit --audit-level=moderate` | Passed | Reported `found 0 vulnerabilities`. |
| `npm run test:ai-boundary-static` | Passed after cleanup | Final scan passed with 15 patterns checked, 675 allowed hits, 0 forbidden hits, and 0 stale allowlist entries. |
| `git diff --check` | Passed | No whitespace errors or conflict markers. |
| Wording scan | Passed after review | Hits remain limited to historical migration references, active repository identifiers, Health separation notes, no-go/control examples, prior planning/evidence command references, and this milestone evidence file's repo identifier plus recorded scan command. |

## Static Scan Summary

Final `npm run test:ai-boundary-static` result after generated `.agent` cleanup:

- Patterns checked: 15.
- Allowed hits: 675.
- Forbidden hits: 0.
- Stale allowlist entries: 0.
- Allowed hits remain classified as control documentation, server-side Edge/Supabase environment references, local-demo/internal-dev fallback references, deprecated cleanup-only references, non-secret app storage state, or scan pattern declarations.
- M3.2t introduced no forbidden browser-side AI, provider-key, or runtime-provider boundary hit.

## Wording Scan Review

Command:

```powershell
rg -n "KlarityPM|Klarity PM|Klarity|AutoBotz|AI Delivery Brain|HIPAA compliant|SOC 2 compliant|GDPR compliant|GxP compliant|autonomous decision|agent decides|AI decides score"
```

Expected review categories:

- Historical migration references.
- Active repository identifier references.
- Health separation notes.
- No-go/control examples.
- Prior planning/evidence command references.
- This milestone's repo identifier and recorded scan command.

Review result:

- Passed after review.
- No unsupported compliance claim was introduced.
- No product copy was introduced implying AI decides scores, approvals, risk gates, recommendations, compliance status, or regulated decisions.

## Outcome Recommendation

Outcome C - blocked.

Reason:

- Sanitized non-production target preflight passed.
- All three deployed target functions returned `404`.
- Deploy was required but did not complete because the local Supabase CLI shim could not find `supabase-go`.
- M3.2t stopped before seed, auth, deployed invocation, audit writes, cleanup deletes, or provider calls.

## Risks Carried Forward

- The three target Edge Functions remain not deployed/reachable in the confirmed non-production target.
- The local Supabase CLI installation is incomplete.
- Deployed blocked scenarios remain unexecuted.
- Deployed runtime-mode failure behavior remains unproven.
- Safe deployed allowed provider-handoff smoke remains blocked without AP approval for live provider use or a controlled mock strategy.

## Next Milestone Recommendation

Run an AP/ops readiness milestone before retrying deployed smoke:

- Restore a complete Supabase CLI installation with `supabase-go`.
- Reconfirm the same non-production target without printing values.
- Deploy only the three existing Edge Function directories.
- Re-run M3.2t from sanitized preflight.
