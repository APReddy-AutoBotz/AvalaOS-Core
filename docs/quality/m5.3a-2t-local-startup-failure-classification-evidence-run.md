# M5.3a-2t Local Startup Failure Classification Evidence Run

## Evidence Run Summary

M5.3a-2t performed the AP-approved local startup failure classification evidence run.

This milestone added only this evidence document:

- `docs/quality/m5.3a-2t-local-startup-failure-classification-evidence-run.md`

The evidence run used the existing classifier only. No code, script, package, SQL, migration, policy, assertion, harness, runtime, UI, CI, provider, deployment, export, scoring, demo, Reports, Teams, Admin, or Health file was changed.

## Prior Closure Baseline

- Previous milestone: M5.3a-2s Local Startup Failure Classification Evidence Run Approval Gate
- Previous PR: `#120`
- Previous accepted commit: `9f57154efd5e3c17a8b850977fa84f9abee0e616`
- Previous merge commit: `6a84f1374cde30b3949dfd57360d67acf5ef492b`
- Previous post-merge verification commit and tag target: `08f0f58ef4ff3d09dde5a1ba52075941fae2e791`
- Previous tag: `avalaos-core-m5.3a-2s-local-startup-failure-classification-evidence-run-approval-gate`

## AP Approval Scope

AP approved exactly one local-only classifier evidence run:

- `node scripts/m5.3a-2k-local-stack-startup-failure-classifier.mjs --classify`

The classifier was permitted to perform at most one local-only startup attempt internally. No standalone startup command was run outside the classifier.

The evidence run remained inside the approved scope:

- Classifier command executed exactly once.
- Startup attempt occurred only inside the classifier.
- No Docker command was run.
- No DB command or DB connection occurred.
- No schema bootstrap or schema readiness command occurred.
- No RLS harness was run.
- No artifact assertion was run.
- No migration or bootstrap command was run.
- No remote, personal, or hosted validation was used.
- No provider call occurred.
- No `.env`, ignored secret file, generated log, scratch log, raw output, or local machine path was inspected manually.

## Classifier Execution Result

- Command label: classifier evidence run.
- Command executed: `node scripts/m5.3a-2k-local-stack-startup-failure-classifier.mjs --classify`
- Classifier run count: exactly one.
- Startup attempt status: `attempted`.
- Sanitized classification: `local-startup-timeout-with-oversized-output`.
- Confidence: `low`.
- Output safety result: `passed`.
- Scratch cleanup result: `removed`.
- Root cause inferred: `no`.

The sanitized classification is a redacted signal category only. It does not identify root cause and does not prove any specific Docker, stack, config, port, image, resource, migration, or environment cause.

## Redacted Signal Fields

The classifier emitted only approved redacted fields:

- `startupAttemptStatus`: `attempted`
- `exitResultClass`: `timeout`
- `commandPhaseClass`: `unknown`
- `outputSizeBucket`: `large`
- `lineCountBucket`: `oversized`
- `timeoutFlag`: `true`
- `knownPatternFamilyCounts`: `docker=0,supabase-cli=0,network=0,health=0,permission=0,config=0,resource=0,unknown=1`
- `sanitizedCategoryCandidates`: `local-startup-timeout-with-oversized-output`
- `confidence`: `low`
- `outputSafetyResult`: `passed`
- `scratchCleanupResult`: `removed`
- `safetyBlockReasonCategory`: `output-too-large`
- `noSecretsConfirmation`: `passed`
- `noLocalPathConfirmation`: `passed`
- `noRawLogConfirmation`: `passed`

No raw stdout, raw stderr, raw logs, raw error lines, stack traces, local paths, host values, port values, IP values, DB URLs, database names, user names, project refs, target values, container IDs, image IDs, volume names, network names, service-role values, anon keys, JWT secrets, provider keys, tokens, `.env` values, local config values, row payloads, auth headers, claim values, personal identifiers, or hosted identifiers were emitted into evidence.

## Stop Condition Status

No safety stop condition prevented the redacted evidence result.

- Output safety passed.
- Scratch cleanup completed with `removed`.
- No-secrets confirmation passed.
- No-local-path confirmation passed.
- No-raw-log confirmation passed.
- Evidence did not require raw output inspection.
- Evidence did not require local environment inspection.

After the approved classifier run completed, no additional classifier, startup, Supabase stack, Docker, DB, schema, RLS, artifact, migration, bootstrap, remote validation, or provider command was run.

## Proof Boundaries

- Local DB availability remains unresolved.
- Schema availability remains not proven.
- Artifact SELECT isolation remains not verified.
- Tenant isolation is not newly verified.
- Hosted-pilot readiness is not claimed.
- Production readiness is not claimed.
- Root cause is not inferred.

The evidence proves only that the approved classifier executed once and returned the recorded sanitized category with output safety passed and scratch cleanup removed.

## Verification Results

- `node --check scripts/m5.3a-2k-local-stack-startup-failure-classifier.mjs`: passed.
- `npm run typecheck`: passed.
- `npm run test`: passed; generated provider resolver folders were removed before final scans.
- `npm run test:ai-boundary`: passed.
- `npm run test:ai-boundary-static`: passed with 15 patterns checked, 734 allowed hits, 0 forbidden hits, and 0 stale allowlist entries.
- `npm run test:secret-hygiene`: passed with 5 rules checked, 749 allowed classified hits, 0 forbidden hits, and 0 tracked `.env*` files.
- `npm run build`: passed.
- `npm audit --audit-level=moderate`: passed with 0 vulnerabilities.
- `supabase --version`: passed with CLI version `2.101.0`.

Final diff checks and restricted wording scans were run after this evidence document was added.

## Secret Hygiene Result

Secret hygiene passed with 5 rules checked, 749 allowed classified hits, 0 forbidden hits, and 0 tracked `.env*` files.

## Wording Scan Result

- Repository-wide restricted wording scan returned 574 existing historical/control hits.
- Focused restricted wording scan on this evidence document returned no hits.

## Exposure Confirmation

No secrets, target values, payloads, `.env` values, hosted identifiers, container IDs, image IDs, raw logs, local config values, local paths, DB URLs, host values, port values, database names, user names, service-role values, provider keys, private tokens, fixture IDs, row payloads, auth headers, or claim values were printed, summarized, copied into evidence, exposed, or committed.

Raw startup output was handled only inside the approved classifier scratch lifecycle to derive redacted metadata, and the classifier reported scratch cleanup as `removed`.

## Clean Git Status

Final git status was clean after staging, commit, push, and draft PR creation.

## Recommended Next Milestone

Recommended next milestone: M5.3a-2u Local Startup Timeout With Oversized Output Remediation Plan, only after AP approval.
