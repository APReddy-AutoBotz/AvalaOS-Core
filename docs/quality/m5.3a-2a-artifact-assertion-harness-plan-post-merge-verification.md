# M5.3a-2a Artifact Assertion Harness Plan Post-Merge Verification

## Milestone

- Milestone: `M5.3a-2a Artifact Assertion Harness Plan`
- PR: `#100`
- Accepted branch: `milestone/m5.3a-2a-artifact-assertion-harness-plan`
- Accepted commit SHA: `615c5b4ca5c3658e7b6a06021748a51c7c06babd`
- Merge commit SHA: `01e5c35d942a7eaecfd814f9e0cec358e791f870`
- Current main HEAD before this closure evidence: `01e5c35d942a7eaecfd814f9e0cec358e791f870`
- Post-merge verification commit SHA: this evidence commit; the exact SHA is the tag target and final closure response.
- Tag name: `avalaos-core-m5.3a-2a-artifact-assertion-harness-plan`
- Tag target SHA: this evidence commit; the exact SHA is recorded by the pushed tag and final closure response.
- Recommended next milestone: `M5.3a-2a Artifact Assertion Harness Implementation`, only after AP accepts this plan closure.

## Files Verified

- `docs/planning/m5.3a-2a-artifact-assertion-harness-plan.md`
- `docs/security/m5.3a-2a-artifact-harness-target-safety.md`
- `docs/architecture/m5.3a-2a-artifact-assertion-runner-architecture.md`
- `docs/quality/m5.3a-2a-artifact-assertion-harness-planning-evidence.md`

## Files Changed

- `docs/quality/m5.3a-2a-artifact-assertion-harness-plan-post-merge-verification.md`

## Docs-Only Scope Confirmation

M5.3a-2a merged exactly four planning/evidence docs.

Confirmed:

- This milestone is docs-only.
- No artifact runner was implemented.
- No scripts were created.
- No `package.json` changes were made.
- No package scripts were added.
- No package dependencies were changed.
- No SQL migrations were changed.
- No applied schema migrations were altered.
- No assertion SQL was changed.
- No M5.3a-1 harness behavior was changed.
- No M5.3a-1 helpers or policies were changed.
- No M5.3a-2 artifact policies were changed.
- No local Supabase config file was created or changed.
- No runtime adapters, UI, routing, view keys, Sidebar behavior, demo data, scoring, provider execution, export behavior, deployment config, CI workflows, Reports/Teams/Admin, or Health repository files were changed.
- No database connection was made.
- No remote Supabase validation was run.
- `scripts/applySupabaseMigrations.mjs` was not run.
- `npm run db:migrate` was not run.
- `npm run test:rls:assess` was not run.
- No unguarded database-target command was run.
- No provider call was made.
- No environment values or secrets were read, printed, summarized, copied, exposed, or committed.
- Artifact SELECT isolation is not verified.
- Hosted-pilot readiness and production readiness are not claimed.

## M5.3a-2 Closure Baseline

M5.3a-2 is closed with tag target `982235876b78a72bf1a2082e73320b3e467e6207`.

Accepted baseline carried into M5.3a-2a:

- Artifact SELECT policies exist on `main` for `assess_processes`, `assessments`, `assessment_review_events`, `projects`, `document_generations`, and `delivery_work_items`.
- Artifact assertion SQL exists at `docs/schema/m5.3a_2_artifact_select_isolation_assertions.sql`.
- No AP-approved artifact assertion runner exists.
- The M5.3a-1 harness remains scoped to identity and membership assertions.
- Artifact SELECT isolation is not verified because executable local artifact assertions have not run and passed against an AP-approved reachable local database.

## Future Runner Strategy Confirmation

The merged plan recommends a separate artifact runner first:

- Future runner: `scripts/m5.3a-2-artifact-isolation-smoke.mjs`.
- Optional package script: `test:rls:m5.3a-2`, only if AP approves package-script impact.
- Future evidence: `docs/quality/m5.3a-2a-artifact-assertion-harness-implementation-evidence.md`.

The first artifact harness implementation should keep M5.3a-1 harness behavior unchanged.

## Local Target Proof Plan Confirmation

Future runner requirements:

- Require explicit local-target proof before any database action.
- Reject hosted URLs.
- Reject remote hosts.
- Reject implicit environment database targets.
- Reject automatic database URL execution.
- Reject secret-file fallback.
- Reject unknown arguments.
- Never print target values, database URLs, environment values, service-role values, provider keys, JWT/private tokens, hosted identifiers, row payloads, claim values, or auth headers.

## Allowed And Rejected Target Behavior Confirmation

Allowed future target forms:

- `localhost`
- `127.0.0.1`
- AP-approved loopback aliases
- Docker-local targets only when proven local, otherwise fail closed

Rejected future target behavior:

- Hosted URLs
- Remote hosts
- Implicit database-target execution
- Secret-file fallback
- Target value output

## SQL Static Guard Plan Confirmation

Future SQL static guard should allow only transaction-scoped assertion behavior, including local transaction setup, rollback, temp tables, transaction-scoped fixture inserts, session-claim setup, and SELECT assertions.

Future SQL static guard should block:

- `COMMIT`
- Policy changes
- Durable table changes
- Function creation
- Extension creation
- Durable grants or revokes
- Service-role behavior
- Provider calls
- Target or secret output

## Output Contract Confirmation

Future runner output must be counts-only and sanitized.

Allowed output:

- Artifact scenario contract present yes/no.
- Assertion SQL present yes/no.
- Local target gate status.
- Local DB assertion execution status.
- Scenarios declared.
- Scenarios executed.
- Pass, fail, and blocked counts.
- Artifact SELECT isolation verified yes/no.
- Sanitized classifications.
- Local target proof status without target value.

Disallowed output:

- Target values.
- Database URLs.
- Environment values.
- Service-role values.
- Provider keys.
- JWTs or private tokens.
- Hosted URLs.
- Bucket names.
- Personal identifiers.
- Row payloads.
- Claim values.
- Auth headers.

## Assertion Compatibility Plan Confirmation

Future runner should use `docs/schema/m5.3a_2_artifact_select_isolation_assertions.sql`.

Artifact SELECT isolation may be claimed only when all AP-approved artifact assertions execute and pass with sanitized output against an AP-approved reachable local database.

If no reachable AP-approved local database exists, future implementation may prove fail-closed target safety only and artifact SELECT isolation remains not verified.

## Local DB Availability Decision

M5.3a-2 closure did not verify artifact SELECT isolation. M5.3a-2a remains a planning milestone and does not connect to any database.

Future implementation evidence must distinguish executable proof from static/fail-closed behavior.

## Future Verification Paths

Executable proof path:

- Explicit local target proof.
- Reachable AP-approved local database.
- Applied migrations available locally.
- Artifact assertion SQL executes.
- All approved artifact scenarios pass.
- Sanitized output only.

Static and fail-closed path:

- No database action when target proof is missing or rejected.
- Non-zero fail-closed result if local database is unavailable.
- Artifact SELECT isolation remains not verified.

## Proof Boundary

M5.3a-2a plans an artifact assertion harness only.

A future harness implementation can prove target safety and assertion execution plumbing. It cannot prove artifact SELECT isolation unless executable artifact assertions run and pass against an AP-approved reachable local database with the M5.3a-2 artifact policies applied.

It does not prove hosted runtime readiness, production readiness, storage/export readiness, provider readiness, mutation safety, end-user workflow readiness, backup/restore readiness, operations readiness, broader certification, or legacy draft table isolation.

## AP Decisions Needed

AP must approve:

- Separate artifact runner versus generalized runner.
- Package script name, if any.
- Accepted local target forms.
- SQL static guard allow list.
- SQL static guard block list.
- Expected artifact assertion scenario count.
- Whether implementation may proceed if no reachable local database exists.
- Conditions for claiming local artifact SELECT isolation.
- Whether local Supabase configuration remains out of scope.

## Risks Carried Forward

- Reusing the M5.3a-1 harness too broadly could weaken its accepted identity and membership boundary.
- A future target parser that accepts remote or implicit values could create unsafe validation behavior.
- SQL assertions that contain durable changes could mutate local state or obscure rollback behavior.
- Static review alone is insufficient to prove artifact SELECT isolation.
- Local verification does not prove hosted runtime readiness.

## Verification Results

- `npm run typecheck`: passed.
- `npm run test`: passed. Aggregate tests generated `.agent/provider-resolver-*` output; only `.agent/provider-resolver-tests` and `.agent/provider-resolver-integration-tests` were removed before final scans.
- `npm run test:view-access-guard`: passed.
- `npm run test:view-state-persistence`: passed.
- `npm run test:module-journey-lifecycle`: passed.
- `npm run test:canonical-demo-seed`: passed.
- `npm run test:decision-pack-rendering`: passed.
- `npm run test:assess-to-studio-handoff`: passed.
- `npm run test:docs-to-delivery-lineage`: passed.
- `npm run test:buyer-demo-copy`: passed.
- `npm run test:monitor-burndown-svg`: passed.
- `npm run test:ai-boundary`: passed.
- `npm run test:ai-boundary-static`: passed with 15 patterns checked, 734 allowed hits, 0 forbidden hits, and 0 stale allowlist entries.
- `npm run test:secret-hygiene`: passed with 5 rules checked, 746 allowed classified hits, 0 forbidden hits, and 0 tracked `.env*` files.
- `npm run build`: passed.
- `npm audit --audit-level=moderate`: passed with 0 vulnerabilities.
- `npm run test:rls:m5.3a-1`: expected fail-closed non-zero result; see current M5.3a-1 harness result.
- `git diff --check`: passed.
- `git diff --cached --check`: passed after staging.
- `supabase --version`: `2.101.0`.

## Current M5.3a-1 Harness Result

`npm run test:rls:m5.3a-1` returned the expected non-zero fail-closed result without a local target:

- Local target gate: blocked.
- Local DB assertion execution: blocked.
- Environment target variable count: 1.
- Scenario contract present: yes.
- Assertion contract present: yes.
- Scenarios declared: 16.
- Scenarios executed: 0.
- Pass count: 0.
- Fail count: 0.
- Blocked count: 16.
- Tenant isolation verified: no.
- Harness status: failed-closed.
- Sanitized failure classifications: `local-target-proof-missing`, `environment-target-variable-present-not-accepted`.
- No target or secret values were printed.

## Secret Hygiene Result

`npm run test:secret-hygiene` passed with 5 rules checked, 746 allowed classified hits, 0 forbidden hits, and 0 tracked `.env*` files.

## Wording Scan Result

Repository-wide restricted wording scan returned 574 existing hits in historical migration notes, repository identity/safety references, Health separation controls, no-go/control documentation, and prior evidence-command records.

The M5.3a-2a closure evidence adds only closure status, scope confirmations, and validation records. It does not add unsupported claims or product copy that violates the product law.

## No-Secrets Confirmation

No environment values, database targets, service-role values, provider keys, tokens, hosted identifiers, row payloads, or claim values were read, printed, summarized, copied, exposed, or committed.

## Clean Git Status

Final git status will be clean after this evidence file is committed and the milestone tag is pushed.

## Recommended Next Milestone

`M5.3a-2a Artifact Assertion Harness Implementation`, only after AP accepts this plan closure.
