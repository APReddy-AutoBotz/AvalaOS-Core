# M5.1 Environment Config And Secret Hygiene Post-Merge Verification

Milestone: M5.1 Environment Config And Secret Hygiene Gate
PR number: #74
Merge commit SHA: 9456cdab8a73b5fbe75662db82d976d68a0e2cfa
Current main HEAD SHA at verification start: 9456cdab8a73b5fbe75662db82d976d68a0e2cfa
Post-merge verification commit SHA: this post-merge verification record commit; exact SHA is the tag target for `avalaos-core-m5.1-environment-config-secret-hygiene-gate` after commit creation.

## Files Verified

- `docs/security/m5.1-environment-config-secret-hygiene-gate.md`
- `docs/architecture/m5.1-environment-config-boundary-map.md`
- `docs/quality/m5.1-environment-config-secret-hygiene-evidence.md`
- `scripts/check-secret-hygiene.mjs`
- `package.json`

All five files are present on `main`.

## Merge Confirmation

- `main` was checked out and pulled from `origin/main`.
- PR #74 is merged.
- PR #74 merge commit is `9456cdab8a73b5fbe75662db82d976d68a0e2cfa`.
- Merge first parent is `4a878dfc1077fc72a8731031e707ae4e765f36c0`.
- Merge second parent is `65edc0197e3ee797e4450caf91e7be6921b91bf9`.
- Diff from first parent to merge commit added exactly the five M5.1 files and 688 lines.

## Env And Config Inventory Summary

- Browser code uses public Vite config names for Supabase URL, Supabase anon key, AI Edge enablement, and Avala AI runtime mode.
- The Supabase anon key is classified as client-public only with an RLS-first architecture and is not a service credential.
- Existing local-demo/internal-dev provider key fallback names remain classified debt in the documented legacy paths.
- Edge/server paths use server-only Supabase, database, provider, and storage-admin configuration names.
- CI smoke names remain secret-store backed in workflow configuration and were not changed.
- Browser/demo storage remains non-pilot authority.
- Ignored environment files were not opened and no secret values were inspected.

## Variable Classification Summary

- Client-public allowed: public Supabase URL, public Supabase anon key, AI Edge enablement, and Avala AI runtime mode names.
- Local-demo/internal-dev debt: existing browser provider fallback names in the documented legacy paths only.
- Server-only: service-role credentials, database URLs, access tokens, management tokens, database passwords, provider secrets, storage-admin credentials, and raw private tokens.
- CI/smoke: smoke email, password, and org identifiers stored through CI secret mechanisms.
- Provider key-reference direction: product-owned provider reference names that point to server-side secret storage only.
- Forbidden in browser: service-role credentials, database URLs, management tokens, direct provider secret values, raw private tokens, storage-admin credentials, raw provider secrets, and secret reference values unless a later AP decision explicitly changes the policy.

## Secret Hygiene Script Summary

- Uses Node built-ins only.
- Discovers files with `git ls-files --cached --others --exclude-standard`.
- Skips `.git`, `node_modules`, `dist`, `build`, `coverage`, `.turbo`, `.vite`, and `.agent`.
- Does not read ignored environment values.
- Fails tracked `.env*` paths by path only.
- Never prints matching line text or matched values.
- Prints only rule ID, file path, line number, and classification for forbidden hits.
- Exits non-zero on forbidden hits.
- Allows classified env variable names and known local-demo/internal-dev debt only where documented.

## Package Impact

- `package.json` changed only to add `test:secret-hygiene`.
- No dependencies were added.
- No CI behavior changed.
- `test:secret-hygiene` was not added to the aggregate `test` script.
- Build and runtime scripts were not otherwise changed.

## Scope Confirmations

- Confirmed docs plus one dependency-free static secret-hygiene script only.
- Confirmed no Supabase schema implementation.
- Confirmed no migrations.
- Confirmed no RLS implementation.
- Confirmed no auth/runtime implementation.
- Confirmed no deployment changes.
- Confirmed no provider runtime changes.
- Confirmed no separate Health repository changes.
- Confirmed no UI, routing, data, scoring, schema/RLS/migration, Supabase config, Supabase Edge/provider/runtime, dependency, CI, Reports/Teams/Admin, or export-policy changes.
- Confirmed no `.env*` values, Supabase tokens, provider keys, service-role values, personal project URLs, project refs, org IDs, bucket names, or secrets were read, printed, summarized, copied, or committed.

## AP Decisions Needed

- Approve future remediation timing for local-demo/internal-dev provider key fallback debt.
- Approve whether `test:secret-hygiene` should be added to CI in a later milestone.
- Approve future server-side secret manager or Supabase environment ownership model.
- Approve M5.2 auth, organization, workspace, user, and role authority direction.

## Risks Carried Forward

- Static secret scanning can produce false positives in historical/control docs; M5.1 keeps checks high-confidence.
- Existing Vite provider fallback remains local-demo/internal-dev debt until a later approved remediation.
- M5.1 improves repository safety but does not prove hosted pilot readiness by itself.
- CI integration for the new secret-hygiene scan remains deferred pending AP approval.

## Verification Results

| Command | Result |
| --- | --- |
| `npm run typecheck` | Passed. |
| `npm run test` | Passed. |
| `npm run test:view-access-guard` | Passed. |
| `npm run test:view-state-persistence` | Passed. |
| `npm run test:module-journey-lifecycle` | Passed. |
| `npm run test:canonical-demo-seed` | Passed. |
| `npm run test:decision-pack-rendering` | Passed. |
| `npm run test:assess-to-studio-handoff` | Passed. |
| `npm run test:docs-to-delivery-lineage` | Passed. |
| `npm run test:buyer-demo-copy` | Passed. |
| `npm run test:monitor-burndown-svg` | Passed. |
| `npm run test:ai-boundary` | Passed. |
| `npm run test:ai-boundary-static` | Passed. |
| `npm run test:secret-hygiene` | Passed. |
| `npm run build` | Passed. |
| `npm audit --audit-level=moderate` | Passed. Found 0 vulnerabilities. |
| `git diff --check` | Passed before this record was created; final post-record result is recorded below. |

## AI Boundary Static Scan Result

Passed.

- Patterns checked: 15
- Allowed hits: 733
- Forbidden hits: 0
- Stale allowlist entries: 0
- Generated provider-resolver test-output folders under `.agent` were removed before final scans and were not committed.

## Secret Hygiene Scan Result

Passed.

- Rules checked: 5
- Allowed classified hits: 696
- Forbidden hits: 0
- Tracked `.env*` files: 0
- Matching line text and matched values were not printed by the script.

## Wording Scan Result

Passed after review.

- Repository-wide scan returned 551 expected historical, repository-safety, separation-control, no-go/control, and evidence-command hits.
- Focused scan of this post-merge verification record returned no hits.
- No unsupported restricted claim, old product identity usage, or wording assigning deterministic scores, approvals, risk gates, recommendations, or certification status to AI was introduced by M5.1.

## Final Post-Record Checks

- `git diff --check`: passed after this record was created.
- Focused wording scan on this record: passed with no hits.

## Recommended Next Milestone

M5.2 Supabase Auth / Organization / Workspace Schema Plan.
